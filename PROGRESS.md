# 开发日志

`README.md` 讲这东西怎么用；这份讲它是怎么长成现在这样的——每一轮做了什么、
为什么这么做、踩了哪些坑。接手的人从这儿看起，不至于两眼一抹黑。

按时间倒序，最新的在最前面。

---

## 待办

- [ ] **理解性默写**（`comprehension` 板块）：给 20 篇背诵篇目写「提示语 | 答案」。
      课本里没有现成内容，得自己写。
- [ ] **译文解析**（`gloss` 板块）：背诵篇目每个句号单位一条。量最大，建议单开一轮。
- [ ] **逐篇通读校对**：目前的把关是自动校验（角标配平、3030 条词条回查正文），
      还没有逐字通读过 66 篇。
- [ ] 难字目前只能按「篇 + 字」圈，圈一处等于整篇都圈。要是想只圈某一处，
      得给 corpus 加一种「只此一处」的写法。

---

## 2026-09-19　按使用反馈改页面

用了一阵之后提的五条改进，外加文档拆分。

**篇目选择收进浮层。** 66 篇平铺占了大半屏，手机上尤其难用。改成一个「篇目」按钮，
点开一块面板：按册分五组、一行一个复选框，行末标课本的要求（背诵 / 部分背诵 / 不背），
顶上能搜篇名、能整册全选、能「只要背诵的」。收起来后按钮旁边一行字说明选了什么。

> 坑：`.overlay{display:flex}` 会盖掉 HTML 的 `hidden` 属性，浮层一直挡在页面上，
> 点什么都点不动。得单写一条 `.overlay[hidden]{display:none}`。

**挑出记不住的那几处。** 一句话挖五个空，往往只有一处想不起来，整题记成错题太粗。
现在答案显示后，标红的地方都能点，点一下标成「不会」，旁边有「全选」；一处不点
就是整句都不会。选择记进 `user.js` 的 `weak`，以后「只练错过的」时**只挖挑过的那几处**。

一题只有一个空时，选中它等于整句都不会，就不单独记了——记了也没有区分度。

**互补默写。** 结束页多一个按钮：把这一轮挖过的地方变成提示、原来的提示挖成空。
两轮合起来每个字都默一遍。不限顺序和题量，想点就点。

**难字改法：在原文上圈，不在字表里找。** 原先想的是给一张 510 字的表让人去改，
但实际用起来，人是照着纸书上圈过的错字来的——所以改成**直接在 `data.js` 的 corpus
里加减大括号**。`build.py` 重跑之前先把手圈的读出来，记进 `tools/hardchars.tsv`
这本账，再原样带回生成结果。

> 这里的权威关系要拎清：**`data.js` 是权威**（人在那儿圈），`hardchars.tsv` 是账本和
> 备份（能看出加了哪些、删了哪些），只有 `data.js` 不在时（比如刚 clone 下来）才反过来
> 拿账本还原。反过来设计——让人改账本——就会和「在原文上圈」打架。

**标记和错题能看了。** 原先只有「筛选 → 只练标记过的」能练，没地方查。现在
「我的数据」面板里列出来：篇名 + 句子，错题带错了几次、挑出了几处记不住，
可以逐条移出，也能一键「只练这些」。没塞进篇目面板——那是选范围的，标记是跨篇的。

**文档拆两份。** `README.md` 只讲怎么用（面向使用者和陌生人），这份 `PROGRESS.md`
记开发过程。

---

## 2026-09-18　还原词曲的句读与分段

抽查时发现三处失真，都和课本版式有关。

**词、歌行、骚体在课本里是连排的**，一行排满就换行，物理行并不是句子。照搬下来，
《念奴娇》头一题成了「大江东去，浪淘尽，千古风流人物。故垒」——句子是残的，
20 篇都这样。改成按句末标点重新断行，一行还它一句；律诗本来就一句一行，不动。

**词的上下阕之间课本留一道白**，宽度和缺字的空洞一模一样，程序分不出来。原先当成
换行丢掉了，八首词的上下阕连成一片。现在统一报成空洞，由补字表点名：是留白就补
`¶` 据此断段，是缺字就补字。

> 补字表也从「按序号认位置」改成「按上下文认」——空洞的个数会随版面处理变动，
> 序号一漂，补的字就全错位。

**《侍坐》整篇并成了一段。** 这篇全是短对话，段首行比续行还多，段落基准线按众数算
就落到缩进那一列上去了。改取左边界（出现两次以上的最小 x）。

顺带调了分题：课本里极短的段（《侍坐》的「夫子哂之。」「“求！尔何如？”」各自成段）
不足六字的并进下一段。门槛压得比分题的十字低——**段落是课本分的，能不动就不动**：
《劝学》的「君子曰：学不可以已。」八个字也是一段，独立成题不算短，就没并走。

「看原文」的注释角标改成整篇连着编，跟课本一样。

---

## 2026-09-18　填入五册古诗文，页面加「看原文」和背诵范围

`data.js` 由 `tools/` 从课本 PDF 生成：66 篇，其中课本要求背诵的 20 篇，
3030 条注释，510 个难字。原来那份是打样用的示例，整个重做。

`index.html` 四处改动：

- **篇头多第四项标背诵范围**：`背` / `背1`（只背第 1 段）/ `不背`。全文都存着，
  默写只挑要背的段出题，不要求背的篇目照样能看原文、能考注释。不写就当成要背，
  旧的三项写法照样能用。
- **难字表可以带拼音**：`衿(jīn)`。课本注了音的是难读字，难字默写看答案时显示；
  字形生僻的是难写字，单写一个字。
- **文的分题加字数下限**：按句号切完，不足十个字的并进下一题。诗仍是一行一题。
- **新增「看原文」模式**：仿课本版式，整篇按段排，被注的词带角标，注释跟在每段后面，
  要背的段左边一道朱线，顶上「只看白文」开关可以自测。

> 常用多音字（不、说、读、还）课本也注音，但那是随语境变读，不是字难写。
> 一进难字表，正文里每个「不」都要被圈，所以按字频（全书 60 次以上）挡掉。

篇目选择区按册分行——六十多篇平铺实在太长。（这一版还是平铺，浮层是下一轮改的。）

---

## 2026-09-18　生成 data.js 三个板块，用注释词条反查正文漏字

`tools/build.py` 把中间稿转成 `corpus` / `hardChars` / `notes`。

**注释靠 `⟦n⟧` 定位**——角标标出了这条注释挂在正文哪个字上，例句就从那里取。
跨句的长词条（〔呦呦鹿鸣……鼓瑟吹笙〕）整个裹进例句，不切一半。课本长注释里套着的
小注（「衿，衣服的交领」）各拆一条，单字也考得到。

**难字分两路**：课本注了音的带着拼音进表，多音节注音按音节拆到每个字头上
（踟蹰 chíchú → 踟 chí、蹰 chú）；全书出现不过三两次的也收。

### 这一轮最要紧的东西：交叉校验

**课本注了什么，正文就该有什么。** 3030 条注释的词条逐条回正文里核对，对不上的
当场报出来。这道关口抓出了空洞检测看不见的漏字：

| 篇目 | 漏的字 | 在哪 |
|---|---|---|
| 劝学 | 輮（輮以为轮） | 行首 |
| 庖丁解牛 | 軱（而况大軱乎） | 行首 |
| 谏逐客书 | 駃騠（骏良駃騠） | 行尾 |
| 五石之瓠 | 絖（世世以洴澼絖） | 行尾 |

这些都在行首或行尾，两字之间没有空当，空洞检测看不见，除了这么查没有别的办法发现。

### 顺带修好的版面问题

- **角标不参与分行，却占着位置**。它让出的空当被误认成跨栏，把《离骚》拦腰切开。
  切分时要把紧跟在字后面的角标算作占位。
- **分栏原先看区间连通**，可插图说明常横跨中缝，一下把左右两栏连成一片，
  《过秦论》的注释因此读串了十四条。改成认栏的起笔对齐。
- **段首要缩进两格，但上一行必须是排不满的段末行**。否则一行的头一个字是画上去的
  （「軱」），整行看着就像缩进了。

---

## 2026-09-18　提取教科书 PDF：版面解析工具

把五册教科书里的古诗文连同课本注释抽出来。这一步只做忠实搬运，不做语文上的判断。

### `tools/pdfdump.py`　按字体切版面

方正的字体成对出现：**JW 是 GB1 基本集，K 是 GBK 扩展集**，同一款字的两套字库，
生僻字会掉到 K 那一套里去。只认一半就会漏字、错分区块——《涉江采芙蓉》整段学习提示
掉进正文，就是因为楷体只匹配了 `FZKTJW` 没匹配 `FZKTK`。

注释角标有专用字体（`RopeSequenceNumberST-R`），但**字形在正文和注释两边对不上**：
正文印 w x y z，注释那边抽出来是 @3 @4。所以一律不认字面，只认出现次序，用 `⟦n⟧` 配对。

版面重排绕开三个坑：

1. **角标是上标，PDF 里自成一行**，按行读会把正文切碎、次序错乱。丢掉 PDF 的行结构，
   按基线重新聚行。
2. **注释有时排得极满，左右两栏只隔两三个点**（必修下 p16 只隔 2 点），横向投影切不开。
   反过来做才稳：先按基线聚行，一「行」里真出现二十点以上的空当就是跨了栏，就地切开；
   切出来的片段再按横向重叠归栏。
3. **区块归属按行投票，不按单字**。否则行内的阿拉伯数字（「背诵课文第 1 段」的 1 用的是
   Times，没有中文字体可认）会掉进别的区块，还在行中留下空当，又被当成跨栏切一刀。

### `tools/assemble.py`　按篇合并

一篇常跨数页而角标每页重编号，先按页配对再整篇重排。

**学习提示一框常管着好几篇，而且印在这组课文的末尾**，所以要反过来找：提示里点了名的
按名字认领，只说「背诵课文」的归紧挨着它的那一篇。

> 坑：得限定在本篇附近。《哈姆莱特》导读里有一句「最好能背诵」，不设范围会顺着
> 「课文」二字认领到三十页开外的《鸿门宴》头上。

### `tools/glyphs.tsv`　补字表

**教科书里十来个生僻字在 PDF 的文本层里根本不存在**——排版时是画上去的，抽出来
只剩一个空洞。輮、薆、騞、軱、駃騠、絖、鞿、纕、巉、巘、餔、摐，逐个照着课本的
注音和上下文认定后按位置补回。行首行尾掉的字撑不出空洞，另行点名。

### 校验

66 篇正文角标与课本注释条数全部一一配平。
