# 开发日志

`README.md` 讲这东西**怎么用**；这份讲它**是怎么长成现在这样的**——每一轮做了什么、
为什么这么做、踩了哪些坑、哪些判断是有意为之。接手的人从这儿看起，不至于两眼一抹黑。

前两节是不随时间变的底子（要解决什么问题、东西是怎么搭的、哪几条原则反复起作用），
之后按时间倒序记账，最新的在最前面。

---

## 一、这东西要解决什么

高中语文五册课本（人教版，必修上下 + 选择性必修上中下）里的古诗文，做成一个能离线
用的默写本。最初的要求是这么几条：

1. **课文从 PDF 里抽**，不手抄，不从网上找。PDF 解析工具自己写。
2. **只要古诗文**。凡是古诗文都收，不管出现在正课、单元末的「古诗词诵读」还是别处；
   但**背诵与否只看正课课文**，从课文后面的「学习提示」里解析——里面出现「背诵……」
   就是背诵篇目，否则不是。
3. **注释逐字照抄**，不改写、不概括。背诵篇目和非背诵篇目都要。
4. 背诵篇目按句号切成一题一题，默写时挖掉逗号分号切出来的短句。
5. 非背诵篇目不出默写题，但注释题要给出原文例句——一题考一个词，句子里那个词标出来。

后来陆续加的：看原文模式、难字（手圈）、词语（看拼音写词／看词写拼音）、
互补默写、错题精确到某一处空。

贯穿始终的一条原则，是使用者原话：

> **力求保证完全和书上一样，无偏差。**

这句话在下面每一轮里都在起作用。举两个例子：《梦游天姥吟留别》末句课本印的是
「使我不得开心颜？」，通行本作「！」——查了 PDF 里的字形，课本确实是问号，照留；
《扬州慢》的序里「千岩老人以为有黍离之悲也」，通行本给《黍离》加书名号，课本没加，
量了字宽确认没有加的余地，也照留。

---

## 二、东西是怎么搭的

### 数据的流向

```
五册 PDF
   │
   │  tools/pdfdump.py      按字体切版面，一页一份 JSON
   ↓                        （正文／注释／学习提示／篇名／插图说明各归各位）
extract/raw/*.json
   │
   │  tools/assemble.py     按篇合并，跨页角标重编号，解析背诵要求
   ↓
extract/篇/*.md             一篇一份中间稿，人工校对看这个
   │
   │  tools/words.py        顺带抽词语表 → extract/词语.tsv
   │  tools/build.py        生成 data.js 的六个板块
   ↓
data.js
   │
   │  merge.py              index.html + data.js + user.js 并成一个文件
   ↓
古诗文默写_单文件.html       拷到手机上就能用
```

三份人工维护的表在 `tools/` 下：

| 文件 | 谁写的 | 管什么 |
|---|---|---|
| `pieces.tsv` | 人工圈定 | 哪 66 篇是古诗文：册、起始页、篇名、作者、体裁 |
| `glyphs.tsv` | 人工认字 | PDF 文本层里不存在的十来个字，按上下文补回；顺便记词的换头留白 |
| `paras.tsv` | 通读时记 | 版面上看不出来的分段点（跨页分段） |
| `hardchars.tsv` | 脚本自动维护 | 手圈难字的账本，见下面「权威关系」 |

`tools/data/` 下是外来参考资料，只作判据，不直接进 `data.js`：

- `常用字-一级3500.txt`　《通用规范汉字表》一级字表。**难写字就按它判**。
- `成语表.tsv`、`词语表.tsv`　第三方词典的词条和读音，只用来定「哪些算一个词、
  标准怎么读」，词本身还得在课本语料里出现过才收。

### 分工：生成侧和页面侧

- **`tools/`** 负责一切和课本版面有关的判断：哪些字属于正文、注释挂在哪个字上、
  段落在哪儿断、哪个字难写。判断的结果全部落到 `data.js` 这个纯文本里。
- **`index.html`** 只认 `data.js` 的格式，不知道 PDF 的存在。出题、挖空、
  记错题、导入导出都在这边。

两边有一处必须对齐：**切句的规则**。`build.py` 切句是为了给注释配例句，
`index.html` 切句是为了出题，两边切法不一样就会出现「注释的例句在题库里找不到对应题」。
2026-09-19 那一轮就修了这个（见下）。

### 几条反复起作用的原则

**权威关系要拎清。** 难字是使用者在 `data.js` 的正文里加大括号圈出来的，所以
**`data.js` 是权威**；`tools/hardchars.tsv` 是账本和备份，记的是「你加了哪些、
又把哪些自动判定的字删了」。重跑 `build.py` 时先把 `data.js` 里的括号读出来
（`harvest()`），生成时原样带回去。只有 `data.js` 不在（刚 clone 下来）才反过来
拿账本还原。**反过来设计——让人去改账本——就会和「在原文上圈」这件事打架。**

改判定规则时要加 `--regen`，否则 `harvest()` 会把上一版的自动结果当成手圈留下来，
新规则等于白改。

**自检比抽查管用。** 有三道自检天天在跑：

1. 每篇正文里的角标数必须和课本注释条数一样。对不上就是版面读错了。
2. 每条注释的词条都要能在正文里找到。课本注了什么，正文就得有什么。
3. 每条注释的例句都要是正文里切出来的完整句子，释义要用句末标点收口。

第 2 条最灵——它抓出的漏字是任何「看一眼」都发现不了的（见 2026-09-18 那一轮）。
第 3 条是通读校对时补的，一上来就抓出 13 条注释里混进了插图说明。

**版面判断要找「成对出现」的信号，别写死一个阈值。** 这是踩了一路坑之后的总结：
「段首缩进 24 点」写死就会死在有插图的那一页（那页有两套版心）；
「注释第二栏顶上的行是上一栏续下来的」写死就会把图注收进注释。
可靠的做法是找相对关系——某个起笔正好比另一个靠右两格、某行的起笔和本栏第一条
注释对得上——这些关系在版面变的时候还成立。

---

## 三、待办

- [ ] **理解性默写**（`comprehension` 板块）：给 20 篇背诵篇目写「提示语 | 答案」。
      课本里没有现成内容，得自己写。
- [ ] **译文解析**（`gloss` 板块）：背诵篇目每个句号单位一条。量最大，建议单开一轮。
- [ ] 难字目前只能按「篇 + 字」圈，圈一处等于整篇都圈。要是想只圈某一处，
      得给 corpus 加一种「只此一处」的写法。
- [ ] 连排的长篇（《琵琶行》《孔雀东南飞》《蜀道难》）按句末标点断行，
      会把一个对句从中间切开（「其间旦暮闻何物？」「杜鹃啼血猿哀鸣。」各成一题）。
      这三篇都不要求背诵，不出默写题，所以没动；将来要是拿它们出题，得按字数把
      明显偏短的行并回去。

---

## 2026-09-19（三）　逐篇通读校对五册

这一轮是**用眼睛看**——之前的把关全是自动校验（角标配平、词条回查正文），
一个字一个字地读还是头一回。读的是生成出来的 `data.js`，不是中间稿：
诗词的重新断行发生在 `build.py` 里，只有读最终结果才算数。

读法是「读 + 起疑 + 回 PDF 查证」：正文通读一遍，凡是看着别扭的地方
（标点怪、句子长得不对、段落数和常识对不上），都回到 PDF 的字形和坐标上核实，
确认是课本如此就留着，确认是解析错了就改生成侧的规则。

查出来的问题分八类，都不是个例——每一类都牵着一串篇目：

### 1. 插图说明串进注释（18 条）

课本的图题和注释同为 9 号宋体，字体上分不开，只能靠版面。原先的规矩是
「注释一定从角标起头，每栏里排在第一个角标之前的是图题」，但只对第一栏有效——
后面几栏栏顶的行一概当成上一栏末条注释续过来的。于是：

- 《赤壁赋》〔舳舻〕后面跟着「赤壁图　［金］武元直作」
- 《侍坐》〔冠者〕的释义中间夹进半句「宋刘氏天香书院刻本《论语》书影」
- 《琵琶行》《声声慢》《鸿门宴》《促织》《阿房宫赋》《苏武传》《望海潮》
  《归去来兮辞》……一共 13 条

第一次修：图题是右对齐的，续行左边齐着栏沿，按起笔位置分开——这 13 条干净了。
然后第三道自检（释义必须用句末标点收口）又报出 5 条：《念奴娇》的
「赤壁图傅抱石作」、《与妻书》的「福州林觉民故居的林觉民烈士塑像」、
《春江花月夜》《过秦论》《蜀道难》的图题。这些排在栏底，或者整整一栏就它一行，
按「本栏最左边的起笔」根本量不出来。

第二次修换了基准：**拿本栏第一条注释的起笔当尺子**。续行挂在它下面只多缩三四点，
图题差着五十到一百点；整栏一条注释都没有的，那一栏就全是图题。

> 中间试过一版「上一栏的注释已经用句号收了口，就没什么可续的」，看着挺有道理，
> 结果把《苏武传》〔前长君为奉车〕的续行「长君，指苏武的大哥苏嘉。奉车，……」
> 判成了图题——课本的注释常常是「主释义。子目甲，……。子目乙，……。」这种结构，
> 句号收口完全不代表说完了。**别拿语义当版面判据。**

### 2. 引号里不断句（386 条注释例句 + 一批正文行）

切句的代码有个引号深度计数，进了引号就不切，等收口再说。本意是不要把
「语人曰：‘我不能。’」拆开，实际后果是：

- 《孔雀东南飞》刘兰芝一开口二十多句，全在一对引号里，连成一行 **592 个字**；
- 《赤壁赋》客人那段话几百字，〔舳舻〕这条注释的例句就成了整整一段。

超长例句一共 386 条。而页面那边（`index.html` 的 `buildQuestions`）本来就是
见句号就切、末尾的引号补回上一句——两边规则不一致。统一成页面那套，
超长例句 386 → 0。这就是前面说的「两边必须对齐」。

### 3. 齐头排的诗丢了分段（8 篇）

`pieces.tsv` 里把《离骚（节选）》《孔雀东南飞》《琵琶行》归在「诗」，
于是分段走的是「看行距跳变」那一路。可这几篇在课本里的版面跟散文一模一样：
连排、两端对齐、段首缩进两格——分段全在缩进上，行距一点破绽都没有。结果：

- **《离骚（节选）》整篇并成一段**。这一篇课本要求「背诵第3段」，
  段都没了，背诵范围整个落空，默写里根本出不来这一篇。
- 《孔雀东南飞》是按说话人分段的，33 段并成 1 段。
- 《氓》少了一处章与章之间的分界（第四章和第五章隔着翻页），六章成了五章。
- 《扬州慢》《蜀道难》《梦游天姥吟留别》《将进酒》《琵琶行》的分段也都不对。

怎么认「这篇是齐头排的」？**看右边，不看左边**：《沁园春·长沙》整块左对齐、
右边参差，左边一样齐，右边只有 8% 的行顶到头；《离骚》有 77%。行长整齐的律诗
两边都齐，但那样每行缩进都是 0，走哪条路结果一样，不会判错。

### 4. 小序的末行被当成署名丢掉（《扬州慢》）

仿宋这个字体既排署名也排小序，原先一行一行按长短判：超过八个字的是序，还给正文。
《扬州慢》的序结在「之悲也。」四个字上，这一行连同它带的那条注释一起没了。
改成先按行距把仿宋行连成一段一段（序的行距 19 点，跟署名隔着两行的距离），
**段里只要有一行成句，整段都是序**。

### 5. 有插图的那一页有两套版心（《论语》十二章）

那一页右边嵌着插图，前十二行缩在插图旁边排，最后两行才用整行的宽度，
两套左边沿差着八十多点。段落基准线只取一条，「子曰：“譬如为山」就接到了
上一章的尾巴上，**十二章成了十一章**。

改成找成对的起笔：某个起笔正好比另一个靠右两格，它就是段首。
顺带发现行首带引号的时候课本缩得窄一点——《侍坐》的「“赤！尔何如？”」只缩 18 点
不是 24 点，容差要覆盖到。

### 6. 换头留白的第三种写法（《虞美人》《江城子》）

词的上下阕之间，课本有三种留白写法：

1. 版面上空出一大块——`pdfdump` 能看出来，报成空洞，由 `glyphs.tsv` 补 `¶`；
2. **跨页**换头——翻页之后纵坐标一重置就看不出来了，只能在 `paras.tsv` 里点名；
3. **两个全角空格**——字面上就带着，「故国不堪回首月明中。　　雕栏玉砌应犹在」。

第三种以前没处理过，两首词的上下阕连成一片。全书扫了一遍，确实只有《虞美人》
和《江城子·乙卯正月二十日夜记梦》这两处，其余的全角空格都是练习题编号里的。

### 7. 一个半角叹号（《兰亭集序》）

「岂不痛哉!」这个叹号排的是 Times 的半角 `!`，宽 4 点，正文的全角标点是 12 点。
印在纸上照样是个叹号，可**半角的话断句时认不出来**，这一句就不成句。
全书就这一处，在 `squeeze()` 里按全角收。

> 判断依据：这是 PDF 排版的疏漏，不是课本有意为之的内容差异——纸面上读者看到的
> 就是一个叹号。跟《梦游天姥》那个问号不是一回事，那个是课本选定的标点。

### 8. 注音切音节切错（《归去来兮辞》）

「葳蕤（wēiruí）」被切成 `wēir` / `uí`，两个字的注音都错了。音节正则允许 `r`
当韵尾（儿化），可后面跟着元音的那个 `r` 是下一个音节的声母。加了个否定前瞻。

借这个机会把全书 200 多个注音逐条比对了字典，剩下两处不一致是课本自己的读法，
照课本：《蜀道难》「噫吁嚱」注 `xī`（字典 `xì`）、《赤壁赋》「山川相缪」注 `liáo`
（同「缭」）。另外课本注音里混着国际音标的 `ɡ`（U+0261），摐、毂、洴、絖、舸
五个字和两个词条都中招，统一成普通的 `g`。

### 这一轮之后的自检结果

- 66 篇角标与注释条数全部配平
- 3031 条注释的词条全部能在正文里找到
- 3031 条注释的释义全部用句末标点收口，例句全部是正文里切出来的完整句子
- 《离骚》要背的十四题正是「长太息以掩涕兮」到「固前圣之所厚」，
  《师说》第 1 段、《屈原列传》第 3 段都对得上
- 浏览器实测（桌面 1280、手机 390）六种模式都能开，无控制台报错

> 附带一个坑：课本用一行「……」表示跳过的段落，那一行版面上也缩进两格，
> 看着像一个段。可它不是段——数成一段的话，《离骚》的「第3段」就落到省略号头上了。
> 现在把它并回上一段的末尾，照样显示，不占段号；页面上跳过没有汉字的题。

---

## 2026-09-19（二）　难字改用国标字表判定，新增词语模式

**难写字的判据从「字频」换成《通用规范汉字表》一级字表。** 原先是「全书出现不过
三两次就算难写」，但字频和难写其实是两回事：「樽」「衿」「掇」「觥」在课本里
出现七八次，按字频不算低，可都是二级（次常用）字，写起来照样容易错；反过来
「俭」「偿」「剖」「堤」在课本里也只出现几次，却都是一级常用字，不该当难写字考。
换成「古诗文正文里凡是不在那 3500 常用字内的就算难写」，难字从 510 涨到 579。

> 换判定规则必须跑 `build.py --regen`，否则 `harvest()` 把上一版的结果当成手圈
> 留下来，新规则等于没换。

**新增词语模式。** 课本里没有现成的字表词表，所以三个来源合起来，按可靠性排序，
前面的覆盖后面的：

1. **课本注音的词条**（188 条）——最权威。《苏武传》的「阏氏」读 `yān zhī`，
   两本第三方词典都注成 `è shì`，只有课本是对的。
2. **课本里出现过的成语**（209 条）——成语表给边界和读音，课本语料保证这套教材里
   真的用到。
3. **课本里出现过的难写词**（923 条）——词表 ∩ 课本语料 ∩ 含生僻字，
   且两个独立来源的注音对得上才收，对不上的宁可丢掉。

题型按使用者定的规矩分：**难读的看字写拼音，难写的看拼音写词语**。
「难读」= 课本特地注过音的，或者词里有字变了读音（「字里行间」的「行」读 háng）；
「难写」= 含《通用规范汉字表》一级字表之外的字。两样都占的出「都考」。
「一个 yí gè」那种变调、「觉得 jué de」那种轻声不算变读。

> 坑：问读音的时候，字得是光秃秃的——难字表里带着拼音，一渲染就把答案漏了。
> 词语模式里的「读」题不能走 `renderHard`。

> 坑：「刽子」是「刽子手」被切下来的半截，词表里两个都有。课本里既然总是连着出现，
> 就只留长的那个。「的的」这种常用字叠字也不算词。

---

## 2026-09-19（一）　按使用反馈改页面

用了一阵之后提的五条改进，外加文档拆分。

**篇目选择收进浮层。** 66 篇平铺占了大半屏，手机上尤其难用。改成一个「篇目」按钮，
点开一块面板：按册分五组、一行一个复选框，行末标课本的要求（背诵 / 部分背诵 / 不背），
顶上能搜篇名、能整册全选、能「只要背诵的」。收起来后按钮旁边一行字说明选了什么。

> 坑：`.overlay{display:flex}` 会盖掉 HTML 的 `hidden` 属性，浮层一直挡在页面上，
> 点什么都点不动。得单写一条 `.overlay[hidden]{display:none}`。

**挑出记不住的那几处。** 一句话挖五个空，往往只有一处想不起来，整题记成错题太粗。
现在答案显示后，标红的地方都能点，点一下标成「不会」，旁边有「全选」；一处不点
就是整句都不会。选择记进 `user.js` 的 `weak`，以后「只练错过的」时**只挖挑过的那几处**。

一题只有一个空时，选中它等于整句都不会，就不单独记了——记了也没有区分度。

**互补默写。** 结束页多一个按钮：把这一轮挖过的地方变成提示、原来的提示挖成空。
两轮合起来每个字都默一遍。不限顺序和题量，想点就点。

**难字改法：在原文上圈，不在字表里找。** 原先想的是给一张 510 字的表让人去改，
但实际用起来，人是照着纸书上圈过的错字来的——所以改成**直接在 `data.js` 的 corpus
里加减大括号**。使用者原话：「我在原文上加大括号就行了，我现在是纸质版的书，
上面我把错字圈起来了。」`build.py` 重跑之前先把手圈的读出来，记进
`tools/hardchars.tsv` 这本账，再原样带回生成结果。权威关系见第二节。

**标记和错题能看了。** 原先只有「筛选 → 只练标记过的」能练，没地方查。现在
「我的数据」面板里列出来：篇名 + 句子，错题带错了几次、挑出了几处记不住，
可以逐条移出，也能一键「只练这些」。没塞进篇目面板——那是选范围的，标记是跨篇的。

标记按**句子内容**存，不按题号，所以往 `data.js` 中间插内容不会让标记错位。

**文档拆两份。** `README.md` 只讲怎么用（面向使用者和陌生人），这份 `PROGRESS.md`
记开发过程。

---

## 2026-09-18（四）　还原词曲的句读与分段

抽查时发现三处失真，都和课本版式有关。

**词、歌行、骚体在课本里是连排的**，一行排满就换行，物理行并不是句子。照搬下来，
《念奴娇》头一题成了「大江东去，浪淘尽，千古风流人物。故垒」——句子是残的，
20 篇都这样。改成按句末标点重新断行，一行还它一句；律诗本来就一句一行，不动。

> 判「是不是连排」用的是「行末落在句末标点上的比例」，后来发现这条不够——
> 《念奴娇·过洞庭》上阕排了两行，一行两句，碰巧都断在句号上，就被当成一句一行
> 放过了。2026-09-19 那一轮补了「一行最多十五六个字」这条：真正一句一行的是
> 律诗绝句，排满一行才折的，二十几个字打不住。

**词的上下阕之间课本留一道白**，宽度和缺字的空洞一模一样，程序分不出来。原先当成
换行丢掉了，八首词的上下阕连成一片。现在统一报成空洞，由补字表点名：是留白就补
`¶` 据此断段，是缺字就补字。

> 补字表也从「按序号认位置」改成「按上下文认」——空洞的个数会随版面处理变动，
> 序号一漂，补的字就全错位。

**《侍坐》整篇并成了一段。** 这篇全是短对话，段首行比续行还多，段落基准线按众数算
就落到缩进那一列上去了。改取左边界（出现两次以上的最小 x）。

顺带调了分题：课本里极短的段（《侍坐》的「夫子哂之。」「“求！尔何如？”」各自成段）
不足六字的并进下一段。门槛压得比分题的十字低——**段落是课本分的，能不动就不动**：
《劝学》的「君子曰：学不可以已。」八个字也是一段，独立成题不算短，就没并走。

「看原文」的注释角标改成整篇连着编，跟课本一样。

---

## 2026-09-18（三）　填入五册古诗文，页面加「看原文」和背诵范围

`data.js` 由 `tools/` 从课本 PDF 生成：66 篇，其中课本要求背诵的 20 篇，
3030 条注释，510 个难字。原来那份是打样用的示例，使用者说「我这些语料是网上搜的，
注释也是网上搜的，都不是很准确，你直接重新做一遍」，所以整个重做。

`index.html` 四处改动：

- **篇头多第四项标背诵范围**：`背` / `背1`（只背第 1 段）/ `不背`。全文都存着，
  默写只挑要背的段出题，不要求背的篇目照样能看原文、能考注释。不写就当成要背，
  旧的三项写法照样能用。
- **难字表可以带拼音**：`衿(jīn)`。课本注了音的是难读字，难字默写看答案时显示；
  字形生僻的是难写字，单写一个字。
- **文的分题加字数下限**：按句号切完，不足十个字的并进下一题。诗仍是一行一题。
  （这个十字是使用者定的：「古诗是很确定的，就是格律对仗，以句号为终止，
  古文就十个字吧」。）
- **新增「看原文」模式**：仿课本版式，整篇按段排，被注的词带角标，注释跟在每段后面，
  要背的段左边一道朱线，顶上「只看白文」开关可以自测。

> 常用多音字（不、说、读、还）课本也注音，但那是随语境变读，不是字难写。
> 一进难字表，正文里每个「不」都要被圈，所以按字频（全书 60 次以上）挡掉。
> （这一版难字还是按字频判的，下一轮换成了国标字表。）

篇目选择区按册分行——六十多篇平铺实在太长。（这一版还是平铺，浮层是下一轮改的。）

---

## 2026-09-18（二）　生成 data.js 三个板块，用注释词条反查正文漏字

`tools/build.py` 把中间稿转成 `corpus` / `hardChars` / `notes`。

**注释靠 `⟦n⟧` 定位**——角标标出了这条注释挂在正文哪个字上，例句就从那里取。
跨句的长词条（〔呦呦鹿鸣……鼓瑟吹笙〕）整个裹进例句，不切一半。课本长注释里套着的
小注（「衿，衣服的交领」）各拆一条，单字也考得到；「看原文」里只显示主条目，
跟课本一个样。

**难字分两路**：课本注了音的带着拼音进表，多音节注音按音节拆到每个字头上
（踟蹰 chíchú → 踟 chí、蹰 chú）；全书出现不过三两次的也收。

### 这一轮最要紧的东西：交叉校验

**课本注了什么，正文就该有什么。** 3030 条注释的词条逐条回正文里核对，对不上的
当场报出来。这道关口抓出了空洞检测看不见的漏字：

| 篇目 | 漏的字 | 在哪 |
|---|---|---|
| 劝学 | 輮（輮以为轮） | 行首 |
| 庖丁解牛 | 軱（而况大軱乎） | 行首 |
| 谏逐客书 | 駃騠（骏良駃騠） | 行尾 |
| 五石之瓠 | 絖（世世以洴澼絖） | 行尾 |

这些都在行首或行尾，两字之间没有空当，空洞检测看不见，除了这么查没有别的办法发现。

### 顺带修好的版面问题

- **角标不参与分行，却占着位置**。它让出的空当被误认成跨栏，把《离骚》拦腰切开。
  切分时要把紧跟在字后面的角标算作占位。
- **分栏原先看区间连通**，可插图说明常横跨中缝，一下把左右两栏连成一片，
  《过秦论》的注释因此读串了十四条。改成认栏的起笔对齐。
- **段首要缩进两格，但上一行必须是排不满的段末行**。否则一行的头一个字是画上去的
  （「軱」），整行看着就像缩进了。
- **《孔雀东南飞》第一条注释用的是普通的「①」**，不是那套专用角标字体。
  角标配对因此从「按数值算」改成「按出现次序配」。

---

## 2026-09-18（一）　提取教科书 PDF：版面解析工具

把五册教科书里的古诗文连同课本注释抽出来。这一步只做忠实搬运，不做语文上的判断。
用的是 PyMuPDF（`pymupdf`），拿的是 span 级的字体、字号、坐标。

### `tools/pdfdump.py`　按字体切版面

版面里认字体，不认位置。教科书用的方正字体各有分工：

| 字体 | 排什么 |
|---|---|
| `FZSSJW` / `FZSSK` | 正文宋体 |
| `FZKTJW` / `FZKTK` / `KaiTi_GB2312` | 学习提示、旁批 |
| `FZFSJW` / `FZFSK` | 署名、小序 |
| `FZZHUNYSK` | 课文篇名 |
| `FZZHUNYSJW` | 课序号、单元名 |
| `RopeSequenceNumberST-R` | 注释角标 |
| `NEU-XT-Regular` | 注音用的拉丁字体 |
| `FZZDXJW` | 页码 |

**方正的字体成对出现：JW 是 GB1 基本集，K 是 GBK 扩展集**，同一款字的两套字库，
生僻字会掉到 K 那一套里去。只认一半就会漏字、错分区块——《涉江采芙蓉》整段学习提示
掉进正文，就是因为楷体只匹配了 `FZKTJW` 没匹配 `FZKTK`。

注释角标虽有专用字体，但**字形在正文和注释两边对不上**：正文印 w x y z，
注释那边抽出来是 @3 @4。所以一律不认字面，只认出现次序，用 `⟦n⟧` 配对。

版面重排绕开三个坑：

1. **角标是上标，PDF 里自成一行**，按行读会把正文切碎、次序错乱。丢掉 PDF 的行结构，
   按基线重新聚行。角标另行按最近邻认领回去——认领的纵向容差得随字号放缩：
   贴 12 号正文抬 8 点，贴 18 号标题抬 18 点，写死就会认错。
2. **注释有时排得极满，左右两栏只隔两三个点**（必修下 p16 只隔 2 点），横向投影切不开。
   反过来做才稳：先按基线聚行，一「行」里真出现二十点以上的空当就是跨了栏，就地切开；
   切出来的片段再按横向重叠归栏。栏与栏之间挨得太近（《过秦论》那页只隔 15 点）
   二十点切不开，就反过来用「哪几个横坐标反复当行首」——那就是栏的起笔。
3. **区块归属按行投票，不按单字**。否则行内的阿拉伯数字（「背诵课文第 1 段」的 1 用的是
   Times，没有中文字体可认）会掉进别的区块，还在行中留下空当，又被当成跨栏切一刀。
   孤立的标点（《诗经·邶风》里那个间隔点）也一样，按最近邻认领。

### `tools/assemble.py`　按篇合并

一篇常跨数页而角标每页重编号，先按页配对再整篇重排。一页上也可能并排着两篇
（《芣苢》和《插秧歌》），按篇名的纵坐标切开。

**学习提示一框常管着好几篇，而且印在这组课文的末尾**，所以要反过来找：提示里点了名的
按名字认领，只说「背诵课文」的归紧挨着它的那一篇。课本一共七八种写法，都得认：

```
背诵课文。                       背诵《短歌行》。
背诵《梦游天姥吟留别》和《登高》。      背诵《阿房宫赋》《六国论》全篇。
背诵课文《劝学》全篇和《师说》的第1段。  背诵《离骚》（节选）第3段。
背诵课文第3段。                   背诵《〈论语〉十二章》和《大学之道》。
```

> 坑：得限定在本篇附近。《哈姆莱特》导读里有一句「最好能背诵」，不设范围会顺着
> 「课文」二字认领到三十页开外的《鸿门宴》头上。

> 坑：篇名要对得上清单里的写法。「《〈论语〉十二章》」和清单里的「《论语》十二章」
> 是同一篇，「背诵《离骚》第3段」要认上「离骚（节选）」。

### `tools/glyphs.tsv`　补字表

**教科书里十来个生僻字在 PDF 的文本层里根本不存在**——排版时是画上去的，抽出来
只剩一个空洞。輮、薆、騞、軱、駃騠、絖、鞿、纕、巉、巘、餔、摐，逐个照着课本的
注音和上下文认定后按位置补回。行首行尾掉的字撑不出空洞，另行点名。

另有两个私有区造字按字形认定：囷（《阿房宫赋》「盘盘焉，囷囷焉」）、
趱（《林教头风雪山神庙》，现代文用不上，一并记着免得漏）。

### 校验

66 篇正文角标与课本注释条数全部一一配平。
