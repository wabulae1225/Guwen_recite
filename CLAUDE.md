# CLAUDE.md

古诗文默写——高中语文五册课本古诗文的离线默写本（`index.html` + `data.js`，
语料由 `tools/` 从课本 PDF 生成）。

> **动手前必须先读 [`modify_manual.md`](modify_manual.md)**（工作流程和停止点）。
> 未经使用者确认，不得跨越手册里标 🛑 的节点。

索引（详见 [`README.md`](README.md) 第七节「仓库里都有什么」）：

- 语料格式 → `data_structure.md`
- 页面程序 → `html_structure.md`
- PDF 解析 → `extract_pdf_method.md`
- 历史（只追加）→ `LOG.md`

这份只当索引，不重复内容。
