# 学科复习

碎片时间拿手机练。一套离线可用的网页，按科目分模块，每个模块一个引擎。

**从 v0.24 起这个仓库不只装语文了**——原先它是「古诗文默写」，只有语文一科；
现在外面套了一层科目选择壳，语文收进 `chinese/`，数学新建在 `math/`，
英语物理化学生物留了位置。本文件是总入口，各模块的详细说明在各自目录里。

---

## 零、怎么用

双击根目录的 **`index.html`**，选一科进去。断网也能用。

```
index.html          ← 从这里进
chinese/            语文模块（原来的古诗文默写，整个搬过来的）
math/               数学模块（新建）
```

手机上如果提示读不到数据文件，是某些安卓文件管理器给的不是正常本地路径，
在电脑上跑一次 `python3 chinese/merge.py`，把生成的单文件版拷过去用。

---

## 一、语文工作模块 `chinese/`

**已建成，六种模式全可用。** 高中五册课本里的古诗文默写本。

课文、注释、难字全是从人教版教科书 PDF 里抽出来的，一字不改：**66 篇古诗文**
（课本要求背诵的 20 篇）、**3031 条课本注释**、**579 个难字**，另有 **1320 个词语**，
每个词都带释义和例句。

| 模式 | 干什么 |
|---|---|
| 句子默写 | 挖掉句子里的小句画横线，看答案时连整段原文一起给，本句标红 |
| 难字默写 | 只挖被标为难字的单个字，课本注过音的把拼音一并给出 |
| 注释 | 给例句，其中一个词带角标，问它在这儿作什么讲 |
| 理解性默写 | 给提示语填句子（板块待补） |
| 词语 | 看拼音写词语 / 看词写拼音，都带例句情景和释义 |
| 看原文 | 仿课本版式整篇排下来，注释可开关 |

**特点**：答对不打扰；答错能挑出「具体哪几处记不住」，以后只挖那几处；
一轮做完可以「互补再默一遍」，把挖过的变提示、提示变挖空。

**语料是生成的**，由 `chinese/tools/` 从五本课本 PDF 抽出来，别手改 `data.js`
（唯一的例外是在正文里加减大括号圈难字，重跑脚本不会冲掉）。

→ **详见 [`chinese/README.md`](chinese/README.md)**，以及
[`chinese/data_structure.md`](chinese/data_structure.md)（语料格式）、
[`chinese/html_structure.md`](chinese/html_structure.md)（页面程序）、
[`chinese/extract_pdf_method.md`](chinese/extract_pdf_method.md)（PDF 解析踩过的坑）。

---

## 二、数学工作模块 `math/`

**v0.25 新建，骨架已通，十个大类里建好了两个。**

和语文最根本的区别：**这里没有题库**。数学题是模板反复构造出来的，
所以题目**现场生成、算出答案、不重样**，只有错题需要存。

**十个大类**（`math/engine.js` 里登记）：

| 大类 | 状态 | 内容 |
|---|---|---|
| 数字计算 | **已建** | 17 个细类：千以内加减、混合运算、平方立方、两位数乘法、多项累加、分数↔小数、质因数分解、最大公约数/最小公倍数、阶乘、分母有理化、根式化简，外加乘 11 / 乘 25 / 十位同个位补十 / 平方差凑整四种速算技巧 |
| 三角计算 | **已建** | 12 个细类：特殊角求值、诱导公式、和差角、二倍角、三倍角、降幂升幂、积化和差、和差化积、万能公式、辅助角、正余弦定理、反三角函数值 |
| 向量计算 | 待建 | |
| 立体与平面几何 | 待建 | 综合几何，不含圆锥曲线 |
| 解析几何 | 待建 | 圆锥曲线只是其中一部分，还有点线坐标式、代数法求距离等 |
| 方程与不等式 | 待建 | 含指数对数运算 |
| 复数计算 | 待建 | |
| 导数计算 | 待建 | |
| 概率统计 | 待建 | |
| 数列 | 待建 | |

**「算」和「背」混在一起，引擎不区分**——两位数乘法是算，三倍角公式是背，
出题、看答案、自评的流程一模一样。**二级结论不单开大类**，散进各自所属的大类
（三角里的 sin⁴α+cos⁴α、tanA+tanB+tanC 就是），将来想专练再加筛选。

**怎么判对错：看答案后自己判，分三档**——`算对了` / `算错了` / `没算出来`。
不做输入判分，因为手机上根式、复数、向量根本打不出来。判「算错了」时
可以**顺手填一句自己算成了几**（可选，不填也能过），复盘时看得见错在哪儿。

**计时两层**：每题用了多久（卡住说明这题有问题）、全轮总时长和平均每题。

**不做弱项归纳，只记错题。** 错题攒在「我的错题」里，能「只练这些」，
能导出 `math_user.js` 换设备。

→ **详见 [`math/engine_structure.md`](math/engine_structure.md)**（引擎怎么组织的、怎么加细类）。

---

## 三、其余科目（待建）

英语、物理、化学、生物在科目壳里占了位置，点不进去。

- **英语**打算复用语文「词语」板块的结构——八个字段几乎一一对应（词↔单词、拼音↔音标、
  释义、例句、来源分级），页面零件和三处防漏都能直接搬。
- **物理/化学/生物**是公式、方程式、概念，属于背诵型，走语文那台引擎；
  内容由使用者提供或使用者提大类、AI 整理后使用者过目。

---

## 四、两类文档，别搞混

- **历时**（发生过什么）→ [`LOG.md`](LOG.md)，正序、只追加、旧条目永不删改。
- **现状**（现在是什么样）→ 各模块的 `*_structure.md` / `*_method.md`，原地更新。

同一件事只在一处写实体，别处放指针。来龙去脉去 `LOG.md`，当前状态去现状文档。

---

## 五、仓库里都有什么

| 想干什么 | 看哪份 |
|---|---|
| **用这个复习本** | 本文件第零节，然后进对应模块 |
| **用语文模块 / 改语文语料** | [`chinese/README.md`](chinese/README.md) |
| **知道语文语料怎么存的** | [`chinese/data_structure.md`](chinese/data_structure.md) |
| **知道语文页面怎么写的** | [`chinese/html_structure.md`](chinese/html_structure.md) |
| **知道 PDF 怎么解析的** | [`chinese/extract_pdf_method.md`](chinese/extract_pdf_method.md) |
| **给数学加细类 / 加大类** | [`math/engine_structure.md`](math/engine_structure.md) |
| **知道项目一步步怎么走到现在的** | [`LOG.md`](LOG.md) |
| **改动这个项目（尤其是 AI）** | [`modify_manual.md`](modify_manual.md) |

代码和数据：

```
index.html                  科目选择壳
chinese/  index.html        语文页面（程序）
          data.js           语文语料（生成物，别手改）
          user.js           语文的标记和错题存档
          merge.py          并成手机单文件版
          tools/            从 PDF 生成语料的脚本和人工维护的表
          extract/          中间产物，人工校对看这个
math/     index.html        数学页面（交互）
          engine.js         题目生成器（加细类就改这个文件）
```

---

> **要改动这个项目，动手前先读 [`modify_manual.md`](modify_manual.md)。**
