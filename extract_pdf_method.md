# PDF 解析方法：遇到的困难和当前解法

> **这份文档记「现状」**——每个坑现在怎么解决的、为什么这么解决。规则变了直接改这份。
> 想知道某个解法是哪一轮试出来的、中间推翻过什么，去 `LOG.md` 查。

课本是五册人教版语文 PDF。目标：把古诗文正文连同课本注释一字不差地抽出来。
用的是 **PyMuPDF（`pymupdf`）**，拿的是 span 级的字体、字号、坐标。工具是
`tools/pdfdump.py`（切版面）+ `tools/assemble.py`（按篇合并）。

这一步的总原则：**只做忠实搬运，不做任何语文上的判断。** 哪个字该不该背、哪个是难字，
都是后面 `build.py` 的事。

---

## 一、认版面靠字体，不靠位置

教科书每种内容用固定的字体，`pdfdump.py` 按字体把 span 分到不同区块：

| 字体 | 排什么 |
|---|---|
| `FZSSJW` / `FZSSK` | 正文宋体 |
| `FZKTJW` / `FZKTK` / `KaiTi_GB2312` | 学习提示、旁批（楷体） |
| `FZFSJW` / `FZFSK` | 署名、小序（仿宋） |
| `FZZHUNYSK` | 课文篇名 |
| `FZZHUNYSJW` | 课序号、单元名 |
| `RopeSequenceNumberST-R` | 注释角标 |
| `NEU-XT-Regular` | 注音用的拉丁字体 |
| `FZZDXJW` | 页码 |

### 坑 1：方正字体成对出现，只认一半会漏字

方正的字体成对：**JW 是 GB1 基本集，K 是 GBK 扩展集**，同一款字的两套字库，
**生僻字会掉到 K 那一套**。只匹配 `FZKTJW` 没匹配 `FZKTK`，《涉江采芙蓉》整段学习提示
就掉进了正文。**解法**：字体一律按「家族名」匹配（`FZKT` 同时覆盖 JW 和 K）。

---

## 二、注释角标（上标）把版面搅乱

课本的注释角标是上标数字，专用字体 `RopeSequenceNumberST-R`。它带来两个问题。

### 坑 2：角标字形在正文和注释两边对不上

正文里印的是 w x y z，注释那边抽出来是 @3 @4——**字面对不上**。
**解法**：一律不认字面，**只认出现次序**，正文和注释各自从头数，用 `⟦n⟧` 配对。

### 坑 3：角标自成一行，按行读会把正文切碎

角标是上标，在 PDF 里往往自成一「行」，直接按 PDF 的行结构读，正文会被切碎、次序错乱。
**解法**：丢掉 PDF 的行结构，**按基线重新聚行**；角标另行按最近邻认领回它所属的那个字。

### 坑 4：角标认领的纵向容差要随字号放缩

角标抬升的幅度随字号走：贴 12 号正文抬 8 点，贴 18 号标题抬 18 点。
容差写死就会认错。**解法**：容差按字号放缩（`limit = size * 1.1 + 4`）。

### 坑 5：角标占位，不算进去会被误判成跨栏

角标不参与分行，但它确实占着横向位置。它让出的那点空当被误认成「跨栏」，
把《离骚》从中间拦腰切开。**解法**：切分时把紧跟在字后面的角标算作占位。

### 坑 6：第一条注释用普通圈号「①」

《孔雀东南飞》第一条注释印的是普通的「①」，不是那套专用角标字体。
**解法**：角标配对从「按数值算」改成「按出现次序配」，圈号也认。

---

## 三、分栏：注释排得极满，横向切不开

一页的注释常分左右两栏，有时两栏只隔两三个点（必修下 p16 只隔 2 点），横向投影根本切不开。

### 坑 7：先分栏再聚行行不通，要反过来

**解法**：先按基线聚行；一「行」里真出现二十点以上的空当，就是跨了栏，就地切开；
切出来的片段再按横向重叠归栏。栏挨得太近（《过秦论》那页隔 15 点）二十点切不开时，
反过来用「哪几个横坐标反复当行首」——那就是栏的起笔。

### 坑 8：插图说明横跨中缝，把两栏连成一片

分栏原先看「区间连通」，可插图说明常横跨中缝，一下把左右两栏连成一片，
《过秦论》的注释因此读串了十四条。**解法**：分栏改成认「栏的起笔对齐」。

---

## 四、区块归属按「行」投票，不按单字

### 坑 9：行内的阿拉伯数字没有中文字体可认

「背诵课文第 1 段」里的 1 用的是 Times，没有中文字体，单字判断会把它掉进别的区块，
还在行中留下空当被当成跨栏。**解法**：**区块归属按整行投票**，行内认不出字体的
数字、孤立标点跟着同行的邻居走。

---

## 五、插图说明混进注释（通读校对时才彻底解决）

图题和注释同为 9 号宋体，字体上分不开，只能靠版面。这一类反复出问题，改了两版：

### 坑 10：图题不只在第一栏

第一版假设「图题只在第一栏，后面几栏栏顶都是上一栏续下来的注释」——这只是某些页的巧合。
《赤壁赋》〔舳舻〕后面就跟着「赤壁图　［金］武元直作」。

### 坑 11：栏底、独占一栏的图题，按「续行贴栏沿」也量不出来

《念奴娇》「赤壁图傅抱石作」独占一栏、《与妻书》的图题排在栏底，没有续行可比。
中间试过「上一栏句号收口就没续行」，结果把《苏武传》一条注释判成图题——
课本注释常是「主释义。子目甲，……。子目乙，……」，句号收口不代表说完了。

**当前解法（最终）**：拿**本栏第一条注释的起笔**当尺子。续行挂在它下面只多缩三四点，
图题差出去几十到上百点；整栏一条注释都没有的，那一栏就全是图题。

> **教训**：同一症状（图题混进注释）在不同页面版面表现可以不一样。样本量不够大的规则，
> 扩到全书就露馅——先别急着信。也别拿语义（句号收没收口）当版面判据。

---

## 六、文本层里根本不存在的字

### 坑 12：十来个生僻字是画上去的，抽出来是空洞

輮、薆、騞、軱、駃騠、絖、鞿、纕、巉、巘、餔、摐——这些字排版时是画上去的，
PDF 文本层里没有，抽出来只剩一个空洞。**解法**：`tools/glyphs.tsv` 按位置补回，
逐个照着课本的注音和上下文认定。行首行尾掉的字撑不出空洞（两边没有可比的空当），
另行点名补。另有两个私有区造字按字形认定：囷、趱。

### 坑 13：漏字用「注释词条反查正文」才抓得到

行首、行尾掉的字，空洞检测看不见。**这一步交给 `build.py` 的交叉校验**：
每条注释的词条都要能在正文里找到，对不上就报出来。《劝学》的「輮」、
《庖丁解牛》的「軱」、《谏逐客书》的「駃騠」、《五石之瓠》的「絖」都是这么查出来的。
（详见 `LOG.md` 第 2 轮。）

---

## 七、按篇合并（assemble.py）

### 坑 14：一篇跨数页，角标每页重编号

先按页把角标配对好，再整篇重排、连续编号。一页上也可能并排着两篇
（《芣苢》和《插秧歌》），按篇名的纵坐标切开。

### 坑 15：学习提示一框管着好几篇，还印在末尾

背诵要求写在「学习提示」里，一框常管着好几篇，而且印在这组课文的末尾。
**解法**：反过来找——提示里点了名的按名字认领，只说「背诵课文」的归紧挨着它的那一篇。
课本七八种写法都要认（`背诵课文。` / `背诵《短歌行》。` / `背诵课文《劝学》全篇和《师说》的第1段。`
/ `背诵《离骚》（节选）第3段。`）。

> **坑**：得限定在本篇附近。《哈姆莱特》导读里有一句「最好能背诵」，不设范围会顺着
> 「课文」二字认领到三十页开外的《鸿门宴》头上。

> **坑**：篇名要对得上 `pieces.tsv` 里的写法。「《〈论语〉十二章》」和「《论语》十二章」
> 是同一篇，「背诵《离骚》第3段」要认上「离骚（节选）」。

### 坑 16：署名和小序都是仿宋，末行短会被当成署名丢掉

仿宋既排署名也排小序。《扬州慢》的序结在「之悲也。」四个字上，一行一行按长短判就丢了。
**解法**：先按行距把仿宋行连成段落，段里只要有一行成句，整段都还给正文当小序。

---

## 八、半角字符与音标混用

### 坑 17：一个半角叹号

《兰亭集序》「岂不痛哉!」的叹号排的是 Times 半角 `!`（宽 4 点，全角标点 12 点），
断句时认不出来。全书仅此一处，在 `squeeze()` 里按全角收。

> 判断依据：这是排版疏漏，不是课本有意为之——纸面上读者看到的就是个叹号。跟《梦游天姥》
> 末句那个问号不同，那个是课本选定的标点，照留。

### 坑 18：注音里混着国际音标的 ɡ

摐、毂、洴、絖、舸的注音里是国际音标 `ɡ`（U+0261），不是拉丁字母 `g`。
统一成普通的 `g`。（这个是 `build.py`/`words.py` 侧处理的，一并记着。）

---

## 九、自检：跑脚本时会自己报的三道关

1. **每篇正文的角标数 = 课本注释条数**。对不上就是版面读错了。
2. **每条注释的词条都能在正文里找到**。课本注了什么正文就得有什么。最灵，专抓漏字。
3. **每条注释的例句是正文里切出来的完整句子，释义用句末标点收口**。抓图题混入、切句错误。

改任何版面解析规则之后，先看这三道关过不过，再谈别的。
