# 开发日志 LOG

> **这份文档记「历时」**——每一轮做了什么、试过什么、成败都留着。**只追加，正序，
> 越往后越新，旧条目永不删改。** 后来发现某轮结论错了，就在那一轮下面补一行
> 「已被第 N 轮推翻」，把错误本身保留下来，别让下一个人重走同一条路。
>
> 想知道「现在」某个东西长什么样，别在这儿翻——去对应的现状文档：
> `data_structure.md`（语料）、`html_structure.md`（页面）、`extract_pdf_method.md`（PDF 解析）。
> 这份只回答「怎么一步步走到现在的」。
>
> 每轮开头带时间戳（本地时间，精确到分）。commit message 带轮次号，代码和日志对得上。

---

## 第 0 轮　起点：接手时的状态

**2026-09-18**

**本轮历程**：这一轮没有我的代码提交，是交接。仓库里原有一份使用者自己搭的框架
（`index.html` 雏形）加一份马马虎虎做的示例语料（11 篇左右）。使用者的原话是
「我这些语料是网上搜的，注释也是网上搜的，都不是很准确，你直接重新做一遍」。

**要解决的问题**：把高中语文五册课本（人教版，必修上下 + 选择性必修上中下）里的
古诗文做成一个能离线用的默写本。最初定下的硬要求：

1. **课文从 PDF 里抽**，不手抄、不从网上找；PDF 解析工具自己写。
2. **只要古诗文**，凡是古诗文都收；但**背诵与否只看正课课文**，从课文后的「学习提示」
   里解析——出现「背诵……」就是背诵篇目。
3. **注释逐字照抄**，不改写、不概括，背诵和非背诵篇目都要。
4. 背诵篇目按句号切题，默写挖掉逗号分号切出的小句。
5. 非背诵篇目不出默写题，但注释题要给原文例句——一题考一个词，句子里那个词标出来。

**贯穿始终的一条原则**（使用者原话）：**力求保证完全和书上一样，无偏差。**

**决定**：示例语料整个推倒重做——因为它来源不可靠，跟「和书上一样」的原则冲突，
留着反而是隐患。框架保留、逐步改。

**本轮未完成 / 趋势**：接下来先解决「从 PDF 里把字抽出来」这个最底层的问题。

---

## 第 1 轮　PDF 解析工具链

**2026-09-18 16:49**（commit 38b8ea3）

**本轮历程**：从零写 PDF 解析工具，把五册课本里的古诗文连同注释抽成可校对的中间稿。
这一步只做忠实搬运，不做语文判断。用 PyMuPDF 拿 span 级的字体、字号、坐标。

**解决了上轮遗留**：上一轮定了「课文从 PDF 抽、工具自己写」，这轮把工具搭起来。

**分条目**：

1. **`tools/pdfdump.py` 按字体切版面**。教科书每种内容用固定字体，按字体把 span 分区块。
   踩的坑：方正字体成对出现（JW 基本集 / K 扩展集），只匹配一半会漏生僻字——
   《涉江采芙蓉》整段学习提示掉进正文，就是楷体只匹配了 `FZKTJW`。解法：按字体家族匹配。
2. **注释角标的处理**。角标是上标、自成一行、字形两边对不上（正文 w x y z，注释 @3 @4）。
   解法：丢掉 PDF 行结构按基线重聚行；角标不认字面、只认出现次序，用 `⟦n⟧` 配对；
   认领容差随字号放缩。
3. **分栏**。注释两栏有时只隔两三点，横向切不开。解法：先聚行、行内二十点空当算跨栏就地切，
   再按横向重叠归栏。
4. **`tools/assemble.py` 按篇合并**。一篇跨数页、角标每页重编号，先配对再整篇重排。
   学习提示一框管几篇且印在末尾，反过来按名字认领；限定本篇附近，免得认到几十页外。
5. **`tools/glyphs.tsv` 补字表**。十来个生僻字是画上去的、文本层没有，按位置补回。

**尝试和看法**：这一轮最大的教训是**版面判断要靠相对关系，不能靠绝对位置**——
「角标只在第一栏」「注释固定在某个 x」这类写死的假设，换一页就崩。
（这些坑的当前解法都汇总在 `extract_pdf_method.md`，这里只记「这轮建起了工具」。）

**本轮未完成 / 趋势**：中间稿有了，但还没转成页面能用的 `data.js`；漏字也还没系统查过。
下一轮做生成 + 校验。

---

## 第 2 轮　生成 data.js 三板块，用注释词条反查漏字

**2026-09-18 17:07**（commit dc20422）

**本轮历程**：`tools/build.py` 把中间稿转成 `corpus` / `hardChars` / `notes` 三板块，
并加了一道关键的交叉校验。

**解决了上轮遗留**：上一轮的中间稿这轮变成 `data.js`；上一轮担心的漏字这轮有了系统查法。

**分条目**：

1. **注释靠 `⟦n⟧` 定位取例句**。角标标出注释挂在正文哪个字上，例句从那里取。跨句的长词条
   整个裹进例句不切一半；课本长注释里套的小注各拆一条，单字也考得到。
2. **难字分两路**：课本注音的带拼音进表，多音节注音按音节拆到每个字头上；生僻的也收。
3. **交叉校验（这轮最要紧的东西）**：每条注释的词条都要能在正文里找到，对不上当场报出来。
   这道关抓出了空洞检测看不见的行首/行尾漏字：

   | 篇目 | 漏字 | 位置 |
   |---|---|---|
   | 劝学 | 輮 | 行首 |
   | 庖丁解牛 | 軱 | 行首 |
   | 谏逐客书 | 駃騠 | 行尾 |
   | 五石之瓠 | 絖 | 行尾 |

**尝试和看法**：行首行尾掉的字两边没有空当、撑不出空洞，肉眼和空洞检测都发现不了，
**只有「课本注了什么正文就得有什么」这条反查能抓到**。这条自检后来一直是最灵的一道关。
顺带修了几个版面坑（角标占位防误切、分栏按起笔对齐、段首缩进要上一行是段末）。

**本轮未完成 / 趋势**：只做了三板块、只填了示例几篇。下一轮把五册真正填进去，页面也要跟上。

---

## 第 3 轮　填入五册古诗文，页面加「看原文」和背诵范围

**2026-09-18 17:17**（commit 8eb3793）

**本轮历程**：`data.js` 填成 66 篇（要背 20 篇）、3030 条注释、510 个难字，示例整个换掉。
`index.html` 四处改动配合新数据。

**解决了上轮遗留**：上一轮只有示例，这轮填满五册；页面补上背诵范围和阅读模式。

**分条目**：

1. **篇头加第四项标背诵范围**：`背` / `背1` / `不背`。全文都存，默写只挑要背的段；
   不背的照样能看原文、考注释。不写当成要背，旧的三项写法兼容。
2. **难字表可带拼音**：`衿(jīn)`。注音的是难读字，生僻的是难写字。
3. **文的分题加字数下限**：按句号切完不足十字并进下一题（`MIN_CHARS`）。这个十字是使用者
   定的：「古诗以句号为终止，古文就十个字吧」。
4. **新增「看原文」模式**：仿课本版式整篇排，带角标、注释跟段后、要背的段左边一道朱线，
   顶上「只看白文」开关可自测。

**尝试和看法**：常用多音字（不、说、读、还）课本也注音，但那是随语境变读不是字难写，
一进难字表正文里每个「不」都要被圈。这版先按字频（全书 60 次以上）挡掉——**这个判据
后来被第 6 轮推翻**（字频和难写是两回事）。

> **已被第 6 轮修正**：难写字的判据从「字频」换成《通用规范汉字表》一级字表。

**本轮未完成 / 趋势**：词曲的句读和分段还是照搬 PDF 的物理行，不对。下一轮还原。

---

## 第 4 轮　还原词曲的句读与分段

**2026-09-18 17:29–17:31**（commits d1202c8, 23cf724）

**本轮历程**：抽查发现三处失真，都和课本版式有关，逐个修。

**解决了上轮遗留**：上一轮照搬 PDF 物理行导致的句读错误，这轮还原。

**分条目**：

1. **词、歌行、骚体在课本里连排**，一行排满就换行，物理行不是句子。《念奴娇》头一题成了
   「大江东去，浪淘尽，千古风流人物。故垒」——句子是残的。解法：按句末标点重新断行，
   一行还它一句；律诗本来一句一行，不动。
2. **词的上下阕之间课本留一道白**，宽度和缺字空洞一样，程序分不出。解法：统一报成空洞，
   由补字表点名——是留白补 `¶` 据此断段，是缺字就补字。补字表也从「按序号认位置」改成
   「按上下文认」（空洞个数会随版面处理变动，序号一漂全错位）。
3. **《侍坐》整篇并成一段**。这篇全是短对话，段首行比续行还多，基准线按众数算落到缩进列上。
   解法：改取左边界（出现两次以上的最小 x）。

**尝试和看法**：顺带把段落合并门槛压到六字（比分题的十字低）——**段落是课本分的，能不动
就不动**：《劝学》「君子曰：学不可以已。」八字也是一段，独立成题不算短，不并走。
另外「行末落在句末标点的比例」这条判「是不是连排」的依据**后来在第 7 轮发现不够**
（《念奴娇·过洞庭》碰巧都断在句号上被误判），补了「一行最多十五六字」。

> **已被第 7 轮补强**：连排判据加了「右边是否对齐」和「行长上限」。

**本轮未完成 / 趋势**：页面选篇还是平铺六十多篇，太长；错题只能整题记；难字改起来不顺手。
下一轮按使用反馈改页面。

---

## 第 5 轮　按使用反馈改页面

**2026-09-19 04:09**（commit 557080f）

**本轮历程**：用了一阵之后提的五条改进，外加文档拆分。

**解决了上轮遗留**：上一轮留的三个页面痛点（平铺太长、错题太粗、难字难改）这轮解决。

**分条目**：

1. **篇目收进浮层**（`buildChips`）：按册分五组、一行一复选框，行末标背诵要求，能搜、
   能整册全选、能只要背诵的。坑：`.overlay{display:flex}` 会盖掉 HTML 的 `hidden`，
   必须单写 `.overlay[hidden]{display:none}`。
2. **挑出记不住的那几处**：答案里标红处可点，点成「不会」记进 `weak`；一处不点=整句不会。
   以后「只练错过的」只挖挑过的那几处。一题只一个空时不单独记（没区分度）。
3. **互补默写**（`complement`）：把这一轮挖过的变提示、原提示挖成空。
4. **难字改用手圈**：原先想给一张 510 字的表让人改，实际人是照纸书圈错字来的——改成
   **直接在 `data.js` 正文里加减大括号**。`build.py` 重跑前先 `harvest()` 出来记进
   `hardchars.tsv` 账本，再原样带回。
5. **标记和错题能看了**（「我的数据」面板）。
6. **文档拆两份**：README 面向使用者，PROGRESS 记开发过程。

**尝试和看法**：难字这条是典型的「顺着用户真实动作设计」——他不想啃字表，想像在纸书上圈字。
权威关系因此定死：**`data.js` 是权威，`hardchars.tsv` 是账本**，反过来让人改账本就打架。

**本轮未完成 / 趋势**：难字判据（字频）还没换；常用字词还没做。下一轮。

---

## 第 6 轮　难字改用国标字表，新增词语模式

**2026-09-19 11:11**（commits 7bc6d01, 4e4c08e）

**本轮历程**：难字判据换成国标一级字表；新增词语模式（看拼音写词 / 看词写拼音）。

**解决了上轮遗留**：上一轮遗留的难字判据和常用字词，这轮做。

**分条目**：

1. **难写字判据：字频 → 《通用规范汉字表》一级字表**。字频和难写是两回事：「樽」「衿」
   出现七八次但是二级字、照样容易错；「俭」「偿」只出现几次却是一级常用字、不该考。
   换成「不在那 3500 常用字内就算难写」，难字 510 → 579。
   > 换判定规则要跑 `build.py --regen`，否则 `harvest()` 把上版结果当手圈留下，白改。
2. **新增词语模式**（`tools/words.py`）。课本没有现成字词表，三个来源按可靠性排序：
   课本注音词条（188）> 课本里出现的成语（209）> 课本里出现的难写词（923）。
   题型按使用者定的规矩：难读的看字写拼音，难写的看拼音写词语。

**尝试和看法**：词的来源里「课本注音最权威」有实证——《苏武传》的「阏氏」读 yān zhī，
两本第三方词典都注成 è shì，只有课本对。这条定了「课本 > 成语 > 词典」的覆盖顺序。
坑：问读音时字要光秃秃的，难字表带拼音一渲染就漏答案，词语「读」题不能走 `renderHard`。

**本轮未完成 / 趋势**：66 篇还没逐字通读过（一直是自动校验把关）。下一轮通读。

---

## 第 7 轮　逐篇通读校对五册

**2026-09-19 11:33–11:42**（commits 76d60fc, c9f2ca0, c8771b7；日志补写 f04e1a2, f095482）

**本轮历程**：头一回一个字一个字地读完 66 篇（读的是生成出来的 `data.js` 不是中间稿——
诗词重新断行发生在 `build.py`）。读法是「读 + 起疑 + 回 PDF 查证」。查出八类版面问题，
全在生成侧修掉。

**解决了上轮遗留**：上一轮遗留的「还没逐字读过」，这轮做完。

**分条目（按发生顺序，含试错）**：

1. **先处理两处全角空格**（《虞美人》《江城子》上下阕）：换头留白的第三种写法，字面带着
   两个全角空格，`cut_blank()` 就地断开。
2. **写结构体检脚本**扫全篇：成对符号配平、行末是否标点收尾、有无重复长串。把 66 篇缩到
   几十行可疑输出。但每条要回 PDF 判断真假——引号不配对那条是课本原文如此（跨段引述），跳过。
3. **图注串进注释（18 条）**。先只看见《赤壁赋》一条，改了「续行贴栏沿、图题右对齐」后
   `diff` 发现多改出 12 条（漏了一整类）。以为好了。
4. **第三道自检又报出 5 条图注**（栏底、独占一栏的）。**试错**：先加「上一栏句号收口就没续行」，
   把《苏武传》一条注释判错、当场撤销（课本注释「主释义。子目甲，……」句号不代表说完）。
   **最终**：拿本栏第一条注释的起笔当尺子。教训：**样本不够大的规则先别信**。
5. **引号里不断句（386 条）**。切句遇引号就不切，《孔雀东南飞》连成 592 字一行。
   **决定**：不修补引号计数，直接把 `build.py` 两处切句换成新函数 `cut_stops()`，
   对齐 `index.html` 那套（已在页面上跑过两轮、验证过的）——**让没验证的一方贴近已验证的一方**。
6. **齐头排的诗丢了分段（8 篇）**。**三轮才收敛**：`flush_left()` 按左对齐判断→误伤《沁园春》
   整个推翻→换成 `justified()` 按右对齐判断→`regap()` 的行距基准从段内中位数改成全篇中位数
   （《声声慢》一段样本太小把中位数带偏）。《离骚》整篇并一段、连「背诵第3段」都落了空，
   这轮修好。
7. **离骚背诵范围错位**：「…………」那行缩进两格被当成独立段，第3段落到省略号上。
   `join_ellipsis()` 并回上段、不占段号；页面跳过没有汉字的题。改完在浏览器里跑
   `parseCorpus`+`buildQuestions` 确认要背的题是「长太息以掩涕兮」到「固前圣之所厚」。
8. **《论语》十二章成了十一章**：那页右边嵌插图、有两套左边界。分段改成找「成对的缩进关系」。
9. **半角叹号**（《兰亭集序》「岂不痛哉!」）：Times 半角，按全角收。
10. **拼音全量比对**（顺手）：「葳蕤 wēiruí」被切成 wēir/uí，音节正则加否定前瞻；
    国标音 ɡ 统一成 g。剩两处不一致是课本自己的读法（噫吁嚱 xī、山川相缪 liáo），照留。

**尝试和看法**：这轮把前几轮的判据补强了——连排判据加了右对齐和行长上限（第 4 轮的漏洞）。
最大的方法论收获：**结构体检 + 全量自检**比肉眼抽查靠谱得多，但每条报警都要回原始 PDF
判真假，不能脚本报什么改什么。收尾前用全量体检代替最后一遍抽查：3031 条注释全部词条回查
通过、释义收口、例句是完整句子；浏览器实测六模式无报错。

**本轮未完成 / 趋势**：理解性默写、译文解析两板块还空着；文档还是单一份 PROGRESS，
不够系统。下一轮建文档体系。

---

## 第 8 轮　建立文档治理体系（本轮）

**2026-09-19 15:06 起**（commits 2c07075, 153bf8a, 9046c71, …）

**本轮历程**：使用者提出把文档系统化，让规矩写进仓库自动生效、而不靠每次口头提醒。
本轮先搭骨架，语料库/图片库等真正动到时再建。

**解决了上轮遗留**：上一轮「文档不成体系」这轮解决；顺带把两板块待补的事项挂到这份 LOG。

**分条目**：

1. **确立文档分工**：`LOG.md` 记历时（只追加、正序、不删改）；`data_structure.md` /
   `html_structure.md` / `extract_pdf_method.md` 记现状（原地更新）。单一事实源，别处放指针。
2. **建三份现状文档**：数据结构、PDF 解析方法、页面结构。
3. **本份 LOG**：把原 `PROGRESS.md` 迁入、正序重排为第 0～8 轮，删掉 PROGRESS（重复）。
4. **`modify_manual.md`**：把工作流程写成停止点，含验收方式和「自行决定」的三条边界。
5. **`README.md` 改成总索引 + 文件指南 + 末句指路**；**`CLAUDE.md` 极短索引 + 硬指令**。

**尝试和看法**：核心是使用者的一条设计——**记现象不记结论**（「某字体在 Safari 下错位」
而非「不能用这字体」），因为经验换环境可能就不成立。这条将来落进 `restrictions.md`。
另一条：旧日志条目永不删改，结论错了就补「已被第 N 轮推翻」——把错误留下来，
省得下一个人重走。这轮我已经按这条给第 3、4 轮补了推翻标注。

**本轮未完成 / 趋势**：
- `restrictions.md`（禁令库，带字段和状态位）、`CORPUS-FORMAT.md`（挖空规则）暂缓，
  等真正处理语料时再建；图片库等处理图片时再建。
- 两板块仍待补：理解性默写（`comprehension`）、译文解析（`gloss`）——课本无现成内容，
  量最大的是译文，建议单开一轮。
- 难字只能按「篇+字」圈，圈一处等于整篇都圈；要只圈某处得给 corpus 加一种「只此一处」写法。
- 连排长篇（《琵琶行》《孔雀东南飞》《蜀道难》）按句末标点断行会把对句切开，这三篇不背、
  不出默写题所以没动；将来若拿它们出题要按字数把偏短的行并回去。
