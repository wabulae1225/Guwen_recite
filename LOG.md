# 开发日志 LOG

> **这份文档记「历时」**——每个版本做了什么、试过什么、成败都留着。
> **只追加，正序，越往后越新，旧条目永不删改。** 后来发现某处结论错了，
> 就在那儿补一行「已被 vX 推翻/修正」，把错误本身保留下来，别让下一个人重走同一条路。
>
> 想知道「现在」某个东西长什么样，别在这儿翻——去现状文档：
> `data_structure.md`（语料）、`html_structure.md`（页面）、`extract_pdf_method.md`（PDF 解析）。
> 这份只回答「怎么一步步走到现在的」。

## 这个项目经了两拨人

- **v0.0 → v0.1**　由**前一位作者**和使用者做出来：一个本地打开、不联网的古诗文默写网页，
  从单文件原型起步、后来拆成三文件，10 篇试点语料。下面「v0.0 → v0.1」一段是**他的开发日志，
  原样并入**——保留他的口径，我不改他的字。（他在自己日志里把交付版称作 v1.0；放到本项目
  统一的版本线里，那就是 **v0.1**，也就是我接手时拿到的初版。）
- **v0.11 → v0.2**　由**我**（接续的助手）和使用者做：把语料从五册课本 PDF 里重新抽出来做到
  66 篇、写解析工具链、加模式、逐篇通读校对、重构文档体系。现在是 **v0.2**。

**版本约定**：`v0.1` 是接手时的初版；`v0.11 / v0.12 …` 是我在其上迭代的里程碑；`v0.2` 是当前。
commit message 从 v0.2 起带版本号，代码改动和日志条目对得上。

> **血缘对照**（v0.1 的文件后来去哪了，免得读者以为凭空冒出来/凭空消失）：
> v0.1 的 `使用说明.md` → 并入我的 `README.md`；`工程日志.md` → 拆进 `LOG.md` 和三份现状文档；
> v0.1 已有的 `modify_manual` → 在 v0.2 重写纳入新体系；`data.js` 从 5 板块扩成 6 板块
> （加了 `words`）；10 篇试点语料 → v0.13 整个换成从 PDF 抽的 66 篇。
---

## 版本速查表（什么被干了，一眼看完）

> **这张表是全项目的索引**，回答「到底什么被干了」。每一行对应下面的一节详细记录。
> **「谁」这一列要紧**：这个项目有三方在改——前一位作者、AI、以及**使用者本人直接改**
> （他会在 GitHub 网页上直接编辑手册和 CLAUDE.md，那种提交的 message 是英文的默认文案）。
> 早期还有几轮工作分散在多个对话窗口里，断过几次，所以**以 git 历史为准、不以某一个
> 窗口的记忆为准**。表格按时间正序。

| 版本 | 日期 | 谁 | 干了什么 | commits |
|---|---|---|---|---|
| — | 09-18 | 使用者 | 建仓库、上传前一位作者的成果、改 README | `6f455cd` `2e82c76` `63703de` `231c0e3` `d89b495` |
| v0.0→v0.1 | 09-18 前 | 前一位作者 | 从零做出能用的默写网页：单文件原型 → 拆三文件 → 四种题型，10 篇试点语料 | （并入本仓库前的工作） |
| v0.11 | 09-18 | AI | 接手，写 PDF 解析工具链，抽出 66 篇中间稿 | `38b8ea3` |
| v0.12 | 09-18 | AI | 生成 `data.js` 三板块，用注释词条反查正文漏字 | `dc20422` |
| v0.13 | 09-18 | AI | 填入五册古诗文，加「看原文」和背诵范围 | `8eb3793` |
| v0.14 | 09-18 | AI | 还原词曲的句读与分段，段落合并门槛压到六字 | `d1202c8` `23cf724` |
| v0.15 | 09-19 | AI | 篇目收进浮层，错题精确到某一处空，加互补默写 | `557080f` |
| — | 09-19 | 使用者 | **自己动手在单文件版里加了一些难字** | `a4347ef` |
| v0.16 | 09-19 | AI | 难字改用国标字表判定，新增词语模式 | `7bc6d01` `4e4c08e` |
| v0.17 | 09-19 | AI | 逐篇通读校对五册（图注串行、引号断句、注音切错） | `76d60fc` `c9f2ca0` `c8771b7` `f04e1a2` `f095482` |
| v0.2 | 09-19 | AI | 建立文档治理体系：四份现状文档 + LOG + 手册 + CLAUDE.md | `2c07075` `153bf8a` `9046c71` `8ed05eb` `db70f39` `64d77eb` `c57cff1` `528b758` |
| v0.21 | 09-20 | AI | 两本词典入库，拼音按字分开，补 220 条自拟释义 | `75bf637` `712d7fb` `8e19810` `4790154` `35f8cde` |
| v0.22 | 09-20 | AI | 补 232 条自拟例句，词语模式每题都有情景 | `ab6cbb1` `133d222` |
| v0.23 | 09-20 | AI | 词里用大括号圈该挖的字，41 条打转释义逐条重写 | `2f9cf4e` `eae8f94` `02712b4` `b336e6d` |
| **v0.23.1** | **09-20 晚** | **使用者** | **本人直接改手册第一、四节和 CLAUDE.md**——加「第 0 步核查环境」、把记日志的规矩改写成自己的话。**这四个提交长期没进日志，v0.26 补记** | `9b0ae6a` `08439a3` `a562a56` `7151edf` |
| v0.24 | 09-21 | AI | **项目性质转折**：目录重构成多科目，外面套科目选择壳 | `5715eb5` |
| v0.25 | 09-21 | AI | 数学速算引擎（29 个细类）、文档改成多科目、日志补记 | `e291b2e` `a0bc1fe` `6c4fafa` |
| v0.26 | 09-21 | AI | 日志补缺与加索引；PWA（可装到手机、离线、自动更新） | 见该节 |
| v0.27 | 09-21 | AI | 总体验收五套全过；文件布局分出源材料与生成物；README 逐条核对；上线方案摆出两条路 | 见该节 |
| v0.28 | 09-21 | AI | 手册编号补上第 3 步；上线定为「公开 + GitHub Pages」；开 PR #6 | 见该节 |


---
---

# v0.0 → v0.1　前一位作者：从零到一个能用的默写工具

> 下面六节是前一位作者的开发日志，原样并入。他当时没有走后来才建立的停止点流程，
> 是事后按统一格式补记的。文中提到的 `使用说明.md`、`工程日志.md`、`CORPUS`/`HARD_CHARS`
> 等文件或符号，都是 v0.1 时期的样子，后续版本有增删改（见文首「血缘对照」）。

## 第 1 轮　需求对齐与单文件原型

**提了什么**　做一个高中古诗文默写练习的网页，本地打开，不联网。参照学校默写卷的挖空规则出题。

**想解决什么**　手上没有能按自己节奏刷、能挑篇目挑题量的默写工具。

**实际改了什么**

- 确定挖空规则：一个句号单位算一题，题内按逗号分号切出的小句是挖空的最小单位，至少留一个小句当锚点不挖。顿号不切。
- 写出第一版单文件 `index.html`，语料以 JS 常量 `CORPUS` 内嵌，用《短歌行》当测试数据跑通框架。
- 实现两种模式（句子默写、难字默写）和四组参数（顺序、题量、挖空量、筛选）。

**试过哪些方案**

- 分句最初用简单的 `split(/[，。；！？]/)`，丢掉了标点本身，答案回显时没法还原原文。改成 `/[^，。；！？：,.;!?]+[，。；！？：,.;!?]?/g` 匹配「内容 + 尾标点」，把标点作为 `punct` 字段跟内容一起存进 unit 对象。

## 第 2 轮　语料录入与分句逻辑修正

**提了什么**　把必修上下册的篇目录进去。

**想解决什么**　只有一篇测试数据，没法真正拿来练。

**实际改了什么**

- 录入 10 篇：短歌行、梦游天姥吟留别、登高、念奴娇·赤壁怀古、劝学（节选）、师说（第一段）、赤壁赋、子路曾皙冉有公西华侍坐、阿房宫赋、六国论。
- 沁园春·长沙只建篇头留空，正文由使用者自行抄录（原因见 `工程日志.md` 第四节）。
- 按使用者要求删除归园田居（其一）。
- 难字用 `{字}` 在正文里圈注，解析时自动并入难字集合，与 `HARD_CHARS` 取并集。

**试过哪些方案**

- 引号归属：录入《侍坐》后发现「『如或知尔，则何以哉？』」这类句子，右引号会被切到下一小句的开头，断成「’如或知尔」。改成在 `segment()` 里加一道修正，把开头的 `”’」』）)` 退回给上一个 unit 的 `punct`，退完如果本 unit 空了就整个并掉。
- 单元素题目：切完发现存在只有一个小句的题，这种题挖了就没锚点。改成 merge 阶段把单元素题并进前一题，前一题已经有 6 个小句就不并，另起一题。
- 挖空上限：《阿房宫赋》「方其破荆州……」一句实际切出 9 个小句，「尽量多挖」档位下整屏全是横线。给 `pickBlanks()` 加硬上限 `Math.min(k, n-1, 6)`。

## 第 3 轮　拆成三文件 + 四种题型 + 三点改进

**提了什么**　想用别的 AI 工具批量生成注释数据，所以要把数据和程序分开，拆成三个文件（程序 / 语料 / 用户数据）。如果环境不支持多文件，再写个合并脚本。另外三点：看答案要带上下文；答错要给译文解析；注释也要能考，来源分课本、学校补充、实词手册三类。

**想解决什么**　语料和程序焊在一起，每次批量改数据都要在代码里找地方改，容易碰坏逻辑。另外挖空只回显本句，缺上下文不好回忆。

**实际改了什么**

*架构*

- 拆成 `index.html` / `data.js` / `user.js`，用 `<script src>` 引入，不用 `fetch`。
- `data.js` 分五个板块：`corpus`、`hardChars`、`notes`、`comprehension`、`gloss`，每块的格式说明写在该块上方的注释里。
- 写 `merge.py`，把三个文件内联合成 `古诗文默写_单文件.html`。用正则匹配 `index.html` 里引入两个脚本的那两行做替换，匹配不到就报错退出，不静默输出半成品。
- `index.html` 开头加数据缺失的兜底：`typeof window.DATA === "undefined"` 时整页替换成提示，说明去查同目录和文件名，并指向合并脚本。

*语料格式*

- 正文改为空行分段。写了个一次性 python 脚本 `corpus_para.txt` 生成器做迁移：散文按原有的每行一自然段，行间插空行；《梦游天姥吟留别》按内容在第 2、5、7、9 行后断段。迁移后段数：梦游天姥 5 段，赤壁赋 5 段，侍坐 8 段，阿房宫赋 5 段，六国论 5 段，劝学 4 段，短诗各 1 段。
- 解析层把 `piece.questions` 拍平的同时，每题记 `para` 下标，另存一份 `piece.paraQs`（段 → 题下标数组），供整段回显用。

*三点改进*

- 答案回显：加 `renderPara()`，看答案时渲染本题所在整段，本题用 `.here` 标红，其余部分用 `--ink-faint` 压暗。诗用 `<br>` 连接，文直接接排。
- 译文解析：加 `gloss` 数据块，键是 `篇名::原句`（原句去空格后精确匹配）。加 `reviewing` 状态，点「我不记得」且查到解析时不推进题号，而是重渲染当前题、追加「译文与解析」区块、按钮换成「看完了，下一题」。查不到解析就直接进下一题。
- 注释模式：加第三种题型。`notes` 一行五段 `来源 | 字词 | 释义 | 例句 | 出处`，例句里 `{}` 圈的字渲染成标红加角标。`来源` 为「课本」用正体，「补充」「实词」用楷体并带标签。出处若是库内篇名则跟随篇目筛选，出处不在库内（实词手册的外篇例句）则恒出。

*用户数据*

- `marks` 和 `wrongCount` 的键统一用 `篇名::句子内容`，不用下标，避免插删语料导致标记错位。
- 加「我的数据」面板：导出 `user.js`（Blob + `a[download]`）、从文本框导入（正则抓 `{...}` 后 `JSON.parse`）、清空。
- 启动时 `window.USER` 与 `localStorage` 取并集：`wrongCount` 逐键取 `Math.max`，`marks` 直接并入 Set。

**试过哪些方案**

- 第一版 `grade()` 里的译文解析是死代码：先 `pos++` 推进到下一题再 `render()`，然后去查 `.gloss` 元素，永远查不到。删掉重写成 `reviewing` 状态机 + 独立的 `next()`。
- jsdom 冒烟测试第一次没跑起来，`beforeParse` 里调 `localStorage.clear()` 抛 `DOMException`：`file://` 是 opaque origin，jsdom 不给 localStorage。改成先跑 `merge.py` 生成单文件，再用 `url: 'https://local.test/'` 加载，绕开这个限制，顺带把单文件版也测了。

**验收**

- `node --check` 过 `index.html` 抽出的脚本和 `data.js`。
- `npm install jsdom`，写 `smoke2.js` 真实加载单文件版、真实触发点击事件：

  ```
  库信息: 10 篇 · 166 题 · 难字 203 · 注释 5
   模式 句子默写   → 第 1 / 166 题
   模式 难字默写   → 第 1 / 126 题
   模式 注释     → 第 1 / 5 题
   模式 理解性默写  → 第 1 / 4 题
   注释题面: 师者，所以传道受①业解惑也
   释义区  : 同「授」，传授课本
   第1题 本段回显: 风急天高猿啸哀，渚清沙白鸟飞回。无边落木萧萧下，不尽长江滚滚…
  统计: 已答 3 不记得 3 错题表 3 条
  只练错过的 → 第 1 / 3 题
  ```

- 写 `smoke3.js` 专测译文解析：只选劝学、按原文顺序逐题推进，命中「骐骥一跃，不能十步；驽马十驾，功在不舍。」后点「我不记得」，确认弹出复习页且按钮为「看完了，下一题」。
- `merge.py` 输出 47.5 KB 单文件，独立加载行为与三文件版一致。
- 冒烟脚本（`smoke.js` / `smoke2.js` / `smoke3.js`、`corpus_para.txt` 生成脚本）是临时产物，验收后删除，不进交付目录。

**同轮产出**　`使用说明.md`：文件怎么放、四种模式、参数含义、标记与导入导出、`data.js` 五个板块的格式规范、已知限制。

## 第 4 轮　补工程日志

**提了什么**　把这些东西是怎么写的、用了什么、结构是什么写成工程 log，v1.0 就算完整了。

**实际改了什么**　新建 `工程日志.md`，十一节：需求演进、三文件架构的理由、环境限制、版权边界、四种题型的实现、答案回显与译文解析、标记按内容存的理由、测试方式、语料规模、交付清单、后续待办。侧重写「为什么这么定」，与 LOG 的「发生过什么」分工。

## 第 5 轮　补 README

**提了什么**　还缺一个 README 当项目入口，说清这些文件各是干什么的。

**实际改了什么**　新建 `README.md`：一句话说明项目、先打开哪个文件、七个文件的用途表、三句话使用流程。

**v1.0（= 本项目版本线的 v0.1）交付清单**

```
README.md              入口
index.html             程序本体
data.js                五板块数据
user.js                用户数据存档模板
merge.py               三合一脚本
使用说明.md             操作手册
工程日志.md             设计决策记录
古诗文默写_单文件.html    预合并版本
```

语料规模：10 篇有效，166 题，难字 203 个；注释 5 条、理解性默写 4 题、译文解析 2 条，均为格式验证用的试点数据。

## 第 6 轮　建立改动手册

**提了什么**　仓库设私有，并确立 AI 在这个仓库里的工作流程。

**实际改了什么**　新建 `modify_manual`：一轮工作的九个步骤、两个硬性停止点（对齐确认、方案确认）、授权三档及「自行决定」的三条边界、验收方式的硬要求、记日志的规矩、文档分工。本轮之前的五轮按该格式事后补记，见上。

> **交付即 v0.1。** 到这儿前一位作者的工作告一段落：一个能用的默写工具，10 篇试点语料，
> 一套文档和工作规矩。**下面从这儿接手。**

---
---

# v0.11 → v0.2　接手：从 10 篇试点到五册全本 + 文档体系

> 下面是我（接续的助手）和使用者的工作。写得比上半段细——因为这些试错的来龙去脉
> 只在这一次对话里，换了窗口就永久丢了，所以尽量都记下来。

## v0.11　接手，写 PDF 解析工具链

**2026-09-18 16:49**（commit 38b8ea3）

**接手时的状态**：拿到前一位作者的 v0.1——三文件框架 + 10 篇试点语料。使用者的原话是
「我这些语料是网上搜的，注释也是网上搜的，都不是很准确，你直接重新做一遍」。

**这一拨要解决的问题**：把五册课本（人教版，必修上下 + 选择性必修上中下）里的**全部**古诗文
一字不差地做进来，而不是 10 篇试点。硬要求：课文从 PDF 抽（不手抄、不从网上找，工具自己写）；
凡是古诗文都收，但**背诵与否只看正课课文**、从课文后的「学习提示」里解析；注释逐字照抄。

**贯穿始终的原则**（使用者原话）：**力求保证完全和书上一样，无偏差。**
——这句话在我之后每一轮里都在起作用。

**决定**：v0.1 的 10 篇语料整个推倒（来源不可靠，跟原则冲突），框架保留、逐步改。
先解决最底层的「从 PDF 把字抽出来」。用 PyMuPDF 拿 span 级的字体、字号、坐标。

**分条目**：

1. **`tools/pdfdump.py` 按字体切版面**。教科书每种内容用固定字体（正文 `FZSSJW/FZSSK`、
   楷体学习提示 `FZKTJW/FZKTK`、仿宋署名 `FZFSJW/FZFSK`、篇名 `FZZHUNYSK`、角标
   `RopeSequenceNumberST-R`、注音拉丁字体 `NEU-XT`……）。**坑**：方正字体成对出现——
   JW 是 GB1 基本集、K 是 GBK 扩展集，生僻字掉在 K 那一套，只匹配 `FZKTJW` 没匹配 `FZKTK`，
   《涉江采芙蓉》整段学习提示就掉进了正文。解法：按字体家族匹配。
2. **注释角标的处理**。角标是上标、自成一行、字形两边对不上（正文印 w x y z，注释抽出来是 @3 @4）。
   解法：丢掉 PDF 行结构按基线重聚行；角标不认字面、只认出现次序，用 `⟦n⟧` 配对；
   认领容差随字号放缩（贴 12 号正文抬 8 点、贴 18 号标题抬 18 点）；角标占位要算进去，
   否则它让出的空当被当成跨栏，把《离骚》拦腰切开。
3. **分栏**。注释两栏有时只隔两三点（必修下 p16 隔 2 点），横向切不开。解法：先聚行、
   行内二十点空当算跨栏就地切，再按横向重叠归栏；栏挨太近时反过来用「哪几个横坐标反复当行首」。
4. **`tools/assemble.py` 按篇合并**。一篇跨数页、角标每页重编号，先配对再整篇重排。
   学习提示一框管几篇且印在末尾，反过来按名字认领。**坑**：得限定本篇附近——
   《哈姆莱特》导读里有一句「最好能背诵」，不设范围会顺着「课文」二字认领到三十页外的《鸿门宴》头上。
5. **`tools/glyphs.tsv` 补字表**。十来个生僻字（輮、薆、騞、軱、駃騠、絖…）是画上去的、
   文本层没有，按位置补回。**`tools/pieces.tsv`** 人工圈定 66 篇。

**尝试和看法**：这一轮最大的教训——**版面判断要靠相对关系，不靠绝对位置**。
「角标只在第一栏」「注释固定在某个 x」这类写死的假设，换一页就崩。这条后来在通读校对
（v0.17）里又被反复验证。（18 个坑的当前解法都汇总在 `extract_pdf_method.md`。）

**本轮未完成 / 趋势**：中间稿有了，但还没转成 `data.js`；漏字也还没系统查过。下一轮做生成 + 校验。

## v0.12　生成 data.js 三板块，用注释词条反查漏字

**2026-09-18 17:07**（commit dc20422）

**提了什么**　把中间稿转成页面能用的 `data.js`。

**解决了上轮遗留**：v0.11 的中间稿这轮变成 `data.js`；担心的漏字这轮有了系统查法。

**分条目**：

1. **`tools/build.py` 生成 `corpus` / `hardChars` / `notes` 三板块**。
2. **注释靠 `⟦n⟧` 定位取例句**。角标标出注释挂在正文哪个字上，例句从那里取。跨句长词条整个裹进
   例句不切一半；课本长注释里套的小注（`衿，衣服的交领`）各拆一条，单字也考得到。
3. **难字分两路**：课本注音的带拼音进表，多音节注音按音节拆到每个字头上（踟蹰 chíchú → 踟 chí、蹰 chú）；
   全书出现不过三两次的也收。
4. **交叉校验（这轮最要紧的东西）**：每条注释的词条都要能在正文里找到，对不上当场报出来。
   这道关抓出了空洞检测看不见的行首/行尾漏字：

   | 篇目 | 漏字 | 位置 |
   |---|---|---|
   | 劝学 | 輮（輮以为轮） | 行首 |
   | 庖丁解牛 | 軱（而况大軱乎） | 行首 |
   | 谏逐客书 | 駃騠（骏良駃騠） | 行尾 |
   | 五石之瓠 | 絖（世世以洴澼絖） | 行尾 |

**尝试和看法**：行首行尾掉的字两边没有空当、撑不出空洞，肉眼和空洞检测都发现不了，
**只有「课本注了什么正文就得有什么」这条反查能抓到**。这条自检从此一直是最灵的一道关。
顺带修了几个版面坑（角标占位防误切、分栏按起笔对齐、段首缩进要求上一行是段末——否则整行
头一个字是画上去的会看着像缩进）。

**本轮未完成 / 趋势**：只做了三板块、还只是试点几篇。下一轮把五册真正填进去，页面跟上。

## v0.13　填入五册古诗文，页面加「看原文」和背诵范围

**2026-09-18 17:17**（commit 8eb3793）

**提了什么**　把五册全部填进去，页面配合新数据。

**解决了上轮遗留**：**v0.1 的 10 篇试点语料整个换成从 PDF 抽的 66 篇**（要背 20 篇）、
3030 条注释、510 个难字。

**分条目**（`index.html` 四处改动）：

1. **篇头加第四项标背诵范围**：`背` / `背1`（只背第 1 段）/ `不背`。全文都存，默写只挑要背的段；
   不背的照样能看原文、考注释。不写当成 `背`，v0.1 的三项写法兼容。
2. **难字表可带拼音**：`衿(jīn)`。注音的是难读字（看答案时给音），生僻的是难写字（单写一个字）。
3. **文的分题加字数下限**：按句号切完不足十字并进下一题。**这个十字是使用者定的**，原话
   「古诗是很确定的，就是格律对仗，以句号为终止，古文就十个字吧」（`MIN_CHARS = 10`）。
4. **新增「看原文」模式**：仿课本版式整篇排，带角标、注释跟段后、要背的段左边一道朱线，
   顶上「只看白文」开关可自测。这是独立于默写/难字/注释之外的第五种模式，供参考查阅。

**尝试和看法**：常用多音字（不、说、读、还）课本也注音，但那是随语境变读不是字难写，一进难字表
正文里每个「不」都要被圈。这版先按字频（全书 60 次以上）挡掉。

> **已被 v0.16 修正**：难写字的判据后来从「字频」换成《通用规范汉字表》一级字表——字频和难写
> 是两回事。

**本轮未完成 / 趋势**：词曲的句读和分段还是照搬 PDF 的物理行，不对。下一轮还原。

## v0.14　还原词曲的句读与分段

**2026-09-18 17:29–17:31**（commits d1202c8, 23cf724）

**提了什么**　抽查发现三处失真，都和课本版式有关，逐个修。

**解决了上轮遗留**：v0.13 照搬 PDF 物理行导致的句读错误，这轮还原。

**分条目**：

1. **词、歌行、骚体在课本里连排**，一行排满就换行，物理行不是句子。《念奴娇》头一题成了
   「大江东去，浪淘尽，千古风流人物。故垒」——句子是残的，20 篇词都这样。解法：按句末标点
   重新断行，一行还它一句；律诗本来一句一行，不动。
2. **词的上下阕之间课本留一道白**，宽度和缺字空洞一样，程序分不出。解法：统一报成空洞，
   由补字表点名——是留白补 `¶` 据此断段，是缺字就补字。补字表也从「按序号认位置」改成
   「按上下文认」（空洞个数会随版面处理变动，序号一漂全错位）。
3. **《侍坐》整篇并成一段**。这篇全是短对话，段首行比续行还多，基准线按众数算落到缩进列上。
   解法：改取左边界（出现两次以上的最小 x）。

**尝试和看法**：顺带把段落合并门槛压到六字（比分题的十字低）。使用者的意思是
**段落是课本分的，能不动就不动**：《劝学》「君子曰：学不可以已。」八字也是一段，独立成题
不算短，不并走。

> **已被 v0.17 补强**：「行末落在句末标点的比例」这条判「是不是连排」的依据后来发现不够——
> 《念奴娇·过洞庭》上阕两行碰巧都断在句号上被误判，v0.17 补了「一行最多十五六字」和
> 「右边是否对齐」两条。

**本轮未完成 / 趋势**：页面选篇还是平铺六十多篇太长；错题只能整题记；难字改起来不顺手。下一轮改页面。

## v0.15　按使用反馈改页面

**2026-09-19 04:09**（commit 557080f）

**提了什么**　用了一阵之后提的五条改进，外加文档拆分。

**解决了上轮遗留**：v0.14 留的三个页面痛点（平铺太长、错题太粗、难字难改）这轮解决。

**分条目**：

1. **篇目收进浮层**（`buildChips`）：按册分五组、一行一复选框，行末标背诵要求（背诵/部分背诵/不背），
   能搜、能整册全选、能只要背诵的。**坑**：`.overlay{display:flex}` 会盖掉 HTML 的 `hidden`，
   浮层一直挡着点不动，必须单写 `.overlay[hidden]{display:none}`。
2. **挑出记不住的那几处**：答案里标红处可点，点成「不会」记进 `weak`；一处不点=整句都不会。
   以后「只练错过的」时只挖挑过的那几处。一题只一个空时选中它=整句不会，不单独记（没区分度）。
3. **互补默写**（`complement`）：把这一轮挖过的地方变成提示、原来给的提示挖成空。两轮合起来
   每个字都默一遍。
4. **难字改用手圈**。原先想给一张 510 字的表让人改，但使用者的真实动作是照纸书圈错字——原话
   「我在原文上加大括号就行了，我现在是纸质版的书，上面我把错字圈起来了，我直接加上大括号就好了」。
   于是改成**直接在 `data.js` 正文里加减大括号**。`build.py` 重跑前先 `harvest()` 出来记进
   `hardchars.tsv` 账本，再原样带回。
5. **标记和错题能看了**（「我的数据」面板）：列篇名+句子，错题带错几次、挑了几处，可逐条移出、
   可「只练这些」。
6. **文档拆两份**：README 面向使用者，PROGRESS 记开发过程。

**尝试和看法**：难字这条是「顺着用户真实动作设计」的典型——他不想啃字表，想像在纸书上圈字。
**权威关系因此定死：`data.js` 是权威（人在那儿圈），`hardchars.tsv` 是账本**；反过来让人改账本
就跟「在原文上圈」打架。

**本轮未完成 / 趋势**：难字判据（字频）还没换；常用字词还没做。下一轮。

## v0.16　难字改用国标字表，新增词语模式

**2026-09-19 11:11**（commits 7bc6d01, 4e4c08e）

**提了什么**　难字库扩充、下限放低；加常用字词的看拼音写词语/看词写拼音。

**解决了上轮遗留**：v0.15 遗留的难字判据和常用字词，这轮做。

**分条目**：

1. **难写字判据：字频 → 《通用规范汉字表》一级字表**。字频和难写是两回事：「樽」「衿」「掇」「觥」
   在课本里出现七八次、按字频不低，但都是二级字、照样容易写错；反过来「俭」「偿」「剖」「堤」
   出现也没几次却都是一级常用字、不该当难写字考。换成「正文里不在那 3500 常用字内就算难写」，
   难字 510 → 579。
   > 换判定规则要跑 `build.py --regen`，否则 `harvest()` 把上一版结果当手圈留下、新规则白改。
2. **新增词语模式**（`tools/words.py`）。课本没有现成字词表，三个来源按可靠性排序、前面覆盖后面：
   课本注音词条（188）> 课本里出现的成语（209）> 课本里出现的难写词（923）。题型按使用者定的
   规矩：**难读的看字写拼音，难写的看拼音写词语，优先生僻字**。「读」= 课本注过音的或词里有字
   变读（「字里行间」的行读 háng）；变调、轻声不算。

**尝试和看法**：「课本注音最权威」有实证——《苏武传》的「阏氏」读 yān zhī，两本第三方词典都注成
è shì，只有课本对，所以定了「课本 > 成语 > 词典」的覆盖顺序。**坑**：问读音时字要光秃秃的，
难字表带拼音一渲染就把答案漏了，词语「读」题不能走 `renderHard`。「刽子」是「刽子手」切下来的
半截，词表两个都有、只留长的。

**本轮未完成 / 趋势**：66 篇一直是自动校验把关，还没逐字通读过。下一轮通读。

## v0.17　逐篇通读校对五册

**2026-09-19 11:33–11:42**（commits 76d60fc, c9f2ca0, c8771b7；随后 f04e1a2, f095482 补日志）

**提了什么**　把原文和注释「逐篇通读校对——目前是自动校验把关，还没逐字读过 66 篇」。

**解决了上轮遗留**：v0.16 遗留的「还没逐字读过」，这轮做完。头一回一个字一个字读完 66 篇
（读的是生成出来的 `data.js` 不是中间稿——诗词重新断行发生在 `build.py`，只有读最终结果才算数）。
读法：读 + 起疑 + 回 PDF 查证。查出八类版面问题，全在生成侧修掉。**下面按发生顺序记，含试错。**

1. **先处理两处全角空格**（《虞美人》《江城子》上下阕）。换头留白的第三种写法——字面带着两个
   全角空格（`故国不堪回首月明中。　　雕栏玉砌应犹在`）。`cut_blank()` 用 `　{2,}` 就地断开。
   全书扫一遍确认只有这两处，其余全角空格都是练习题编号里的。
2. **写结构体检脚本**，不死磕肉眼通读。扫全篇：成对符号配平、行末是否标点收尾、有无重复长串。
   第一次跑报出「括号不配对 齐桓晋文之事」（一查是课本原文跨段引述、每段开引号只收一次口，跳过）、
   一批「行末无标点」（指向下面第 5 条的引号 bug）。**经验**：体检把 66 篇缩到几十行可疑输出，
   但每条要回 PDF 判真假，不能脚本报什么改什么。
3. **图注串进注释——先只看见冰山一角**。通读到《赤壁赋》肉眼看到〔舳舻〕后面跟着「赤壁图　
   ［金］武元直作」。去 `split_captions()` 看，发现写死了「图题只在第一栏」（只是那页的巧合）。
   改成「续行贴栏沿、图题右对齐甩远处」，`diff` 一看多改出 12 条——漏的是一整类。以为好了。
4. **第三道自检又报出 5 条图注**（栏底、独占一栏的：《念奴娇》「赤壁图傅抱石作」、《与妻书》
   「福州林觉民故居…」、《春江花月夜》《过秦论》《蜀道难》的图题）。这几栏没有续行可比。
   **试错**：先加「上一栏句号收口就没续行」，逻辑上说得通，跑完把《苏武传》〔前长君为奉车〕的
   续行「长君，指苏武的大哥苏嘉…」判成图题——课本注释常是「主释义。子目甲，……。子目乙，……」，
   句号收口不代表说完，当场撤销。**最终**：拿本栏第一条注释的起笔当尺子，续行只多缩三四点、
   图题差几十上百点，整栏没注释的就全是图题。**教训**：同一症状在不同页面版面表现可以不一样，
   样本不够大的规则先别信；别拿语义当版面判据。
5. **引号里不断句（386 条例句）**。切句遇引号深度就不切，《孔雀东南飞》刘兰芝一开口二十多句
   连成 592 字一行，《赤壁赋》客人那段几百字成了一条例句。**决定**：不修补引号计数，直接把
   `build.py` 的 `sentences()`/`relineate()` 两处切句换成新函数 `cut_stops()`、对齐 `index.html`
   的 `buildQuestions`（见句号就切、末尾引号补回上句）——**让没验证的一方贴近已在页面上跑过两轮、
   验证过的一方**。超长例句 386 → 0。
6. **齐头排的诗丢了分段（8 篇）——三轮才收敛**。《离骚（节选）》《孔雀东南飞》《琵琶行》版面跟
   散文一样连排、两端对齐、段首缩进，分段全在缩进上，却按行距去找、整篇并一段。《离骚》因此连
   「背诵第3段」都落了空。**第一轮** `flush_left()` 按左对齐判断→误伤《沁园春·长沙》（它整块居左、
   右边参差被误判成散文）→整个推翻。**第二轮** `justified()` 改按右对齐判断（《沁园春》右边只 8%
   顶到头、《离骚》77%）。**第三轮** `regap()` 的行距基准从段内中位数改成全篇中位数
   （《声声慢》下阕才四行、两行带角标偏短把中位数带偏，「梧桐更/兼细雨」被从中间劈开）。
7. **离骚背诵范围错位**。分段修好后核对「背诵第3段」，发现「…………」那行缩进两格被当成独立段，
   第3段落到省略号上。`join_ellipsis()` 并回上段、不占段号；页面 `buildQuestions` 跳过没有汉字的题。
   改完在浏览器里跑 `parseCorpus`+`buildQuestions` 确认要背的题是「长太息以掩涕兮」到
   「固前圣之所厚」，正好是课本第3段。
8. **《论语》十二章成了十一章**。那页右边嵌插图、前十二行绕图缩进排、最后两行才整行宽——一页
   两套左边界。分段基准线只取一条、落在绕图那段上，第七章第八章被并成一段。解法：不取一条基准线，
   找**成对的缩进关系**（某起笔正好比另一个靠右两格就是段首）；顺带发现引号开头的段只缩 18 点
   不是 24 点（《侍坐》「“赤！尔何如？”」），容差放宽到 14–28 点。
9. **半角叹号**（《兰亭集序》「岂不痛哉!」）。Times 半角 `!`（宽 4 点，全角 12 点），断句认不出来，
   全书仅此一处，`squeeze()` 里按全角收。判断依据：这是排版疏漏、纸面上就是个叹号——跟《梦游天姥》
   末句那个课本选定的问号不同，那个照留。
10. **拼音全量比对**（顺手）。「葳蕤 wēiruí」被切成 wēir/uí（音节正则允许 r 当韵尾，加否定前瞻）；
    国标音 ɡ（U+0261）统一成 g（摐、毂、洴、絖、舸）。拿 pypinyin 把两百多条注音跟字典逐条比，
    剩两处不一致是课本自己的读法（噫吁嚱 xī、山川相缪 liáo），照留。

**尝试和看法**：这轮把前几版的判据补强了（连排判据加右对齐和行长上限，补 v0.14 的漏洞）。
方法论最大收获：**结构体检 + 全量自检比肉眼抽查靠谱，但每条报警都要回原始 PDF 判真假**。
收尾前用全量体检代替最后一遍抽查：3031 条注释全部词条回查通过、释义收口、例句是完整句子；
浏览器实测六模式无报错。（补日志 f04e1a2/f095482 把过程记进当时的 PROGRESS.md，v0.2 迁进本 LOG。）

**本轮未完成 / 趋势**：理解性默写、译文解析两板块还空着；文档还是单一份 PROGRESS，不成体系。下一轮建文档体系。

## v0.2　建立文档治理体系（当前）

**2026-09-19 15:06 起**（commits 2c07075, 153bf8a, 9046c71, 8ed05eb, db70f39, 64d77eb, c57cff1, 及本次重构）

**提了什么**　把文档系统化，让规矩写进仓库自动生效、而不靠使用者每次口头提醒。使用者还确立了
新的工作方式：动手前先反复沟通对齐、每次问、确认了再开始。

**解决了上轮遗留**：v0.17「文档不成体系」这轮解决。

**分条目**：

1. **确立文档分工与单一事实源**：`LOG.md` 记历时（只追加、正序、不删改）；`data_structure.md` /
   `html_structure.md` / `extract_pdf_method.md` 记现状（原地更新）。同一件事只在一处写实体、别处放指针。
2. **建三份现状文档**：数据结构（六板块字段、生成管道、权威关系、user.js）、PDF 解析方法
   （18 个坑和当前解法）、页面结构（零依赖前端、六模式、解析/出题/批改）。
3. **建 `LOG.md`**：把原 PROGRESS.md 迁入。**本次又按使用者要求重构**：改用版本号
   （v0.0→v0.1 是前一位作者、v0.11→v0.2 是我），把前一位作者的日志原样并入作 v0.0→v0.1，
   我的每一段扩写得更细（因为要换窗口、不写就永久丢）。删掉 PROGRESS.md。
4. **建 `modify_manual.md`**：工作流程写成停止点（🛑 对齐确认、🛑 方案确认），含验收方式硬要求、
   「自行决定」的三条边界（不改既有约束/数据格式/依赖）。
5. **`README.md` 改成总索引 + 文件指南 + 末句指路**；**`CLAUDE.md` 极短索引 + 硬指令**
   （Claude Code 每次自动读根目录 CLAUDE.md，把「动手前必读 modify_manual」写进去，规矩自动生效）。

**尝试和看法**：核心是使用者的两条设计。一是**记现象不记结论**（「某字体在 Safari 下错位」而非
「不能用这字体」）——经验换环境可能不成立，将来落进 `restrictions.md`。二是**旧日志条目永不删改**，
结论错了补「已被 vX 推翻」——把错误留下来省得下一个人重走；这轮我已给 v0.13（难字字频判据）、
v0.14（连排判据）补了修正/补强标注。这次「其实项目是两拨人做的」也是这个精神：不把前一位作者的
v0.1 冒认成自己的，如实并入。

**本轮未完成 / 趋势**：
- `restrictions.md`（禁令库，带字段和状态位）、`CORPUS-FORMAT.md`（挖空规则）暂缓，等真正处理
  语料时再建；图片库等处理图片时再建。
- 两板块仍待补：理解性默写（`comprehension`）、译文解析（`gloss`）——课本无现成内容，量最大的是
  译文，建议单开一轮。
- 难字只能按「篇+字」圈，圈一处等于整篇都圈；要只圈某处得给 corpus 加一种「只此一处」写法。
- 连排长篇（《琵琶行》《孔雀东南飞》《蜀道难》）按句末标点断行会把对句切开（「其间旦暮闻何物？」
  「杜鹃啼血猿哀鸣。」各成一题），这三篇不背、不出默写题所以没动；将来若拿它们出题要按字数
  把偏短的行并回去。
- 常用字词目前只覆盖课本里出现过的词，不是脱离课本的 3000/6000 通用字表；将来若要通用字表，
  单开独立模块，别跟现在的词语模式混。

## v0.21　词语模式：拼音分字、只挖难字、补释义与例句

**2026-09-20**（commits 75bf637, 712d7fb, 8e19810, 4790154, 及本条）

### 对话实录

> 从这一轮起，日志头一节照使用者的要求放「对话实录」——他的原话、我的汇报、
> 怎么一步步定下来的。技术总结在后面。

**使用者（带一张词语模式的截图，题目是「穹庐」）**：
> 「我问问看，我觉得看拼音写词语有些小问题，看词语读拼音的拼音要加上空格，
> 字和字之间得分开，看拼音写词语的时候只挖空难得那个字，然后我希望在这个模式
> 加上解释和例句或者出处功能，你能拿到权威的字库和解释吗，如果可以的话加个解释
> 如图，至少拼音之间得加空格，然后我根本不知道这个啥意思，你可以搞点权威例句和
> 解释我看看」

**我的汇报（第一轮）**：查证后报了实数——拼音空格是生成时 `norm()` 吃掉的、
源表本来就有；「写」类里 1 个生僻字的 962 题；释义能覆盖 1100/1320、220 查不到。
当场说清第三方词典是 OCR 整理的、有四类残缺，**不能叫「权威」**。提了四条建议
（例句取法、板块扩字段、词典入库、自拟释义单开一表）和六个待定问题。

**使用者（这一段最重要，含好几条定规矩的话）**：
> 「我建议：你之后要是多条建议给我，你就写成选项，**1 同意，2 反对，
> 3 大部分同意+自己的看法，4 大部分反对但有可取之处**。
> 1 例句优先从课本正文抓，别用词典例句。我觉得可以，但是新华字典之类的有例句
> 也可以写上嘛，没问题；2 人名最简单，你就写，**人名，字 xxx 号 xxxx，xxx 朝
> 政治家军事家，写过 xxxxx**；3 同意。
> 六个问题：1 你可以搞下来放到我们的 github 库里嘛，就下载一下免得以后找不到了；
> 2 查不到的先看书下注释，然后插两本字典，要是都没有说明看考察概率较低，
> 你自己写吧，说明是你自己写的就行；3 说过了；4 都挖，但是还是，**你看拼音写汉字
> 的时候得给个情景，要不然太难了，或者给例句也可以**；5 每个字之间的拼音分开；
> 6 同意，找不到你就自己写，说明是自己写的就行；7 核对一下呗，**所以为什么我说
> 日志要写详细仔细**。」

从这段里定死的规矩，除了本轮内容，还有三条是长期的：**多条建议写成选项让他用
1/2/3/4 回**、**人名的写法模板**、**日志要写详细**。「看拼音写汉字得给个情景」
是他在这儿临时加的第五条需求，成了 v0.22 补例句的由头。

**我的汇报（第二轮）**：把他的答复复述一遍核对，又提了四条建议（题面样式、
八段格式、词典入库方式、自拟释义标记落表）和执行七步、验收方式。

**使用者**：
> 「1 同意 2 同意 3 同意 4 同意，先做起来吧，**记得把现在你给的方案也写进日志**。」

——「方案也写进日志」这条，是我在 v0.21 日志里专门留「这一轮的方案」一节的原因。


**提了什么**　使用者用词语模式时提的四条：①看词写拼音的拼音要按字分开加空格；
②看拼音写词语只挖难写的那个字；③这个模式要加释义和例句／出处——「我根本不知道
这个啥意思」（截图里是「穹庐」）；④问我能不能拿到权威的字库和解释。
后来又补一条：**看拼音写汉字得给个情景，要不然太难了，或者给例句也可以**。

使用者同时定下一条沟通规矩：**以后多条建议要写成选项**，他用
「1 同意／2 反对／3 大部分同意+自己的看法／4 大部分反对但有可取之处」回。

### 这一轮的方案（先对齐、后动手，原样记在这儿）

按 `modify_manual.md` 的两个停止点走：先查证、复述、提选项，等使用者确认了才写文件。
**查证的实数**（提方案前先跑出来的，不是拍脑袋）：

- 拼音空格：源表本来就是 `穹庐　qióng lú`，是生成时 `norm()` 把空格吃掉了，零成本恢复。
- 只挖一个字：「写」类 1147 题里，1 个生僻字的 962 题、2 个的 179 题、3–4 个的 6 题。
- 释义覆盖：课本注释 293 + 成语词典 199 + 新华词典 608 = 1100/1320（83%），220 查不到。

**当场说明的数据质量缺陷**（不能把第三方词典说成「权威」）：chinese-xinhua 这套是
纸书 OCR 整理来的，开引号被吃掉（「即骆驼”其奇畜则橐驼」）、义项带 `1.` 序号、
成语例句混着 `★` 和 `～`、释义常给古义不贴课本语境。

**我提的四条建议 + 使用者的答复**：

| # | 建议 | 答复 |
|---|---|---|
| 一 | 写题题面给例句当情景，例句里那个词按和答案同样的范围遮住 | 1 同意 |
| 二 | `words` 板块四段扩成八段（越了「不改数据格式」的界，单列） | 1 同意 |
| 三 | 36 MB 原始词典原样入库 | 1 同意——「下载一下免得以后找不到」 |
| 四 | 自拟释义标 `自拟`、落在 `tools/释义-自拟.tsv` 单独人工表 | 1 同意 |

**六个问题的答复**：①词典文件下载入库；②查不到的先看书下注释、再插两本字典，
都没有说明考察概率较低，自己写、标明是自己写的；③人名不剔，按
「人名，字…号…，某朝某家，写过《…》」写；④两个生僻字都挖；⑤拼音每个字之间分开；
⑥采信顺序同意（课本注释 > 课本原句 > 成语 > 新华 > 自拟），找不到自己写并标明。
另外要求核对 `words.py` 里「课本注音词条 387 条」与 LOG 的 188 对不上那处。

**执行方案七步**：词典入库 → `words.py` 拼音分字并抽释义例句 → 释义清洗 →
人工写自拟释义 → `build.py` 扩字段 → `index.html` 三处改动 → 重跑 `merge.py` 并更新文档。
**验收方式**（方案里就写死的）：`words.py` 跑完拼音 100% 带空格、释义空缺 0 条；
`build.py` 三道自检全过；Playwright 六模式无报错并肉眼看截图；抽查穹庐／樊哙／
字里行间／朱熹／青蒿素五条回落链；单文件版一致。

### 实际做了什么

1. **两本词典入库** `tools/data/新华词语.json`（264434 条）、`成语释义.json`（30895 条），
   来源、用途、四类固定残缺记进新建的 `tools/data/来源.md`。
2. **拼音按字分开**，成语也分（`ē dǎng bǐ zhōu`）。
3. **释义四级回落**：课本 294 / 成语 199 / 新华 607 / 自拟 220，**空缺 0**。
   课本那级连长注释里套的小注一起收——「衿，衣服的交领」只在小注里讲过，
   不收就白白落下，收法和 `build.py` 拆 `notes` 板块一致（课本命中因此 251 → 294）。
4. **例句三档**：课本古诗文原句 532 > 成语的古典书证 > 其余课文，合计 1088 条。
5. **220 条自拟释义**：人名 16、地名篇名 2、成语与四字语 20、文言词约 100、
   现代常用词约 80。
6. **页面**：只挖难字、给例句情景、翻答案给释义和来源标签。

### 试错，成的败的都记下

- **例句配平的教训（同一个坑第二次踩）**。一开始按「成对符号配平」筛例句，把引号也算
  在内，结果《鸿门宴》整段都活不下来——文言对话本来就跨句，「沛公奉卮酒为寿……曰：
  “吾入关」只有半个引号却是完完整整的课本原文。**这正是 v0.17 第 5 条「引号里不断句」
  记下的同一件事，换个地方又踩了一次**。改成只查括号和书名号（《窦娥冤》的舞台提示
  「（刽子做取席站科」确实该挡），引号放行，只把甩在句尾的开引号抹掉。
  卮酒、嗟乎、谗言、一篑等一批词的例句因此才捞回来。
- **长句切窗口的 off-by-one**。「孝公用商鞅之法，」带逗号 8 字、去掉逗号 7 字，
  而下限正是 8——先判长短再去标点，把这句好例句误杀了，「商鞅」因此一度没例句。
  改成**长短按去掉首尾标点之后算**。
- **例句挑错了篇**。「琵琶」一开始取到单元导语里「白居易《琵琶行》对音乐的传神描写」，
  只因为那句更短。加了档位：古诗文原句永远排在其余课文前面，才取到「忽闻水上琵琶声」。
- **开引号补不回来**。`即骆驼”其奇畜则橐驼` 这类，试过补一个开引号，但补在哪儿全靠猜，
  猜就是编。最后决定**在那个 ” 处截断**，只留前半截「即骆驼」——宁可少给，不要编。
- **三处漏答案，都是跑起来才看见的**：写题的例句明摆着那个词；读题的例句走
  `renderHard` 把难字拼音一并渲染出来（「见樊哙kuài」），正是要问的答案；
  **出处也会漏**——「青蒿」的例句出自《青蒿素：人类征服疾病的一小步》，标题里就有
  那个「蒿」。第三处是看截图才发现的，脚本检查根本发现不了。
- **栏目标题当不了出处**。「第八单元」「二运用有效的推理形式」这类 48 条，
  套上书名号不成话，归到册名，页面写「课本 必修上」不加书名号。

### 核实使用者点名的那处数字

`words.py` 文档字符串写「课本注音词条 387 条」，实测 `from_notes()` 抽出 **191 条**、
去重过滤后进表 **188 条**。**LOG v0.16 记的 188 是对的，387 是错的**，已改正并在
代码里注明。——这正是使用者说「日志要写详细仔细」的用处：两处数字打架时，
有日志才知道该信哪个。

### 顺带发现、已按住的一个错

**「岑参」诗人名里的「参」读 shēn**，pypinyin 按常用音注成 cān，看词写拼音会教错。
给 `释义-自拟.tsv` 加了第四列「拼音订正」按住它。其余两百多条人名地名的注音逐个核过。
**釜底抽薪的办法（拿人名词典系统校验专名读音）这轮没做**，专名多了再说。

### 本轮未完成 / 趋势

- 232 个词没有例句（多是只在注释里出现过的词和人名），页面拿释义当情景顶上。
- 专名读音没有系统校验，只按住了「岑参」一条。
- 自拟的 220 条释义是我写的，**没有第二个人校过**；使用者说过要过目，清单在
  `tools/释义-自拟.tsv`，可以逐条驳回。
- 理解性默写（`comprehension`）、译文解析（`gloss`）两板块仍然空着，跟 v0.2 一样。

## v0.22　补齐自拟例句，词语模式每题都有情景

**2026-09-20**（commits ab6cbb1, 及本条）

### 对话实录

**我的汇报（v0.21 收尾）**：报了一条遗留——232 个词还没例句（多是只在注释里
出现过的词、人名，如白芷、颔联、姜夔），这些题只能拿释义当情景顶上。问他要不要
给这批也写自拟例句。

**使用者**：
> 「232 个词仍然没有例句…同意，做吧，**记得做完了写日志！这个以后专门向我汇报
> 一下，写的详细点哈，你刚才列给我的东西你可以都写进去，技术细节够了你之后看起来
> 就方便了**。」

——「你刚才列给我的东西都写进去」，指的就是我汇报里列的那些发现，他要日志把这些
一并收着。这是他第二次强调日志要详细。

**我的汇报（做完后）**：例句补齐、每题都有情景；顺带报了两个新发现的问题——
41 条词典释义在打转、400 条整词挖空——各给一条建议，请他定。


**提了什么**　v0.21 收尾时我报了一条遗留：232 个词没例句，这些题只能拿释义顶上。
使用者答「同意，做吧」，并交代**做完写日志、日志写详细些，技术细节都写进去，
以后自己看着方便**。

**解决了上轮遗留**：v0.21 的「232 个词没有例句」，这轮做完。

### 做了什么

人工写了 **232 条例句**，例句覆盖 **1088 → 1320，缺口 0**。分两种落法：

- **55 条**填进 `tools/释义-自拟.tsv` 已有的行（这些词本来就是自拟释义）。
- **177 条**新起一行，只给例句、释义列留空——它们的释义走课本／成语／新华那三级，
  自拟表在这里只提供情景。表里为此单开一段，标题写明这件事。

自拟例句的出处记作 `自拟`，页面上 `fmtFrom` 写成「**自拟例句**」，**不套书名号**：
它不是课本原文，不能跟《鸿门宴》那样的出处混为一谈。

### 这轮定的规矩：一个字都不许重复

写例句时最容易犯的错是**例句本身把答案漏了**。页面遮字看难字表，而且
「一个难字都没有的词整词都遮」——到底遮哪几个字，写例句的时候算不清。
所以定了条最严的、不依赖难字表的规矩：

> **圈起来的那处之外，例句里不许再出现这个词里的任何一个字。**

这样无论页面遮一个字还是整词都漏不了。写成 `words.py` 的 `check_hand()`，
跑脚本时自己报，是继角标配平、词条回查、例句完整之后的**第四道自检**。
另两道：词必须用大括号圈起来；长短 8–46 字，和自动抓的例句一个尺度。

**这道自检当场抓出 10 处**，都是我自己写漏的：「曲水流觞」的例句里又出现了
「水」、「鼓槌」里又出现「鼓」、「虞舜」里又出现「舜」、「青衿」里又出现「青」、
「邯郸记」里又出现「记」……逐条改掉。**教训和 v0.17 一样：人写的东西也要过机器，
自己检查自己是查不出来的。**

写之前还先跑了一版宽松的检查（只查「会被遮住的那几个字」），只报 9 处；
换成最严的规矩后又多报 10 处。**宽松的检查给人虚假的安心**，这是两版检查
对比出来的，记下来。

### 顺带发现的两个问题（还没改，等使用者定）

1. **41 条词典释义在打转**：「橐驼→即骆驼」「卮酒→犹言杯酒」「嗟夫→犹嗟乎」
   「踌蹰→见踌躇」「六幺→见六幺令」——查了等于没查。都是新华那级的交叉引用条目，
   `clean_def()` 没认出来。另有 **81 条释义里还留着 `｜` `～` `◇` `ㄧ` 等符号**
   （「呦呦→拟声词呦呦鹿鸣｜呦呦有声｜呦呦地哭了」），那是原书的例句分隔符和
   代词符号，清洗时漏掉了。
   > 注：这个判据会误伤，「晤谈→见面交谈」是正常释义，只是正好以「见」开头。
   > 真要处理得逐条看，不能脚本报什么改什么——和 v0.17 那条教训一样。

2. **400 条「写」题整词挖空，其实只有部分字生僻**（「刽子」「桅杆」「唢呐」
   「札记」「笺注」）。原因是页面判「难写」看的是 `hardChars`，而那张表是从
   **66 篇古诗文正文**里抽的 579 字；词语表里大量词来自现代文，一个字都不在表内，
   于是走了「一个难字都没有就整词挖」的兜底分支。**这等于没有完全做到 v0.21
   说的「只挖难的那个字」**，是上一轮交付里没发现的缺口。
   解法我倾向在 `words` 的词字段里用大括号圈出该挖的字（`{桅}杆`），和 corpus
   圈难字一个写法，页面按括号遮——但这是改数据格式，按手册得使用者点头才能动。

### 本轮未完成 / 趋势

- 上面两条都还没改，等使用者定。
- 232 条自拟例句是我一个人写的，**没有第二个人校过**；使用者说过要过目，
  清单在 `tools/释义-自拟.tsv`。
- 专名读音仍只按住了「岑参」一条，没有系统校验。
- 理解性默写（`comprehension`）、译文解析（`gloss`）两板块仍然空着。

## v0.23　只挖该挖的字，打转的释义逐条重写

**2026-09-20**（commits 2f9cf4e, eae8f94, 及本条）

### 对话实录

**我的汇报（v0.22 收尾）**：把两个问题写成两条建议——建议一：在词里用大括号圈出
该挖的字（`{桅}杆`），修掉 400 条整词挖空；建议二：41 条打转的释义逐条改成自拟。
并说明建议一是改数据格式，按手册得他点头。

**使用者**：
> 「1 同意 2 同意，做吧。**记得写日志大哥，你没提这个，请写进去**。」

——这一句是因为我上一条汇报的收尾没主动说「做完写日志」，他补的。到这儿他已经
连着三轮盯日志了。

**我的汇报（做完后）**：两条都做完，报了数（整词挖 450 → 132、打转释义分两批
处理）。

**使用者（本轮，把「日志要全文记对话」正式定成规矩）**：
> 「还是那句话，每次你和我说了啥，你都写到 log 里面，你和我说了一二三我看是次要的，
> 最重要的是你下次得看到看懂，**日志就首先应该包含我们对话的所有内容和你的汇报**，
> 写了吗，去写。」

——照这条，我回头给 v0.21、v0.22、v0.23 都补了「对话实录」放在最前面，并把这条
规矩写进 `modify_manual.md` 第四节：**日志头一节必须是对话实录，使用者的话尽量
原样引，我的汇报照录，「他说什么 → 我怎么回 → 他怎么定」的链要连得起来。**
理由是他反复讲的那句：换窗口后接手的 AI 靠日志还原现场，只留提炼过的一二三，
下一个人看不懂为什么这么定。


**提了什么**　v0.22 收尾时我报了两个问题并各给一条建议，使用者都答「1 同意，做吧」，
另外叮嘱**写日志**。

**解决了上轮遗留**：v0.22 报出的两条，这轮都做完。

### 一、400 条整词挖空——v0.21 交付里的缺口

**病根**：页面判「难写」看的是 `hardChars`，而那张表是从**66 篇古诗文正文**
抽出来的 579 字。词语表里大量词出自现代文（刽子、桅杆、唢呐、札记），
一个字都不在表内，于是全走了 `hardIn()` 的兜底分支——「一个难字都没有就整词挖」。
**等于没做到 v0.21 自己说的「只挖难的那个字」**，而 v0.21 那轮的验收里没查这一项，
所以没发现；是 v0.22 顺手统计时才撞见的。

**解法**：判据在生成侧本来就现成（不在《通用规范汉字表》一级字表里的算难写），
所以让 `words.py` 直接在词里把该挖的字用大括号圈出来——`{桅}杆`、`{琵琶}`、`{穹}庐`
——**和 corpus 圈难字一模一样的写法**，页面照括号遮字（`mark_hard()` / `hardIn()`）。
连续的生僻字并成一对括号，整词都是常用字的不圈、页面自会整词挖。

**要紧的一点：这些字不进 `hardSet`。** 难字默写模式的范围是古诗文正文，词语表管词语，
两边各管各的。实测 `hardSet` 仍是 579 字、「桅」不在其中，难字默写没被带歪。

**效果**（1147 道「写」题）：只挖其中几个字 697 → **1015**；整词挖的 450 → **132**，
而这 132 条是「琵琶」「箜篌」这种整词都生僻的，本来就该全挖。

> 为什么不把词语表的生僻字并进 `hardChars`：那张表是难字默写模式的题源，
> 掺进现代文的字会把那个模式的范围搞乱。**两个模式的「难字」不是一回事**。

### 二、41 条打转的释义，逐条看过分两批

**「打转」指查了等于没查**：「马厩→见马厩」「词笺→见词笺」「六幺→见六幺令」
「嗟夫→犹嗟乎」——新华那一级的交叉引用条目。

- **29 条确实在打转，人工重写**：马厩→养马的棚舍；相缪→互相缭绕盘结，《赤壁赋》
  「山川相缪」即此；五羖→指百里奚，秦穆公用五张黑羊皮赎回；砒霜→剧毒的白色粉末，
  成分是三氧化二砷；青荇→水中的荇菜，徐志摩「软泥上的青荇」即此……
- **13 条留着不动**：「纸鸢→即风筝」「羌管→即羌笛」「自诩→犹自夸」「皎月→犹明月」
  「能彀→同能够」这类是**有效的同义解释**，学生一看就懂；而且新华那级比自拟更可信，
  改成自拟反而降格。**「晤谈→见面交谈」压根不是打转**，只是正好以「见」开头——
  v0.22 日志里就提醒过这个判据会误伤，果然误伤了。**所以是逐条看的，没让脚本一刀切。**

为此把**自拟表的释义列改成人工覆盖**：写了就压过三本词典，留空才走词典。
道理和难字一样——人在表里写了，就以人写的为准。同时加一道提醒：自拟释义要是
压过了课本注释会报出来（课本是最权威的一级，不该被悄悄压掉），目前 0 条。

### 三、81 条释义里的原书符号

`｜`（例句分隔）、`～`（代指词条本身）、`◇`、`ㄧ`，以及 OCR 把标点认坏的 `『` `∨`
（「蜀郡『仍其旧」那个 `『` 其实是「汉」字）。`clean_def()` 在第一个这类符号处收口，
只留前半截——后面跟的是例句不是释义。现在 0 条。

**顺带修了个自己挖的坑**：这轮新加的「亦作某某；正经释义 → 砍前半截」这条规则，
碰上整条就只有那一句的（「咔嚓：同‘喀嚓’。」）会把释义砍得一个字不剩，当场少了
2 条释义。加了判断：砍完没剩下就不砍。**新加的清洗规则要连「整条都是它」的情形
一起想到**，这是第二次在清洗上栽同样的跟头（上次是 v0.21 抹开引号抹掉半句）。

### 现在的数（1320 个词）

| | 课本 | 成语 | 新华 | 自拟 | 缺口 |
|---|---|---|---|---|---|
| 释义 | 294 | 199 | 576 | 251 | **0** |
| 例句 | 课本古诗文原句 532 | 成语书证 | 其余课文 | 自拟 232 | **0** |

验收：`words.py` 四道自检静默通过；`build.py` 三道自检全过；浏览器里 1504 道
词语题全部有情景，六模式零报错；「桅杆」「刽子」「札记」现在都只挖一个字，
`hardSet` 仍是 579、「桅」不在其中；单文件版重跑一致。

### 本轮未完成 / 趋势

- 251 条自拟释义、232 条自拟例句都是我一个人写的，**没有第二个人校过**。
- 13 条同义解释的释义留着没动，见上面的理由；将来若嫌简略可以再补。
- 专名读音仍只按住了「岑参」一条，没有系统校验。
- 理解性默写（`comprehension`）、译文解析（`gloss`）两板块仍然空着——
  从 v0.2 挂到现在，是眼下最大的一块空白。

---

## v0.24 → v0.25　项目升级成多科目：科目壳 + 数学速算引擎

**2026-09-20 / 09-21**（commits 5715eb5, 及本条所附提交）

> **这一轮是项目性质的转折**：从「古诗文默写」变成「学科综合复习软件」。
> 语文那台引擎一个字没改，只是搬了家；新造了一台完全不同的引擎给数学。
> 下面先录对话（本轮讨论了四个来回才动手），再录执行过程。

---

### 一、对话实录

#### 第 1 轮　使用者提出「善用这台背诵机器」

**使用者（起头）**：
> 「我想就做一些拓展性的内容，就是你看我们这个它本质上是个背诵系统嘛，那我们可不可以
> 背诵一些别的东西，比如说英语单词或者这个数学，物理，化学的方程式和公式？因为有很多
> 东西你可能推导，那是可以推导，但是你如果把它背下来的话，就会很省力啊。说三角里的那个
> 二倍角公式，三倍角公式各种乱七八糟的组合公式和差化积积化和差。叫什么降幂公式三倍角
> 公式化学里的一堆方程式对吧？这个基础概念啊，物理里的一堆二级结论你都可以背一下。就
> 我在想，他既然反正是一个背诵的内容，那我们可不可以就是就善加利用这个背诵的这个机器？」

**我的汇报**：读了 `data_structure.md`、`html_structure.md` 和 `index.html` 的关键段落，
报了三条：

1. **引擎能复用，而且有个决定性的有利条件**——这个系统不判对错、靠自评，所以公式写法
   千变万化的判分难题天然绕过去了。
2. **两个硬的格式冲突**：半角竖线 `|` 是六个板块的字段分隔符，而数学里它是绝对值 `|x|`、
   集合 `{x | x>0}`、条件概率 `P(A|B)`；大括号 `{}` 是挖空/圈难字标记，而 LaTeX 通篇
   `\frac{a}{b}`。这两个符号是 `parseRows` 和 `stripMarks`/`hardIn` 的地基，改了等于动
   全部数据格式，正撞手册第二节「不改数据格式」红线，所以我不自己定。
3. **建议先拿英语单词试刀**，因为它和现有 `words` 板块八个字段几乎同构（词/拼音 →
   单词/音标），`renderWordBlank`、`renderWordEx` 和三处防漏能直接复用。

另外单独点出一个矛盾：**现在每条数据都能溯源到五本课本 PDF、「一字不改」，释义还分
课本/成语/新华/自拟四档标可靠性**；而物理二级结论、化学方程式、英语词表我手上没有对应
教材，靠我写的话整批数据的可靠性就是「自拟」，和项目已定的原则冲突。请他拍板。

发了一份四问的问卷（内容来源／试点科目／架构落点／公式渲染）。

#### 第 2 轮　使用者回问卷，并甩出两件新东西

**内容来源**，使用者：
> 「这些东西全部都是我给你建议加一半，我自己提供……数学，物理，化学的公式呃，有一些我会
> 直接给你，因为我有整理了一些，然后另外一些就是我会提出一些大类。比如说我说来我们整理
> 一下铁的反应。那我们就把铁的方程式写一下，然后我稍微自己看一眼。」

**新东西一：口算**，使用者：
> 「我们不仅可以做这些，还可以做一个口算类的内容。因为我们在算很多东西的时候，经常会遇到
> 口算的情景。比如说算差积，就是我想跳过草稿部分，直接算出来，但是会有点慢，然后可能会
> 错……还是比较二次方程快速求根啊。比如说一到30的平方啊。呃，两位数乘两位数之类的。」

**新东西二：部署形态**，使用者：
> 「一个是写成APK文件，我就在我的手机上装了一下，然后一个呢，就是我们在网端网页端去部署
> 一下……这两个数据或许我们可以做一个互通。就我们能够做到离线的时候也能使用，然后在线的
> 时候也能在网页端使用。」

**产品定位**（这句我认为是整个项目的立足点，写进了 README）：
> 「我做这个东西是因为有很多垃圾时间，这些时间我可能没有办法去，我只能看手机。那我在想
> 如何在看手机的这个同时去掌握巩固知识呢？我是首先想到了语文。然后我就洞察了生活中的
> 很多痛点，我又找到了数学，英语呃等等的这些问题。」

**架构形态**，使用者：
> 「比如说一开始进来有个选项，就是今天要做什么？然后比如说有语文，数学，英语，化学，生物，
> 物理。一点进去，每个就是一个单独的页面，每个页面就是现在你做的那个类似的那个页面，就在
> 外面再加一层。因为里面那个各层级，我觉得现在就挺好的。」

**并叮嘱**：
> 「你先别急着做，我们就先讨论讨论。」

**我的汇报**：报了一条我认为决定架构的判断——**口算不是「再加一个科目」，是第二台机器**，
现有引擎的四条根基假设它全反过来了，列了对照表（静态题库↔现场生成、遮住已有文本↔算出
不存在的答案、不判对错↔能判且必须判还得计时、按句子内容存 key↔每题不重样 key 存什么）。
结论是他那个「外面再套一层」的直觉对：外壳管科目和数据，里面每个科目挂不同引擎。

部署上报了：纯静态零依赖，**GitHub Pages 一推就有网址，零成本**；建议 **PWA 代替 APK**
（manifest + service worker，加到主屏幕，离线可用，和网页版同一套代码）；「多用户备份 +
两端自动互通」需要后端+账号+数据库，是质变，建议留到最后。

还提醒了一条按手册要回来问的事：仓库叫 `Guwen_recite`、README 通篇古诗文，改成多科目
平台就得重写文档体系。

#### 第 3 轮　使用者定调，并要我发问卷

**定调**，使用者：
> 「我是工科类的这个人士……我基本上来说做工程的时候我可以以理性为主导。所以说我觉得没
> 关系，你要做不出来就留白吧，不用担心，就是我进去会感觉不美观或者受到挫败感没关系，
> **只要日后方便维护就好，最终的一个版本肯定要搞点美观性，中间版本咱们就暂时就以这个
> 维护性为上头吧**。」

**文档改动的边界**（这条我逐字照办了），使用者：
> 「对你 read me，你要改一下，然后 modify manual。**你看着改吧，但是你别改太离谱了，
> 因为那玩意儿是我写的。就是和我们目标冲突的，你改了别的大部分，你别动。**」

**顺序**，使用者：
> 「这个 APK 和 PWA 我不是很了解啊，但他应该暂时不是第一个步骤。我觉得可以，你先在外面套
> 一层大的壳，就是选择科目的那个壳。然后咱们就挂那个数学口算吧。然后如果我们验证成功的话，
> 我们就把它挂到那个 PWA 上面。」

**并问了一个具体问题**：
> 「如果我挂到了 PWA 上面的话，那就是你还能实时的更新吗？还是说我要手动同步一下？」

**数学的分类**（他自己列的大类和细类），使用者：
> 「数学可能分为几类嘛……第一类是数字计算。一个是三角计算。一个是向量计算。一个是平面
> 立体几何计算。一个是方程计算。一个是复数计算，一个可能是别的，比如说导数计算或者概率
> 计算，每个计算就是原来我选篇目的那个地方，你就放一个细的，比如说数字计算里面，就比如说
> 有1000以内的加减法。然后这个加减乘除混合运算，然后平方数。或然后两位数乘法。还有就是
> 比如说算一些很常见的东西，比如说 a×b+c×d+e×f。然后又算一下，比如说什么？分母有理化呀，
> 化简数啊之类。」

**要我发问卷**：
> 「你先搭个壳……然后你就给我来一个问题，就是说给我发问卷嘛，就是你觉得这个玩意儿有哪些
> 方面，然后你列出的是哪些，然后我就每个东西针对性的跟你说一说是哪个方面。」

**并定死了实现方式**：
> 「这个东西你也不用有数据库了，**你就自己造个程序程序生成题目，然后给出答案，然后再写个
> 判断器就行**。怎么样？我觉得这样做比较好。」

**我的汇报**：先回答了 PWA 那个问题，分成两件事讲——**程序代码是全自动更新**的
（service worker 后台拉新版，可做成「下次打开生效」或「弹一条提示点一下更新」，不用重装，
这正是 PWA 比 APK 强的地方）；**答题数据仍是手动**（存各设备自己的浏览器，要互通得上
账号+后端，也就是排在最后的第 4 步）；离线由 service worker 缓存保证。

然后发了问卷：按他给的八个大类展开，每类列细项，我已提的标 ★、我补的不标，另外补了
**I 数列**、**J 指数对数**、**K 二级结论专项** 三组。并先说了一句观察：他举的例子混着
两类——「两位数乘法」是真**算**、「1–30 的平方」是**背**——而这台引擎两类通吃，生成题目
和比对答案的机制一样，不用分开做。

另外问了三个配置问题（计时怎么做、弱项按什么记、答案怎么输入），并**建议先窄后宽**：
这一轮只做「壳 + 引擎骨架 + 两个大类」，理由是交互只有一套，如果手感错了，铺开十个大类
再改就是十倍返工。

#### 第 4 轮　使用者逐条批复（本轮所有设计决定都出自这一条）

**否决了输入判分**，使用者：
> 「就是如果是输入判对错的话，第一呢，输入不太方便，因为手机端嘛，第二，如果你遇到根式啊，
> 什么复杂的形式向量，你可能打不出来，所以我觉得**要么还是我自己判对错吧。反正是我自己
> 决定了，那我也肯定不会瞎判的**。」

**定了自评三档 + 可选填错答**，使用者：
> 「**你就写三个算对了，算错了和没算出来。然后如果是再算错了，那个里面呢？你可以填一下你
> 算错的答案是几？那我们下次可以当做参考。这个是这个不是必选项，这是可选项，因为有的时候
> 这个真没法输入。**」

**删了两条题目**，使用者：
> 「就是除了那个对数和根号的常用值之外，那个可以不用写，因为我们不用背那个……**因为我们
> 考试是可以采用计算器的，所以对计算性的要求没有那么高。**比如说常用无理数近似值和常用
> 对数近似值我们直接从计算机上敲就可以了。而且一般来说，它不会让你写近似值。」

**留了分数小数互化**，使用者：
> 「分数小数互化，因为物理和化学可能有时候，比如说你算个 3 分之几啊，这个你也不想敲
> 计算器，对吧？我觉得这个可以练一下。」

**把几何拆成两类**，使用者：
> 「圆锥曲线就几何里面，咱们不谈论圆锥曲线几何，就是单纯是那个综合几何，解析几何，对我们
> **可以单开一个解析几何，这个也很重要**，那些就是圆锥曲线是解析几何的一部分嘛，还有什么
> 点和线的那个坐标式啊，这些玩意你也可以放进去，就是比如说用代数方法去求点到线的距离啊。」

**后四类交给我定**，使用者：
> 「导数概率数列，我觉得也可以。就是因为后面这些，我感觉我暂时没没有什么感觉，那就先按
> 你的想法做，我到时候再改。」

**计时要两层**，使用者：
> 「计时，我觉得可以看一下，**就是每道题目用了多久，因为我要这道题目卡了，肯定说明这道
> 题目有点问题，然后再看一下总时长就是我的总熟练度**。」

**不做弱项归纳**，使用者：
> 「咱们没法总结这个弱项嘛，**咱们只能记录错题**，然后就这个项也不用分的太细吧，然后我
> 到时候看着错题，我来总结弱项在哪就行了。」

**错题只存题面，不存生成参数**（我原方案是存参数以便同型重现，被他改了），使用者：
> 「我觉得**错题暂时就只存错题吧**，因为存参数之类的这个后面再想这个可能就比较复杂，
> **可以把它保留成一个方向吧，就是如何智能的精准定制，我可能不会的题，这是一个很好的方向**，
> 但是这个可能我们先搭框架吧。」

**二级结论**，使用者：
> 「可以二级结论先放进去，但是二级结论我还没整理，有哪些就你可以，**反正先随便写一写，
> 到时候我们再改**，也没什么问题。」指数对数运算并入方程与不等式：「放进去可以的。」

**同意先窄后宽**，使用者：
> 「就是你说先做个 model，然后后面再把这个东西填进去，我觉得可以。」

**目录和文件名**，使用者：
> 「你可以重构一下文件结构嘛，比如说把语文的都打包放到一个文件夹里面……**我认为要分开会
> 好一点**，当然我也不了解，这还要参考一下你的意见。」
> 「**可以啊，文件名用英文，我同意啊**，我其实一开始就觉得用英文只是你写成了中文而已，
> 你看我这不是我这个项目名字都是英文。」

**README 要分大类写**，使用者：
> 「read me 就写详细点呗，就一开始上面分三个大类，一语文工作模块，二是数学工作模块，
> 什么什么什么，我觉得就是放到那个外面呗。」

**放行**：「啊，可以，我觉得大概是没什么问题了，**你干吧**。」

**最后反复强调日志**（本轮他强调了三次），使用者：
> 「就还是记得千万记得干完之后写日志。然后日志要写的详细详实。就是把我们做的这个历程写
> 进去，然后你怎么做的也写进去，然后就感觉是我们之后再想复制一个，或者让别人去看着它
> 复制，基本上也是可以做出来，因为目的，实现模式，手法等都在里面。」
> 「**甚至现在说这个成品都没有那么重要了，有了这个日志，我们再换个人都可以把这个东西
> 复刻出来。就是我就这么比喻吧，你的成品是一个成品日志，就是类似于说明书和制造手册。**」

**开工后他又补了一条，限定日志的写法**：
> 「日志可忽略你的思考过程，不要写关于此的部分……**我要求的只是你把你怎么做的和我怎么说
> 的写进去，不包含你怎么想的**……把日志写的详细不等于提取你的思考过程……**我只想把你做了
> 什么写进去。然后把你给我的报告写进去。**」

——照这条，本条日志只记**操作、结论、对话、汇报**，不记推理过程。这一点和 v0.23 定下的
「日志首先记对话」不冲突，是对它的补充限定：**记对话和动作，不记心理活动。**

---

### 二、执行过程（按操作顺序）

#### 1　目录重构成多科目

语文的全部文件用 `git mv` 整体搬进 `chinese/`，新建空的 `math/`：

```
（重构前，全部平铺在根目录）      （重构后）
index.html                       index.html            ← 新建，科目选择壳
data.js  user.js                 chinese/index.html    ← 原来的 index.html，一字未改
tools/  extract/                 chinese/data.js  user.js  tools/  extract/
五本教材 PDF                      chinese/ 五本教材 PDF
merge.py                         chinese/merge.py
data_structure.md 等三份现状文档   chinese/ 三份现状文档 + chinese/README.md
                                 math/index.html  engine.js  engine_structure.md
LOG.md  modify_manual.md         LOG.md  modify_manual.md  README.md  CLAUDE.md（留在根目录）
```

**`merge.py` 不用改**：它用的是 `HERE = os.path.dirname(os.path.abspath(__file__))`，
跟着自己所在目录走，搬进 `chinese/` 后照常工作。实跑确认过。

**文件名用英文的理由**（使用者同意的那条）：将来要上网，中文文件名在 URL 里会被编码成
`%E8%AF%AD%E6%96%87` 这种，难看也容易出错。页面上显示的仍然是中文。

#### 2　科目壳 `index.html`

六个格子：语文、数学可点进去；英语、物理、化学、生物用 `.card.todo` 标成「待建」并置灰
（使用者原话「你要做不出来就留白吧」）。配色、字体、版式直接沿用语文页那套 CSS 变量
（宣纸底 `--paper`、朱砂红 `--cinnabar`），两页看起来是一套东西。窄屏下 `grid` 从两列
掉成一列。

#### 3　数学引擎 `math/engine.js`

按使用者定的「程序生成题目、算答案、写判断器」，**没有任何题库文件**。

**核心约定**（已写进 `math/engine_structure.md`）：每个细类就是一个 `gen()` 函数，
调一次出一道题，返回三个字段：

```js
{ q: "37 × 84 = ?",        // 题面，可含简单 HTML
  a: "3108",               // 答案
  note: "可选的一行提示" }   // 没有就不给
```

页面只认这三个字段，不关心题目怎么造出来。**加一个细类 = 在对应大类的 `subs` 里加一条，
页面代码一行都不用动。**

十个大类全部登记在 `window.MATH.cats`，`subs` 为空的就是待建，页面自动标「待建」并禁用：

| # | 大类 | 本轮 | 说明 |
|---|---|---|---|
| 1 | 数字计算 | **已建，17 个细类** | |
| 2 | 三角计算 | **已建，12 个细类** | |
| 3 | 向量计算 | 待建 | |
| 4 | 立体与平面几何 | 待建 | 按使用者要求，**不含圆锥曲线** |
| 5 | 解析几何 | 待建 | 使用者要求单开；圆锥曲线、点线坐标式、代数法求点到线距离归这儿 |
| 6 | 方程与不等式 | 待建 | 指数对数运算并入此类 |
| 7 | 复数计算 | 待建 | |
| 8 | 导数计算 | 待建 | |
| 9 | 概率统计 | 待建 | |
| 10 | 数列 | 待建 | |

**数字计算 17 个细类**：1000 以内加减法、加减乘除混合、平方数 1–30、两位数×两位数、
多项累加 a×b+c×d(+e×f)、立方数 1–20、2/3/5 的幂、分数↔小数互化、质因数分解、
最大公约数/最小公倍数、阶乘 1–10、分母有理化、根式化简、速算·乘 11、速算·乘 25/乘 5、
速算·十位同个位补十、速算·平方差凑整。

**按使用者要求删掉的两条**：常用无理数近似值（√2、√3、π、e）、常用对数值（lg2、lg3）。
理由是他原话「考试可以用计算器，一般也不会让你写近似值」。**按他要求保留**：分数↔小数
互化（「物理化学里算个三分之几，你也不想敲计算器」）。

**三角计算 12 个细类**：特殊角三角函数值、诱导公式、和差角、二倍角、三倍角、降幂/升幂、
积化和差、和差化积、万能公式、辅助角公式、正弦/余弦定理、反三角函数值。

**这一类里「算」和「背」是混着的**——特殊角求值是算，公式默写是背。引擎不区分，都是
出题→看答案→自评。为此写了个 `formulaGen(list)` 的公用零件：给一组
`[题面, 答案, 提示?]`，随机抽一条，公式类的细类全用它，一行一条公式，往里加很省事。

**二级结论按使用者说的「先随便写一写」散进去了**（不单开大类，因为它是个属性不是学科
分支）：`sin⁴α + cos⁴α = 1 − ½sin²2α`（降幂类）、`A+B+C=π 时 tanA+tanB+tanC =
tanA·tanB·tanC`（正余弦定理类）、辅助角公式的振幅 `√(a²+b²)` 就是最值。将来要专练，
加个「只练二级结论」的筛选即可。

**特殊角用查表不用浮点**：`ANGLES` 表每行是 `[角度, sin, cos, tan]`，值写成 `√3/2`、
`−1/2`、`不存在` 这样的精确符号串。如果现场用 `Math.sin` 算，`sin(π/3)` 会得到
`0.8660254037844386`，那不是答案该有的样子。

#### 4　数学页面 `math/index.html`

**交互按使用者第 4 轮的批复实现**：

- **自评三档**：`算对了` / `算错了` / `没算出来`，键盘 1/2/3，空格看答案和继续。
- **可选填错答**：选「算错了」后卡片下方出现一个输入框和「下一题」，**填不填都能走**
  （使用者原话「这不是必选项，这是可选项，因为有的时候这个真没法输入」）。填了就跟着
  这条错题一起存下来，下次在错题本里能看见「我算成了 xx」。
- **双层计时**：每题一个秒表（出题开始跑、看答案停表，停在那儿给你看这题花了多久），
  全轮一个总计时，结束页给总时长和平均每题。
- **只存错题、不归纳弱项**：`localStorage` 的 `math_wrong_v1`，一条错题存
  `{cat,sub,catName,subName,q,a,note,my,kind,n,t}`。`kind` 区分「算错了」和「没算出来」
  ——这两种复盘时处理不一样，成本几乎为零所以留着。`n` 是错过几次，`my` 是填的错答。
- **「只练错过的」是原题重现**：因为按使用者要求只存题面和答案、不存生成参数，所以重做
  时直接把存下来的那道题原样放出来，**不是重新生成同型题**。页面上会标「重做」。
- 错题本可逐条移出、可清空、可导出成 `math_wrong.json`、可整段贴回来导入（导入时按
  `cat|sub|题面` 去重合并次数）。

**细类浮层**对应语文的「篇目」浮层：当前大类的细类一行一个复选框，能全选/全不选。
切大类时默认全选新大类的细类。

#### 5　文档

按使用者划的边界「**和我们目标冲突的你改，别的大部分你别动**」逐条处理：

- **`modify_manual.md`**：**流程和两个 🛑 停止点一个字没动**（它们和多科目毫无冲突，
  反而更需要）。只改了两处：第二节「不改数据格式」那条补上数学侧的约定（`gen()` 返回的
  `{q,a,note}` 三字段、错题本结构）；第五节「文档分工」按多科目重列了各模块的现状文档。
- **`README.md`**：按使用者要的「一语文工作模块、二是数学工作模块」重组成总入口。
- **`chinese/README.md`**：新建，**v0.24 之前根 README 的语文正文原样搬过来、一字未改**，
  只把指向 `LOG.md`、`modify_manual.md` 的链接补了 `../`。
- **`math/engine_structure.md`**：新建，数学模块的现状文档（和语文的 `html_structure.md`
  对等），记 `gen()` 契约、十个大类的登记表、错题存储结构、怎么加细类。
- **`CLAUDE.md`**：项目定位那句从「综合的语文学习复习项目」改成多科目。

---

### 三、验收（按执行方案里写的逐条跑）

#### 1　生成器：先跑通、再验算对不对

**第一道**：把 29 个细类每个跑 300 次，共 **8700 道题**，检查有没有 `undefined`/`NaN`、
空题面、空答案 → **零异常**。

**第二道（要紧的一道）**：跑通不等于算得对。另写了个独立校验器，**把题面解析出来用
另一种方法重算，再和答案比对**：纯算术类直接 `eval` 题面左边；平方/立方/幂/阶乘按定义
重算；质因数分解把答案乘回去看等不等于原数；gcd/lcm 用辗转相除重算；根式化简检查
`out²×in === k` 且根号里再没有平方因子；分母有理化对题面和答案**求数值**比对（误差
< 1e-9）；三角特殊角和反三角用 `Math.sin/cos/tan/asin/acos/atan` 对着精确符号串验。

共验算 **8500 + 3500 道题**，结果：**全部算对，零误差**。

**过程中报出 170 + 84 条「算错」，查下来全是校验器自己的毛病，不是题目错**：

- 170 条全是 `tan(π/3) = √3` 这类——校验器的答案解析器只认 `√a/b` 形式，不认裸 `√3`。
- 84 条全是 `arctan(−1) = −π/4` 这类——我从未 strip 的原题面取参数，里面的减号是
  Unicode `−`(U+2212) 而不是 ASCII `-`，解析器按 ASCII 判负号，判不出来。

两处都是改校验器、没改题目。**记下来是因为：将来再写这类验算脚本，这两个坑会再踩——
数学符号在项目里一律用 Unicode（`−` `×` `÷` `√` `π`），任何解析脚本都得先归一化。**

#### 2　浏览器实跑（Playwright 无头 Chromium，43 条断言）

**环境上的一个坑**：`/opt/pw-browsers` 里装的是 chromium-1194，而 Python 版 playwright
要 1243，直接 launch 会报「Executable doesn't exist」并叫你跑 `playwright install`。
按环境约定不重装，改成显式指定
`executable_path="/opt/pw-browsers/chromium-1194/chrome-linux/chrome"` 即可。

43 条断言覆盖：

- **科目壳**：6 个格子、2 个可点、4 个待建、零控制台报错。
- **语文页（重点：搬家后不能坏）**：读到 `data.js`、解析出 **66 篇**、六个模式按钮
  逐个点开都渲染出内容、零控制台报错。
- **数学页**：十个大类都列出来、2 个已建、一进来就出题、秒表在走（实测 0.5 秒 → 1.0 秒）、
  空格看答案、三档自评都在、选「算错了」出输入框、填的错答存进去了、错题存的是原题面、
  「没算出来」也进错题且 `kind` 分开标、「算对了」不进错题、错题本列出 2 条并显示我填的
  错答、「只练错过的」重现的就是存下来的原题、结束页给了总时长和平均每题、数字计算
  17 个细类、三角计算 12 个细类、一个细类都不选时给提示而不崩、待建大类是禁用的、
  零控制台报错。
- **390px 窄屏**（使用者主力是手机）：三个页面 `scrollWidth` 都是 390，不横向溢出。

**结果：43 条全过。**

#### 3　抓到一个真 bug（本轮唯一的代码 bug）

`buildQueue()` 在「一个细类都没选」时写的是直接 `return`，**没调 `render()`**——
页面会停在上一题不动，那句「一个细类都没选，点上面的『细类』挑几个」的提示根本没机会
显示出来。改成 `if(!subs.length){ render(); return; }`。

这个 bug 靠人点是能发现的，但很容易被当成「没反应」放过去；是验收脚本那条断言
「一个都不选时给出提示而不是崩」把它逼出来的。

#### 4　语文那边零改动

- `git diff --cached -M --name-status -- chinese/data.js chinese/index.html` → **空**，
  确认是纯改名、内容一个字没动。
- `python3 chinese/merge.py` → 正常生成单文件版 **902.5 KB**，和搬家前一致。

---

### 四、本轮的数（一眼看完）

| | 数 |
|---|---|
| 大类 | 10 个登记在案，**2 个已建**，8 个待建 |
| 细类 | **29 个**（数字计算 17、三角计算 12） |
| 生成器压测 | 8700 + 11600 道题，零异常 |
| 答案独立验算 | 12000 道题，**零误差** |
| 浏览器断言 | **43 条全过** |
| 语文语料改动 | **0 字**（纯改名） |
| 代码 bug | 抓到并修掉 1 个 |

---

### 五、遗留与下一步

**使用者明确留作方向、这轮没做的：**

- **错题存生成参数**。他原话：「存参数之类的这个后面再想这个可能就比较复杂，可以把它
  保留成一个方向吧，**就是如何智能的精准定制，我可能不会的题，这是一个很好的方向**。」
  现在「只练错过的」是原题重现；存了参数才能做到「同型不同数地再考你一遍」。
- **PWA 部署**。顺序是他定的：壳 → 数学口算 → 验证成功 → 上 PWA。我报过的结论记在这儿
  免得下次重查：**代码自动更新、数据仍需手动导出导入**；多用户备份和两端自动互通要
  后端+账号，属于再往后的第 4 步。

**这轮按「先窄后宽」有意没做的：**

- 其余八个大类（向量、立体与平面几何、解析几何、方程与不等式、复数、导数、概率统计、
  数列）全部待建。**交互这一套已经验证过了，填这八类是纯填空**——在对应大类的 `subs`
  里加 `{id, name, gen}` 即可，页面代码不用动。
- 二级结论现在只是「随便写一写」的几条，使用者说他还没整理，整理完再改。
- 英语、物理、化学、生物四科只有格子，没有内容。

**已知的小事：**

- 数学模块还没有单文件版（语文那个 `merge.py` 是语文专用的）。数学只有两个文件
  （`index.html` + `engine.js`），手机上读不到同目录文件时会失效，将来要么照写一个
  合并脚本，要么等 PWA 上线后直接用网址，就不存在这个问题了。
- 美观性按使用者定的「中间版本以维护性为上」没有专门打磨。


---

## v0.26　日志补缺与加索引；PWA：装到手机、离线、自动更新

**2026-09-21**（本条所附提交）

---

### 一、对话实录

**使用者**：
> 「接下来做两件事情，1 因为你干的是你做的事情，其实**对话窗口中断过几次**，我每次想要
> 的是：**什么被干了，包含了你干的和别的窗口干的**，你在整理优化一下日志，2 你不是说有
> 那个 PWA？**能不能搞起来，这样我就不用每次下载一个副本而查看效果**，你说呢」

**我的汇报（查完之后）**：翻了 git 全历史，报了两件事——

1. **日志确实有缺口**：v0.23 合并（`9e6974e`）之后有 **4 个提交从没进过日志**，而且
   commit message 是英文默认文案。查作者是 **`wabulae1225`，也就是使用者本人**，
   09-20 晚 22:47–22:56 改的 `modify_manual.md` 和 `CLAUDE.md`，看提交形态像是在
   GitHub 网页上直接编辑的。
2. **「别的窗口」其实是两回事**：`5715eb5`（v0.24 目录重构）这个提交**带的是和我同一个
   session id**，是本会话在被打断的那几轮里已经跑完的工具调用，只是回复没显示出来。
   所以它不是别的窗口干的，是同一个会话的断点。

**关于 PWA 我报的前提问题**：查 GitHub 接口，这个仓库 **`private: true`（私有）**、
`has_pages: false`（Pages 没开）。而**免费账号的 GitHub Pages 只对公开仓库开放**，
所以「上线」这一步卡在使用者手里，不是我能替他按的开关。PWA 本身我全部做好并验收了。

---

### 二、第一件事：日志补缺与加索引

#### 做了什么

**1. 在 `LOG.md` 开头加了「版本速查表」**，直接回答使用者那句「我每次想要的是：什么被
干了」。一行一个版本，四列：日期、**谁干的**、干了什么、commits。

**「谁」这一列是这次新加的重点**，因为这个项目实际有三方在改：

| 谁 | 特征 | 例子 |
|---|---|---|
| 前一位作者 | v0.1 之前的工作 | 单文件原型、四种题型 |
| AI | commit message 带 `[vX.XX]`，中文 | 绝大多数 |
| **使用者本人** | **message 是英文默认文案**（`Update xxx.md`） | `a4347ef`、`9b0ae6a`…`7151edf` |

顺带还捞出一条以前也没记的：**`a4347ef`（09-19）是使用者自己动手在单文件版里加了难字**。

**2. 给使用者那 4 个提交编号为 `v0.23.1` 并补记**（见下一节）。

#### `v0.23.1`：使用者本人改了手册和 CLAUDE.md（补记）

`9b0ae6a` `08439a3` `a562a56` `7151edf`，2026-09-20 22:47–22:56，作者 `wabulae1225`。
**这四个提交改的是约束 AI 怎么干活的文件，却一直没进日志，等于规矩改了没人知道。**
逐条记下来：

**（1）手册第一节，加了「第 0 步：核查环境」**——这是新增的一整步，原文：

> 「0. **核查环境**。读相关文档……跑只读命令了解现状，看看内容是否自洽，**是否有上一轮
> 尚未合并的 issue 或者 PR，是否拷贝到最新版的文件**，是否一切准备就绪开始工作。」

同时把原来的第 2 步「复述 + 提建议」和第 3 步「问清歧义」**并成了一步**。

> **一个遗留的小问题**：并完之后编号成了 0、1、2、**4**、5、6、7、8、9——**第 3 步空了**，
> 是合并时留下的编号断层。两个 🛑 停止点仍在第 4、6 步，不影响执行。
> **没有自作主张改**，因为这是使用者本人写的文件，按他后来定的边界「别改太离谱」，
> 这种要他自己定。**摆在这儿等他定夺。**

**（2）手册第九步和说明里加了「日志要详细」**：

> 「9. 记日志。在 `LOG.md` 末尾追加本轮记录（见第四节）**日志要详细**。」
> 「**日志必须详细些，日志的目的是让我们了解项目的细节，方便以后改善，因此必须认真
> 对待，是很重要的步骤！**」

**（3）手册第四节「记日志的规矩」整节改写成他自己的话**。原来是 AI 整理的条目式，
他改成了自己的口径，新增的要求有：

> 「日志 LOG.md 应当要求就是**尽可能详细，多写原始，底层的细节，然后总结为高层的功能
> 修改**，至少要像对我汇报一样写到日志中，包含我们对话的大部分内容，**包括我给你的建议
> 和你每一步调用工具的过程和你最后给我的报告**……」
> 「之后，再按照你操作的顺序将过程写，尤其是你最后给我的总结汇报，格式形如：
> 1 使用了工具调取字典原文 2 分离原文 总结：本次操作下载和分离了原文，有哪些技术细节」
> 「**日志请记录时间戳**，commit message 带版本号，代码改动要和日志条目对得上。」

**（4）`CLAUDE.md` 从索引式改成他自己的三行交代**：

> 「这是一个综合的语文学习复习项目，我们将要一起完成它，先看 README.md……
> **然后进行改动和工作之前前必须先读 modify_manual.md** 了解工作流程。
> 你的所有项目历史改动都详细的记录在 LOG.md 当中，你可以参考，**希望你也能详细的维护他**」

（这一句后来在 v0.24 被改成多科目口径，「语文学习复习项目」→「学科复习项目」。）

**这四条合起来看是一件事：使用者在用自己的话，把「日志要多详细」这条规矩钉死。**
后面 v0.25、v0.26 两轮他又各强调了一次。

---

### 三、第二件事：PWA

#### 先回答使用者的痛点

他要的是「**不用每次下载一个副本而查看效果**」。这件事分两层，我做的是第二层，
第一层的开关在他手里：

- **有网址** → 打开就是最新的，不用下载。**这一层需要部署，需要他做决定**（见下）。
- **装成 App + 离线 + 自动更新** → **这一层我做完了**，且已验收。

#### 做了什么（按操作顺序）

**1. 生成图标。** 环境里没有 PIL，装库又违反手册「不引入新依赖」。改用**现成的
Chromium 截图**来生成：写一段 HTML（朱砂红底 + 白色「习」字，字体用系统里的
文泉驿正黑），用 Playwright 以不同 `device_scale_factor` 截出 192 和 512 两档 PNG。
另外单独截一张 **maskable** 版（字往里收到 52% 安全区），因为安卓会把图标裁成圆形，
不收的话「习」字边缘会被切掉。

**2. `manifest.webmanifest`。** 关键一点：**所有路径都用相对的**（`start_url: "./"`），
这样部署到 GitHub Pages 那种带子路径的地方（`/Guwen_recite/`）才不会错位。

**3. `sw.js`（Service Worker）。** 两种缓存策略分开用：

- **导航请求（点开一个页面）→ 网络优先**，拿到就顺手更新缓存，断网再退回缓存。
  这样**在线时总能看到最新的页面**，正对使用者「不用下载副本」的诉求。
- **其余静态资源 → 缓存优先**（秒开），同时后台拉新的存起来，下次就是新的。

装机时预缓存 13 项（两个页面、语料 `data.js`、引擎 `engine.js`、图标等），逐个 `add`
而不是一次 `addAll`，**某一个 404 不至于让整个装机失败**。

**特意没有用 `skipWaiting()` 自动切版本**：正做着题的时候页面突然换版是很讨厌的。
改成新版装好后**弹一条「有新版本了」+ 一个「更新」按钮**，点了才切。

**4. `pwa.js`。** 注册 SW、弹更新提示、以及安卓上主动弹「装到桌面」。
**开头两行判断很要紧**：不是 https 也不是 localhost 就直接 return——这样
**双击本地文件（`file://`）打开时静默跳过，页面照常工作**，离线单文件的用法一点不受影响。

**5. 接到页面上。** 根目录 `index.html` 和 `math/index.html` 各加了 manifest、
theme-color、图标、`pwa.js` 四行。

**6. `.github/workflows/pages.yml`。** 上线配置备好，但**只发布程序和语料**：
根目录 PWA 四件套 + 图标 + `chinese/{index.html,data.js,user.js}` + `math/`，
**不发布五本教材 PDF，也不发布 `tools/` 和 `extract/`**。另外写了 `robots.txt`
不让搜索引擎收录——这是个人复习用的东西。实测产物 **1.0 MB、零个 PDF**。

#### 验收（17 条，全过）

起本地 http 服务用无头 Chromium 实跑：

- **清单**：`start_url` 是相对路径、声明了 standalone、带 maskable 图标、三个图标都在
- **注册**：Service Worker 激活，**作用域是根目录**（所以语文、数学两个子页都归它管）
- **预缓存**：13 项**全部命中**，`index.html`、`chinese/data.js`、`chinese/index.html`、
  `math/engine.js`、`math/index.html` 逐个验过
- **真·离线**（把浏览器切到断网再打开）：首页正常、**数学页能出题**、
  **语文页 66 篇语料读得到**
- **`file://` 打开**：静默跳过 SW、**零报错**、页面照常工作（6 个科目格子都在）
- **子路径部署**（模拟 `https://xxx.github.io/Guwen_recite/`）：SW 作用域正确、
  断网下语文 66 篇、数学能出题、**零控制台报错**

另外回跑了 v0.25 那套 **43 条断言，仍然全过**；`chinese/merge.py` 仍能生成单文件版。

#### 过程中踩的一个坑：`/favicon.ico` 404

验收卡在「全程无控制台报错」这一条，报的是 `404 (File not found)`。**查了四轮才定位**，
因为它不在页面的请求列表里——**它是 Service Worker 自己发的请求，页面级的监听抓不到**。
最后是打印控制台消息的 `location` 才看出来是 `/favicon.ico`。

根因：**语文页没有图标引用**（那一轮我有意没动 `chinese/index.html`），浏览器就去
**站点根目录**要 `/favicon.ico`。

- 第一次修：在仓库根目录放了个真的 `favicon.ico`（用 Python `struct` 手写 ICO 头，
  把 192 的 PNG 包进去）。**部署在域名根目录时有效**。
- **但子路径部署下还是 404**——浏览器要的是 `站点根/favicon.ico`，不是
  `/Guwen_recite/favicon.ico`，根目录那个够不着。
- 最终修法：**给 `chinese/index.html` 内嵌一个 data URI 图标**（48px PNG 转 base64，
  1039 个字符）。**不产生任何外部请求**，所以子路径部署、本地 `file://`、
  以及 `merge.py` 生成的单文件版，三种情形全都带得走。

**这是本轮唯一一次动 `chinese/index.html`**（v0.24/v0.25 都是零改动）。加的是一行
`<link rel="icon" href="data:image/png;base64,...">`，不碰任何逻辑；`merge.py` 重跑
正常，单文件版从 902.5 KB 变成 903.6 KB。

---

### 四、上线：卡在一个使用者才能按的开关

查 GitHub 接口的实际结果：**仓库 `private: true`、`has_pages: false`**。
**免费账号的 GitHub Pages 只对公开仓库开放。** 所以三选一，已写进 `README.md`：

1. **把仓库设为公开** → Settings → Pages → Source 选「GitHub Actions」，
   `pages.yml` 自动发布到 `https://wabulae1225.github.io/Guwen_recite/`
2. **保持私有，换个地方托管** → Cloudflare Pages / Netlify / Vercel 都免费、
   都支持从私有仓库自动部署
3. **先不上线** → PWA 已备好，哪天想上随时能上

**公开之前有一件事要他定**：`chinese/data.js` 里是人教版课本的正文和注释，
**一字不改**地抄下来的；仓库里还放着五本教材 PDF。所以部署配置**只发程序不发 PDF**、
并带 `robots.txt` 不让收录。**要不要公开，由使用者定，我不替他定。**

---

### 五、本轮的数

| | 数 |
|---|---|
| 日志补缺 | 补记 **4 个**从未入账的提交（使用者本人改的），新增版本速查表 **19 行** |
| PWA 新增文件 | `manifest.webmanifest`、`sw.js`、`pwa.js`、`favicon.ico`、3 个图标、1 份部署配置 |
| PWA 验收 | **17 条全过**（含真·离线、子路径部署、`file://` 回退） |
| 回归验收 | v0.25 的 **43 条仍全过**；单文件版正常 |
| 踩坑 | `/favicon.ico` 404 一个，修了两次才彻底解决 |
| 语文模块改动 | **1 行**（内嵌图标），不碰逻辑 |

### 六、遗留

- **上线没做**，卡在仓库公开与否，开关在使用者手里（见第四节）。
- **手册第一节的编号断层**（缺第 3 步），使用者本人的文件，等他定夺。
- iOS 上装 PWA 要手动「共享 → 添加到主屏幕」，安卓会自动弹「装上」的提示——
  这是系统差异，不是代码能抹平的。
- **`sw.js` 顶上的 `VERSION` 改完代码要加一**，否则浏览器可能还在用旧缓存。
  已写进 `README.md` 和 `sw.js` 的注释里。


---

## v0.27　总体验收、文件布局分层、README 逐条核对、上线方案摆两条路

**2026-09-21**（本条所附提交）

---

### 一、对话实录

**使用者**：
> 「**总体验收一遍**，日志写一下，我们就进入下一次，**要不我把仓库公开吧**，你可以
> **综合优化一下文件布局和结构**，**综合查阅一遍 readme**，**综合调整一下日志
> （我做的和不是你做的也写进去哈，这是工程的变化日志）**，核对完了叫我去干吧」

**我的汇报**：五套验收全过、布局分层做完、README 逐条核对无误。
**但在他动手公开之前，我把一件不可逆的事摆了出来**（见第五节）：
仓库里五本教材 PDF **在 git 历史里**，公开仓库等于公开全部历史，
**现在删掉也没用**；而要抹掉就得改写历史，**改写会把所有 commit 号换掉，
`LOG.md` 里大量引用的 commit 号会全部失效**——日志恰恰是他最看重的东西。
所以建议走另一条路：**仓库保持私有，用 Cloudflare Pages 部署**，同样拿到网址，
什么都不用公开。

---

### 二、文件布局：把「源材料」和「生成物」从源码里分出来

**改之前** `chinese/` 底下 15 项混在一起：程序、语料、构建脚本、构建产物、
五本 PDF 源材料、工具链、中间产物、四份文档，平铺一层。

**改之后**分出两个目录：

```
chinese/
  index.html  data.js  user.js        程序和语料
  merge.py                            构建脚本
  dist/       古诗文默写_单文件.html    ← 新：生成物
  textbooks/  五本教科书 PDF            ← 新：源材料（31 MB）
  tools/      extract/                 工具链和中间产物
  README.md  三份现状文档
```

**跟着改的三处代码/配置**（不改就断）：

1. **`tools/pdfdump.py`**：新增 `BOOKDIR = os.path.join(HERE, "textbooks")`，
   两处 `pymupdf.open(os.path.join(HERE, …))` 改成 `BOOKDIR`。
2. **`merge.py`**：产物写到 `dist/`（目录不存在就建），输出提示也改成 `dist/…`。
3. **`.github/workflows/pages.yml`**：排除清单里补上 `textbooks/` 和 `dist/`。

**验证方式**：
- `merge.py` 实跑 → `已生成 dist/古诗文默写_单文件.html（903.6 KB）`，正确。
- `pdfdump.py` **跑不了**——这个容器里没装 `pymupdf`（环境限制，不是改坏的）。
  改用**桩件验证路径解析**：临时把 `pymupdf` 换成一个假模块，`open()` 只记下
  被请求的路径就抛出退出，以此确认脚本去哪儿找 PDF。结果：
  `/home/user/Guwen_recite/chinese/textbooks/普通高中教科书·语文必修 上册.pdf`，
  **文件存在 → 路径正确**。

**文档里提到旧位置的六处也一并跟上**：`chinese/README.md`（两处）、
`chinese/data_structure.md`（两处）、根 `README.md`（两处，目录树里补了
`dist/` 和 `textbooks/` 两行）。

---

### 三、总体验收：五套全过

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 生成器语法 + 压测 | **14500 道题，异常 0 条**；10 大类 / 已建 2 / 29 细类 |
| 2 | 答案独立验算（题面解析后重算再比对） | **10600 道题，零误差** |
| 3 | 页面功能断言 | **43 条全过** |
| 4 | PWA | **17 条全过**（含真·断网、`file://` 回退） |
| 5 | 子路径部署（模拟 `github.io/Guwen_recite/`） | SW 作用域正确、断网下语文 66 篇、数学能出题、**零控制台报错** |

外加 `merge.py` 在新布局下实跑正常。

---

### 四、README 综合核对：逐条查，没有查出错

**1. 文档内部链接**：写脚本把九份 Markdown 里的相对链接全部解析、逐个查文件是否存在。
**28 个链接，全部指得到，零死链。**

**2. 提到的代码文件**：12 个（`index.html`、PWA 四件套、语文三件、`merge.py`、
数学两件、工作流）**全部存在**。

**3. README 声称的数字 vs `data.js` 实际**——这一项是重点，写脚本直接读语料数：

| 声称 | 实际 | |
|---|---|---|
| 66 篇古诗文 | **66** | ✓ |
| 3031 条注释 | **3031** | ✓ |
| 1320 个词语 | **1320** | ✓ |
| 579 个难字 | **579** | ✓ |
| 理解性默写「待补」 | **空** | ✓ 如实 |
| 译文解析「待补」 | **空** | ✓ 如实 |

**4. 数学细类数**：README 写「数字计算 17、三角计算 12」，实际就是 17 和 12。✓

> **踩了个小坑**：第一次核数字时用 `node -e` 内联正则去抠 `data.js` 的六个板块，
> 四项全返回 0。**不是数据没了，是命令行里反斜杠转义被吃掉**，正则没匹配上。
> 改成写进脚本文件、直接 `require("./chinese/data.js")` 读 `window.DATA`，数就对了。
> **教训：核数据的脚本别写在 `node -e` 的命令行里，正则会被 shell 咬。**

---

### 五、上线：摆出两条路，把不可逆的部分说清楚

使用者说「要不我把仓库公开吧」。查 GitHub 接口：**仓库 `private: true`**，
**免费账号的 GitHub Pages 只对公开仓库开放**，所以要用 GitHub Pages 就必须公开。

**但公开这件事有两处不可逆，必须在他动手之前讲：**

**（一）五本教材 PDF 在 git 历史里。** 现在它们在 `chinese/textbooks/`（31 MB），
但从 09-18 的最初几个提交起就一直在库里。**公开仓库 = 公开全部历史**，
所以**即使现在删掉再公开，历史里那几份照样能被下载**。

**（二）要真抹掉就得改写历史，而改写会废掉日志里的 commit 引用。**
`git filter-repo` 能把 PDF 从历史里剔除，代价是**所有 commit 号全部改变**。
而 `LOG.md` 里**大量引用 commit 号**——版本速查表整张表都是——改写之后**全失效**。
日志恰恰是使用者反复强调「比成品还重要」的东西。

另外 `chinese/data.js` 是人教版课本正文和注释**一字不改**抄下来的。
发布配置已经做到「只发程序和语料、不发 PDF」，但那管的是**发布产物**，
**管不着仓库本身公开后的历史**。

**所以给了两条路，写进 `README.md`：**

- **路 A（我建议的）**：**仓库保持私有**，用 **Cloudflare Pages** 部署。免费、
  支持私有仓库自动部署、带 https。**什么都不公开**，照样拿到网址。
- **路 B**：设为公开 + GitHub Pages。简单，但要接受上面两条。

**决定权在使用者。** 我没有替他做，也没有动任何历史。

---

### 六、本轮的数

| | |
|---|---|
| 验收 | **五套全过**（14500 压测 / 10600 验算 / 43 条页面 / 17 条 PWA / 子路径部署） |
| 布局 | `chinese/` 从 15 项平铺 → 分出 `dist/`（生成物）和 `textbooks/`（源材料） |
| 跟着改的代码 | `pdfdump.py`、`merge.py`、`pages.yml` 各一处；文档六处 |
| README 核对 | 28 个链接零死链、12 个文件全在、**6 项数字全部对得上** |
| 踩坑 | `node -e` 里的正则被 shell 吃掉反斜杠，误报「数据为 0」 |
| 待使用者决定 | **仓库公不公开**（两条路已写进 README，含不可逆警示） |

### 七、遗留

- **上线仍未做**，等使用者在路 A / 路 B 之间选。
- **手册第一节编号断层**（缺第 3 步）仍在，使用者本人的文件，等他定夺。
- `pdfdump.py` 在本容器里跑不了（无 `pymupdf`），本轮只用桩件验证了路径，
  **完整的 PDF → data.js 流水线没有端到端重跑过**。要重跑得在装了
  pymupdf / pypinyin 的机器上。
- 其余八个数学大类、英语物理化学生物四科，仍然待建。


---

## v0.28　手册编号补上第 3 步；上线定为「公开 + GitHub Pages」

**2026-09-21**（本条所附提交）

---

### 一、对话实录

**我上一轮的汇报**（摘要）：五套验收全过、布局分层完成、README 逐条核对无误；
另外报了两件要他定的事——手册第一节编号断层（缺第 3 步），以及公开仓库的两处不可逆。

**使用者**：
> 「**改，第三步改就行了**，**我直接公开不就完了。。为啥要删这个删那个**」

**我的回应**：**先澄清了一个误会**——我从来没要求删任何东西。「删 PDF / 改写历史」
是**「如果他不想让教材 PDF 公开」才需要的动作**；他不介意的话，就没有什么要删的，
**直接公开最省事**。按手册第二节「明确」档，**使用者已经重申了决定，照做，不再劝**。

---

### 二、做了什么

#### 1　手册编号补上第 3 步

`modify_manual.md` 第一节原来是 `0、1、2、4、5、6、7、8、9`——**第 3 步是空的**，
是使用者上一轮把原第 2、3 步合并时留下的断层（见 v0.23.1）。这轮按他「改，第三步改就行了」
顺次补齐，并**把所有交叉引用一起改掉**（这是关键，只改列表不改引用会更乱）：

| | 改前 | 改后 |
|---|---|---|
| 对齐确认 🛑 | 第 4 步 | **第 3 步** |
| 给执行方案 | 第 5 步 | **第 4 步** |
| 方案确认 🛑 | 第 6 步 | **第 5 步** |
| 执行 / 验收 / 记日志 | 7 / 8 / 9 | **6 / 7 / 8** |
| 正文「两个停止点（🛑 第 4、6 步）」 | 第 4、6 步 | **第 3、5 步** |
| 第七步正文「按第 5 步写好的验收方式」 | 第 5 步 | **第 4 步** |
| 第三节标题「验收方式（第 5 步必写）」 | 第 5 步 | **第 4 步** |

**两个 🛑 停止点现在是第 3、5 步。** 手册的其余内容一个字没动。

#### 2　上线方案定稿：公开 + GitHub Pages

`README.md`「上线」一节从「两条路，先看清代价」改写成**「使用者已定」**，给出四步操作：
设为 Public → 合进 `main` → Settings → Pages 选「GitHub Actions」→ 等 Actions 跑完。
`.github/workflows/pages.yml` 顶部注释也跟着改（不再说「仓库现在是私有的」）。

**公开意味着什么，仍如实记在 README 里一段引文中**，但措辞从「警示」改成「记录」：
公开仓库等于公开全部 git 历史，所以 `chinese/textbooks/` 的五本教材 PDF 和一字不改的
`chinese/data.js` 在仓库里可被公开访问；发布配置只管网页产物，管不着仓库本身。
另外两条路（改写历史 / 私有 + Cloudflare）也留在那儿作记录。
**当前选择是直接公开——使用者知情并决定照做。**

---

### 三、本轮的数

| | |
|---|---|
| 手册 | 编号断层补齐，**连带改掉 4 处交叉引用**；停止点改为第 3、5 步 |
| README | 「上线」一节改写成已定方案 + 四步操作 |
| 工作流 | 顶部注释跟上（不再说仓库是私有的） |
| 代码 | **零改动**（本轮只动文档） |

### 四、开 PR

**使用者**：
> 「好，把本轮改动写入日志，**然后开 pr**」

照办。开 PR 前先查了两件事：

1. **有没有 PR 模板** —— `.github/` 下只有 `workflows/`，四个常见位置都没有模板，
   所以按常规写法组织正文。
2. **`main` 停在哪** —— `git fetch origin main` 之后发现 **`main` 已经合过 PR #5**
   （v0.24/v0.25 那批），所以本次 PR 实际只含 **5 个提交：v0.26 → v0.28**。
   （fetch 之前算出来是 51 个，那是本地的 `origin/main` 引用过期了，
   **这种时候先 fetch 再数，否则 PR 描述会写错范围。**）

**PR #6**：<https://github.com/wabulae1225/Guwen_recite/pull/6>
标题 `[v0.26–v0.28] PWA（装到手机、离线、自动更新）；布局分层；日志补缺；手册编号修正`。
正文写了四块改动、一张验收结果表、`chinese/index.html` 只加一行的原因说明、
以及**合并后上线的四步**；公开仓库的含义也如实写在正文末尾作记录。

变动汇总：**24 个文件、+864 / −19 行**，其中 `LOG.md` 占 480 行。
**语文语料 `data.js` 和数学引擎 `engine.js` 都没动。**

---

### 五、遗留

- **使用者尚未执行上线四步**（设为 Public → 合 PR #6 → 选 Actions → 等发布）。
  `pages.yml` 监听的是 `main` 的推送，**PR 不合进 main 就不会触发**。
- 其余八个数学大类、英语物理化学生物四科，仍然待建。
- `pdfdump.py` 完整流水线在本容器里跑不了（无 `pymupdf`），见 v0.27 遗留。

