# 数据结构：语料是怎么储存的

> **这份文档记「现状」**——现在数据长什么样、为什么这么设计。规则变了直接改这份，
> 不叠加历史。想知道某个格式是哪一轮、为什么这么演变过来的，去 `LOG.md` 查。

页面（`index.html`）只认一个文件：`data.js`。它是纯文本，由 `tools/` 从课本 PDF
生成。下面从「最终产物」讲到「怎么生成的」，再讲「用户数据」。

---

## 一、最终产物：`data.js`

一个挂在 `window.DATA` 上的对象，六个板块，每个板块是一段用反引号包起来的多行字符串。
**除了在正文里加减大括号圈难字，其余内容别手改**——改了下次重跑 `build.py` 会丢。

```js
window.DATA = {
  corpus:        `...`,   // 正文
  hardChars:     `...`,   // 难字表
  notes:         `...`,   // 注释
  words:         `...`,   // 词语
  comprehension: `...`,   // 理解性默写（待补，现在是空的）
  gloss:         `...`,   // 译文与解析（待补，现在是空的）
};
```

分隔符统一是**半角竖线 `|`**，字段前后的空格会被自动去掉。

### 1. `corpus`　正文

一篇的结构：

```
# 篇名 | 作者 | 诗 | 背
正文第一行
正文第二行

空行之后是新的一段
```

- **篇头**以 `# ` 起头，四个字段：
  - 篇名、作者
  - 第三项：`诗` 或 `文`。决定切题方式（诗一行一题，文按句号切）。
  - 第四项 **背诵范围**，照课本「学习提示」填：
    - `背`　全篇要背
    - `背1`　只背第 1 段（课本写「背诵《师说》的第1段」，`背3` 同理）
    - `不背`　不要求背，只进「看原文」和「注释」，不出默写题
    - 不写第四项当成 `背`（旧的三项写法照样兼容）
- **`// 册 必修上`** 这样的行是分组标记，篇目面板按它分五组。以 `//` 开头的行一律忽略。
- **空行分段**。看答案时显示的就是这一段。
- 段内切题：`诗` 每行一题；`文` 按句号问号叹号切，**不足十个 CJK 字的并进下一题**
  （阈值 `index.html` 的 `MIN_CHARS = 10`）。
- **逗号、分号**切出挖空的最小单位；**顿号不切**。
- **难写字用大括号圈**：`艰难苦恨繁霜鬓，{潦}倒新停浊酒杯。` 圈过的字自动进难字表，
  **一篇里圈一处，这篇里这个字就都算难字**。这是使用者手工维护的唯一入口
  （「像在纸书上圈错字一样」）。

### 2. `hardChars`　难字表

空格分隔的一串字，两类混在一起：

- **难读的**——课本注过音的，写成 `衿(jīn)` `匝(zā)`，括号里的音在看答案时给出。
- **难写的**——字形生僻的，单写一个字。判据是《通用规范汉字表》一级字表：
  正文里不在那 3500 常用字内的算难写。

页面最终用的难字集 = 这张表 ∪ 正文里手圈的字。

### 3. `notes`　注释

一行一条，五段：

```
来源 | 字词 | 释义 | 例句 | 出处
```

- **来源**三种：`课本`（视为准确，正体显示）、`补充`（学校补充，楷体+标签）、
  `实词`（实词手册，楷体+标签）。
- **例句**里把被考的词用大括号圈起来，页面据此加角标。
- **出处**在库里有对应篇名就跟着篇目筛选走；不在库里（实词手册的外篇例句）则任何时候都出。
- 课本长注释里套着的小注（`〔青青子衿…〕…衿，衣服的交领。`）入库时主条目和小注**各占一条**，
  这样单字也考得到；「看原文」里只显示主条目，跟课本一样。

### 4. `words`　词语

一行一条，八段：

```
词 | 拼音 | 题型 | 词源 | 释义 | 释义来源 | 例句 | 例句出处
穹庐 | qióng lú | 写 | 词典 | 古代游牧民族居住的毡帐；泛指北方少数民族 | 新华 | 三岁余，王病，赐武马畜、服匿、{穹庐}。 | 苏武传
```

- **拼音按字分开写**（`qióng lú`）。看词写拼音时，字和音一个一个对得上才好核对。
- **题型**：`写` 看拼音写词语（字生僻）、`读` 看词写拼音（有字变了读音）、`都考` 两种都出。
- **词源**（这个词打哪儿收进来的）：`课本`（课本自己注的音，最权威）、`成语`、`词典`。
- **释义来源**（释义打哪儿抄的）：`课本`（课本注释）、`成语`、`新华`、`自拟`，
  可靠性依次递减，**页面照实标出来**。四级回落，所以不存在没释义的词：
  | 释义来源 | 条数 | 哪来的 |
  |---|---|---|
  | `课本` | 294 | 课本注释，连长注释里套的小注一起收 |
  | `成语` | 199 | `tools/data/成语释义.json` |
  | `新华` | 607 | `tools/data/新华词语.json` |
  | `自拟` | 220 | 三本都查不着，人工写在 `tools/释义-自拟.tsv` |
- **例句**把被考的词用大括号圈起来，和 `notes` 板块一个写法。四档优先：
  **课本古诗文原句**（532）>**成语的古典书证**>**其余课文**>**自拟**（232），
  合计 **1320 条，每个词都有**。自拟的出处记 `自拟`，页面上写「自拟例句」，
  不套书名号——不是课本原文，得说清楚。
- 自拟例句写在 `tools/释义-自拟.tsv` 第三列，跑 `words.py` 时过**三道自检**
  （`check_hand()`）：词要用大括号圈起来；**圈起来的那处之外，不许再出现
  这个词里的任何一个字**（页面遮几个字要看难字表，写的时候算不清，索性按最严的来，
  遮几个都漏不了答案）；长短 8–46 字，和自动抓的一个尺度。
- **例句出处**：篇名（页面加书名号），成语书证是《答新渝侯和诗书》这样的书名，
  册名（`必修上` 一类）是给栏目标题兜底的，页面写成「课本 必修上」不加书名号。
- 由 `tools/words.py` 生成，词都在这五本课本里出现过。

### 5. `comprehension`　理解性默写（待补）

一行一题，三段：`篇名 | 提示语 | 答案`。篇名要和 `corpus` 里一字不差。
**课本里没有现成内容，现在是空的。**

### 6. `gloss`　译文与解析（待补）

答错时才显示。一行一条，三段：`出处 | 原句 | 译文与解析`。原句要和切出的那一题一字不差；
第三段想分行用两个斜杠 `//` 隔开。**课本里没有现成内容，现在是空的。**

---

## 二、`data.js` 是怎么生成的

三步管道，产物落在 `extract/` 下：

```
五册 PDF
  │ tools/pdfdump.py    按字体切版面，一页一份 JSON
  ↓
extract/raw/*.json      正文/注释/学习提示/篇名/插图说明各归各位
  │ tools/assemble.py   按篇合并，跨页角标重编号，解析背诵要求
  ↓
extract/篇/*.md         一篇一份中间稿，人工校对看这个
  │ tools/words.py      顺带抽词语 → extract/词语.tsv
  │ tools/build.py      生成 data.js 六板块
  ↓
data.js
```

PDF 解析那一步（`pdfdump.py`）踩过的所有坑，单独记在 **`extract_pdf_method.md`**。

### 中间产物

- **`extract/raw/<册>.json`**　每页一条记录，字段有 `body`（正文）、`notes`（注释）、
  `hints`（学习提示）、`captions`（插图说明）、`titles`（篇名）等，每行带 `x0/x1/y0`
  坐标和 `col`（栏号）。正文和注释里的注释角标写成 `⟦n⟧`。
- **`extract/篇/<册>-p<页>-<篇名>.md`**　可读的中间稿：正文（带 `⟦n⟧` 角标）、注释、
  学习提示、背诵判定、体检结果。**人工校对就看这个。**
- **`extract/词语.tsv`**　词语板块的来源，`words.py` 生成，字段比 `data.js` 多两列
  （出处、课本里出现次数），供排序和核查。

### 人工维护的几张表（在 `tools/` 下）

| 文件 | 谁写 | 管什么 |
|---|---|---|
| `pieces.tsv` | 人工圈定 | 哪 66 篇是古诗文：册、起始页、篇名、作者、体裁 |
| `glyphs.tsv` | 人工认字 | PDF 文本层里不存在的十来个字（排版时画上去的），按上下文补回；词的换头留白记成 `¶` |
| `paras.tsv` | 通读时记 | 版面上看不出来的分段点（主要是跨页分段） |
| `hardchars.tsv` | **脚本自动维护** | 手圈难字的账本，见下面「权威关系」 |
| `释义-自拟.tsv` | 人工写 | 三本词典都查不着的词的释义（220 条）、自动抓不到的例句（232 条），外带一列拼音订正 |

### 外来参考资料（在 `tools/data/` 下，只作判据，不直接进 `data.js`）

- `常用字-一级3500.txt`　《通用规范汉字表》一级字表。**难写字就按它判。**
- `成语表.tsv`、`词语表.tsv`　第三方词典的词条和读音，只用来定「哪些算一个词、
  标准怎么读」，词本身还得在课本语料里出现过才收。
- `成语释义.json`、`新华词语.json`　词语板块的释义来源（v0.21 入库，36 MB）。
  **来源、用途和这套 OCR 数据的四类固定残缺（开引号被吃、义项序号、例句混 ★～
  符号、常给古义），记在 `tools/data/来源.md`。** 清洗在 `words.py` 的 `clean_def()`。

### 拼音订正

`释义-自拟.tsv` 的第四列。自动注音错了、又确实该收的词写在那儿，覆盖 pypinyin
的结果。现在只有一条：**「岑参」诗人名里的「参」读 shēn**，机器按常用音注成 cān。
专名读音没有系统校验，将来专名多了要单想办法。

---

## 三、权威关系（难字）

难字是使用者在 `data.js` 的正文里加大括号圈出来的，所以：

- **`data.js` 是权威**——人在那儿圈字。
- **`hardchars.tsv` 是账本和备份**——记「你加了哪些、又把哪些自动判定的字删了」。

重跑 `build.py` 时先把 `data.js` 里的括号读出来（`harvest()`），生成时原样带回去，
**不会冲掉手圈的**。只有 `data.js` 不在（刚 clone 下来）才反过来拿账本还原。

> 改「难写字」的判定规则时，要跑 `build.py --regen`，否则 `harvest()` 会把上一版的
> 自动结果当成手圈留下来，新规则等于白改。

---

## 四、用户数据：`user.js`

和语料完全分开的一个文件，挂在 `window.USER` 上，页面导出、用户替换：

```js
window.USER = {
  marks: [ ... ],   // 标记过的题（按句子内容存的 key，不是题号）
  wrong: { ... },   // 错题：key → 错了几次
  weak:  { ... }    // 每题具体哪几处不会（逗号分号切出的小句为单位）
};
```

- **按句子内容存 key，不按题号**——所以往 `data.js` 中间插内容不会让标记错位。
- 平时答题痕迹存在浏览器 `localStorage`，`user.js` 是存档点。网页没权限写本地文件，
  换设备前要在「我的数据」里导出一次。

---

## 五、手机单文件版

`merge.py` 把 `index.html` + `data.js` + `user.js` 内联成一个
`古诗文默写_单文件.html`（安卓某些文件管理器读不到同目录文件时用）。
**改完 `data.js` 要重跑一次 `merge.py`**，否则单文件版还是旧的。
