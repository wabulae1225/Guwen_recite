# 改动手册 modify_manual

> 这份文档规定 **AI 在这个仓库里怎么干活**。任何要改动项目的人（尤其是 Claude Code）
> 动手前必须先读它。它不是建议，是**停止点**：下面标 🛑 的节点，未经使用者确认不得跨越。

---

## 一、一轮工作的流程

每一轮由使用者提出想法和要求，AI 按下面走。带 🛑 的是硬性停止点。

1. **分析意图**。读相关文档（`README.md` 指路、`data_structure.md` / `html_structure.md` /
   `extract_pdf_method.md` 看现状、`LOG.md` 看来龙去脉），必要时查代码、跑只读命令了解现状。
2. **复述 + 提建议**。把使用者的想法复述一遍，附上自己认为可以改进的地方。
   **复述和建议本身就是理解程度的凭据**：说得准说明对齐了，说偏了使用者当场能指出来。
3. **问清歧义**。有看不懂或有歧义的地方直接问，别猜着做。使用者指出理解和实际想法之间的偏差，
   经过一到两轮讨论达成一致。
4. 🛑 **对齐确认**。使用者认可复述和方向之后，才进入下一步。**没确认不许开工。**
5. **给执行方案**。列出这一轮具体要改什么、按什么顺序、**以及本轮的验收方式**
   （改完做什么操作、应该看到什么结果——见第三节）。
6. 🛑 **方案确认**。使用者看过执行方案、同意了，才动手。**没同意不许写文件。**
7. **执行**。按方案改，每个大改动一次提交，commit message 带版本号（如 `[v0.2] …`）。
8. **验收**。按第 5 步写好的验收方式实际跑一遍，把结果给使用者看。
9. **记日志**。在 `LOG.md` 末尾追加本轮记录（见第四节）。

> 两个停止点（🛑 第 4、6 步）是这份手册的核心。把流程写成停止点而不是步骤，是因为
> 「步骤」容易被当成建议性参考、顺手就跨过去了；停止点是明确的「到这儿必须停下等确认」。

---

## 二、授权三档

使用者对每件事的授权分三档，照档位行事：

| 档位 | 使用者说了什么 | AI 怎么做 |
|---|---|---|
| **明确** | 明确说了要怎么做 | 照做，**不要自作主张** |
| **未提** | 没说到的地方 | **酌情提问**，别默默替他定 |
| **自行决定** | 「你自己定」「自行决定」 | 自己定——**但有边界，见下** |

**「自行决定」的边界**：即便使用者说了自行决定，碰到下面三条也要**回来问一句**：

1. **不改既有约束**——已经定下的规则、原则（比如「和书上一样」「难字按篇圈」）。
2. **不改数据格式**——`data.js` 六板块的字段格式、`user.js` 结构、中间稿格式。
3. **不引入新依赖**——除了已在用的（pymupdf、pypinyin、playwright），不擅自加库。

超出这三条的，哪怕说了自行决定，也回来确认。

---

## 三、验收方式（第 5 步必写）

执行方案里必须写清这一轮**怎么算做完了**，否则「本轮解决了某问题」这句话无从核验。
验收要具体到「做什么操作 → 应该看到什么结果」。举例：

- 改了版面解析 → 跑 `pdfdump.py && assemble.py && build.py`，三道自检全过
  （角标配平、词条回查、例句完整）。
- 改了页面 → 起 Playwright 无头浏览器开 `index.html`，六模式都能开、控制台无报错。
- 改了某篇的分段 → 跑小脚本打印那篇的段落，段数和内容跟课本对上。

**没有可核验的验收标准，不算合格的执行方案。**

---

## 四、记日志的规矩

> 使用者反复强调过（v0.21、v0.22、v0.23 一路说到定为规矩）：
> **日志首先是「对话实录」，其次才是技术总结。** 原话——「每次你和我说了啥，
> 你都写到 log 里面，你和我说了一二三我看是次要的，最重要的是你下次得看到看懂，
> 日志就首先应该包含我们对话的所有内容和你的汇报」。理由是**换窗口后接手的 AI
> 靠日志还原现场**，AI 提炼过的「一二三」丢掉了使用者的原话、语气和当时的来龙去脉，
> 下一个人就看不懂为什么这么定。所以：

- 每轮结束在 `LOG.md` 末尾**追加**，头一节必须是 **`### 对话实录`**：
  - **使用者说的话尽量原样引**（他的需求、对选项的回答、临时补的要求、对工作方式的
    交代），别只写「使用者同意了」——同意的是哪句、有没有附加条件，都要留住。
  - **我给他的汇报也照录**：提了哪几条、报了什么数、发现了什么问题。
  - 「他说了什么 → 我怎么回 → 他怎么定」这条链要连得起来。
- 对话实录之后，再写技术总结：想解决什么、实际改了什么、试过哪些方案
  （**成功的和失败的都写**，写明当时为什么这么试）。
- **正序、只追加、越往后越新，旧条目永不删改。** 后来发现某轮结论错了，在那一轮下面
  补一行「已被 vX 推翻/修正」，把错误保留下来。
- commit message 带版本号，代码改动和日志条目对得上。

---

## 五、文档分工（改哪份）

- **历时**（发生过什么）→ `LOG.md`，只追加。
- **现状**（现在是什么样）→ `data_structure.md` / `html_structure.md` /
  `extract_pdf_method.md`，原地更新。
- 同一件事只在一处写实体，别处放指针，别让两份文档各说各话。

（`restrictions.md` 禁令库、`CORPUS-FORMAT.md` 挖空规则等文档，等真正需要时再建，
建了在这儿和 `README.md` 补上。）
